---
sidebar_position: 3
---

import { SortableDataTable, SortableDataButton } from "@site/src/components/SortableDataTable";
import { columns, devices, moduleTests, manufacturer_to_bubbles } from "./devices.table";

# Geräte

***WARP Charger und WARP Energy Manager können direkt auf fremde Geräte zugreifen und Daten von diesen abrufen. So kann meistens ein PV-Überschussladen oder ein dynamisches Lastmanagement OHNE zusätzliche Hardware realisiert werden,
wenn zum Beispiel ein Stromzähler für eine PV-Anlage bereits installiert wurde!***

Die folgende Geräteübersicht führt Geräte auf, die direkt unterstützt werden. Zusätzlich werden Informationen zu den bereitgestellten Daten und den Anwendungsmöglichkeiten aufgelistet.

:::note
Ist ein Gerät nicht aufgelistet besteht die Möglichkeit, das es dennoch über eine der [Schnittstellen](/docs/compatible_devices/interfaces) (z.B. Modbus/TCP mit benutzerdefinierter Registertabelle) integriert werden kann.

:::

***Messort***

Siehe hierzu: [Einführung - Messort](/docs/compatible_devices/introduction#messort-von-ger%C3%A4tedaten).
In grün dargestellt sind die unterstützten Messorte. Mögliche Messorte sind: *Netzanschluss*, *PV*, *Speicher* und *Last*.


<table>
<tr><td><div class="device available">Messort</div></td><td>Generell vom Gerät unterstützt. Entsprechende Hardware muss aber verbaut sein.</td></tr>
<tr><td><div class="device maybe">Messort</div></td><td>Diese Information liegt uns nicht vor. Solltest du Informationen hierzu besitzen, schicke uns doch eine Email an [info@tinkerforge.com](mailto:info@tinkerforge.com). Wir tragen diese Information dann nach.</td></tr>
</table>


***Anwendung***

Siehe hierzu: [Einführung - Anwendungen](/docs/compatible_devices/introduction#anwendungen)
In blau dargestellt sind die unterstützten Anwendungen. Mögliche Anwendungen sind: [*Dynamisches Lastmanagement (dLM)*](/docs/compatible_devices/introduction#dynamisches-lastmanagement), [*PV-Überschussladen (PVÜ)*](/docs/compatible_devices/introduction#pv-%C3%BCberschussladen) und die Steuerung eines [*Batteriespeichers (BAT)*](/docs/compatible_devices/introduction#batteriesteuerung).


:::note
Coming soon - Batteriesteuerung: Diese ist noch in der Entwicklung und hier aktuell noch nicht dokumentiert.
:::

<table>
<tr><td><div class="application available">Anwendung</div></td><td>Die Anwendung wird generell vom Gerät unterstützt. Entsprechende Hardware muss aber verbaut sein.</td></tr>
<tr><td><div class="application maybe">Anwendung</div></td><td>Diese Information liegt uns nicht vor. Solltest du Informationen hierzu besitzen, schicke uns doch eine Email an [info@tinkerforge.com](mailto:info@tinkerforge.com). Wir tragen diese Information dann nach.</td></tr>
</table>

:::note

Verschiedene oder auch neue Firmwareversionen seitens der Hersteller führen dazu,
dass sich manchmal Geräte-Schnittstellen ändern.
Solltest du Probleme mit einem der aufgeführten Geräte haben, oder aber
die Unterstützung eines Geräts wünschen, dann schreibe uns im Forum auf
[Tinkerunity](https://www.tinkerunity.org/forum/13-warp-charger/).

Wenn du ein Gerät besitzt, das du getestet hast und dieses noch nicht
in der Liste aufgeführt ist, schicke uns doch eine Email an
[info@tinkerforge.com](mailto:info@tinkerforge.com) damit wir die
Liste erweitern können. Solltest du eine eigene Modbus/TCP
Registertabelle definiert haben, schicke uns diese gerne zu, damit wir
diese hinterlegen können.

:::

# Geräteübersicht

<SortableDataTable
  columns={columns}
  data={ devices }
/>

:::note

Je nach Gerät gibt es oft diverse Anschlussmöglichkeiten und Hardwarekonfigurationen. Es erklärt sich von selbst, dass zum Beispiel auf Daten zum Netzanschluss
nur zugegriffen werden kann, wenn auch ein entsprechender Stromzähler an diesem Ort installiert ist.

:::

# Herstellerspezifische Informationen

## Alpha ESS

### SMILE-Serie
{manufacturer_to_bubbles("Alpha ESS")}

Die folgenden Alpha ESS Smile Heimspeicher unterstützen eine Anbindung mittels Modbus/TCP:
 * SMILE5
 * SMILE-Hi5
 * SMILE-Hi10
 * SMILE-G3-S3.6/B5/S5
 * SMILE-BAT-8.2PHA
 
Der Stromzähler "Netz" ist nur nutzbar, wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Die Geräteadresse bei Alpha ESS ist üblicherweise 85.

Um die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) nutzen zu können, muss ein mit dem Wechselrichter verbundener Stromzähler am Netzanschluss verbaut sein.

## APsystems

### Mikro-Wechselrichter
{manufacturer_to_bubbles("APsystems")}

APsystems Mikrowechselrichter können von APsystems ECU-R und ECU-C mittels SunSpec ausgelesen werden. Die Kommunikation zwischen Mikrowechselrichter und ECU erfolgt
über Zigbee. Folgende ECUs sind kompatibel:
 * ECU-R mit ID rule 2160XXXXXXXX und Version > 1.3.7
 * ECU-R mit ID rule 2162XXXXXXXX und Version > 2.0.2
 * ECU-C mit ID rule 215XXXXXXXXX und Version > C1.1.3

Es steht nur der Messwert für die Erzeugung (PV) bereit.

## Carlo Galvazzi

### EM24 (Ethernet) Modbus/TCP Stromzähler
{manufacturer_to_bubbles("Carlo Galvazzi")}
Carlo Galvazzi Stromzähler mit Modbus/TCP (über Ethernet) Schnittstelle können direkt ausgelesen werden.
Folgende Stromzähler können direkt per Ethernet ausgelesen werden:
 * EM24 in der Ausführung mit Ethernet (EM24-DIN, EM24-E1)

Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es können mehrere Stromzähler dieses Typs angelegt werden.
Um die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) nutzen zu können, muss der Stromzähler am Netzanschluss verbaut sein.

### EM-Serie und ET-Serie Modbus/RTU Stromzähler
{manufacturer_to_bubbles("Carlo Galvazzi")}
 
Folgende Stromzähler sind RS485 bzw. Modbus/RTU Stromzähler, so dass ein [Modbus/RTU Wandler](/docs/compatible_devices/interfaces/#modbusrtu-wandler) erforderlich ist:
 * EM100 / EM111 / EM112
 * ET100 / ET111 / ET112
 * EM210
 * EM270
 * EM280
 * EM300 / EM330 / EM340
 * ET300 / ET330 / ET340
 * EM510 / EM511
 * EM530
 * EM540
 * EM580

Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es können mehrere Stromzähler dieses Typs angelegt werden.
Um die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) nutzen zu können, muss der Stromzähler am Netzanschluss verbaut sein.

## Chisage

### ESS-Hybrid-Wechselrichter
{manufacturer_to_bubbles("Chisage")}

Chisage ESS-Hybrid-Wechselrichter werden unterstützt. Getestet wurde die Implementierung mit einem Mars-Wechselrichter.

## Deye

### SUN-Serie
{manufacturer_to_bubbles("Deye")}

Deye Hybrid-Wechselrichter der SUN Serie (Nieder- und Hochspannung) unterstützen Modbus/RTU.
Mit einem Umsetzer von Modbus/RTU auf Ethernet kann auf diese auch mittels Modbus/TCP zugegriffen werden. Siehe [Modbus/RTU Wandler](/compatible_devices/interfaces.md#modbusrtu-wandler).
Die ausgelesenen Werte für "Netz" beziehen sich auf den Netzanschluss des Wechselrichters. Der über diesen Anschluss geführte Strom wird gemessen. Das ggf. verbaute Smartmeter (Chint DSU666 etc.)
kann anscheinend NICHT über die Modbus/TCP Schnittstelle des Wechselrichters ausgelesen werden! Ist nur ein Wechselrichter vorhanden und wird der gesamte Strom über dessen Netzanschluss geführt, kann der Messwert "Netz" für ein PV-Überschussladen (PVÜ) und ein dynamisches Lastmanagement (dLM) verwendet werden.

Die Deye-Unterstützung  wird auch im Forum diskutiert: [Forumpost auf Tinkerunity](https://www.tinkerunity.org/topic/12271-weitere-wechserichter-modbustcp-supporten-deye-hybrid-wechselrichter/).


:::note
Anscheinend stellt, je nach Firmwareversion, auch das Deye WLAN Dongle direkt Modbus/TCP zur Verfügung. Es muss dann Port 8899 anstatt 502 als Modbus/TCP Port ausgewählt werden.
Es sieht aber so aus, als wenn das Dongle kein konformes Modbus/TCP spricht, so dass dies für uns nicht nutzbar ist. Leider gibt es hierzu unterschiedliche Berichte im Netz und keine offiziellen Informationen
von Deye.
:::

## E3DC

### Hauskraftwerk
{manufacturer_to_bubbles("E3DC")}

Es werden S10 und S20 Hauskraftwerke unterstützt.
E3DCs Hauskraftwerke unterstützen SunSpec und können somit einfach eingebunden werden. Die aktuellen E3DC Firmware (Stand 28.03.25) liefert aber keine korrekten Werte über SunSpec für den Speicher.
Daher stellen wir ein spezifisches E3DC Modbus/TCP Profil bereit.

Um dieses nutzen zu können müssen im Hauskraftwerk unter "Hauptmenü > Funktionen > Funktion Modbus" folgende Einstellungen vorgenommen werden:

 * Modbus aktiv
 * Modbus/TCP aktiv
 * Protokoll auf "E3DC" (E3/DC Simple-Mode)
 * Gerät auf "1" (Geräte ID)
 * Port auf "502"

Um die Anwendung PV-Überschussladen (PVÜ) nutzen zu können, muss ein Stromzähler am Netzanschluss verbaut sein. Dies sollte bei E3DC immer der Fall sein.
E3DC stellt keine Phasenströme zur Verfügung. Daher ist die Anwendung dynamisches Lastmanagement (dLM) nicht möglich. Soll dennoch ein dynamisches Lastmanagement realisiert werden, muss ein zusätzlicher Stromzähler, der Phasenströme bereitstellt, installiert werden.


## Eastron

### SDM630 und SMD72V2
{manufacturer_to_bubbles("Eastron")}

Eastron Stromzähler mit RS485 (Modbus/RTU) Schnittstelle können direkt am WARP Energy Manager angeschlossen und von diesem ausgelesen werden.
Bei den Typen SDM630 und SDM72V2 wird der Strom über den Zähler geführt. Diese Zähler werden also direkt in die Leitungen eingebaut.

Mit dem Typ SDM630MCT wird auch ein Typ unterstützt der externe Wandler nutzt. Dieser Zähler wird also nicht in die Leitungen eingebaut, sondern es
werden nur die Wandler um die Leitungen gelegt. Durch die Wahl der entsprechend dimensionierten Wandler können auch sehr große Ströme gemessen werden.

Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es kann nur ein Stromzähler dieses Typs angelegt werden, da die RS485 (Modbus/RTU) Schnittstelle
des WARP Energy Managers nur einen Stromzähler unterstützt. Um die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) nutzen zu können, muss der Stromzähler am Netzanschluss verbaut sein.

## elgris

### Smart Meter LAN 2.0
{manufacturer_to_bubbles("elgris")}

Das elgris Smart Meter LAN 2.0 kann einfach mittels SunSpec eingebunden werden. SunSpec sollte direkt auf dem Stromzähler aktiviert sein.
Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es können mehrere Stromzähler dieses Typs angelegt werden.
Um die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) nutzen zu können, muss der Stromzähler am Netzanschluss verbaut sein.

## Eltako

### DSZ15DZMOD Stromzähler
{manufacturer_to_bubbles("Eltako")}

Der Eltako Stromzähler DSZ15DZMOD mit RS485 (Modbus/RTU) Schnittstelle kann am WARP Energy Manager angeschlossen und von diesem ausgelesen werden.
Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es kann nur ein Stromzähler dieses Typs angelegt werden, da die RS485 (Modbus/RTU) Schnittstelle
des WARP Energy Managers nur einen Stromzähler unterstützt. Um die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) nutzen zu können, muss der Stromzähler am Netzanschluss verbaut sein.

## Fox ESS

### Hybrid Serie H3/AC3, H3 Smart, H3 Pro
{manufacturer_to_bubbles("Fox ESS")}

Fox ESS Wechselrichter scheinen nicht mittels Modbus/TCP über den Fox ESS LAN oder WLAN Dongle ausgelesen werden zu können. Sie sind aber mittels RS485 (Modbus/RTU) auszulesen.
Wird ein RS485 nach LAN oder WLAN Wandler installiert (siehe [Modbus/RTU Wandler](/compatible_devices/interfaces.md#modbusrtu-wandler)), so können diese Wechselrichter ebenfalls ausgelesen werden.
Am Wechselrichter muss die Geräte-ID von 000 (Modbus/RTU deaktiviert) auf 247 eingestellt werden. Die Einstellung dazu findet sich unter Settings -> Communication -> RS485 -> Device ID.
Das Wechselrichterpasswort ist standardmäßig auf 0000 gesetzt.

Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann. Nur in diesem Fall wird die Anwendung PV-Überschussladen (PVÜ) unterstützt.
Ein dynamisches Lastmanagement (dLM) scheint mit den Zählerdaten nicht möglich zu sein, da keine Phasenströme geliefert werden. Soll dennoch ein dynamisches Lastmanagement realisiert werden, muss ein zusätzlicher Stromzähler, der Phasenströme bereitstellt, installiert werden.

## Fronius

### Argeno String-Wechselrichter
{manufacturer_to_bubbles("Fronius", "Argeno String-Wechselrichter")}

Fronius Wechselrichter der Argeno-Serie sprechen SunSpec und können direkt ausgelesen werden. Es muss nur die Datenausgabe über Modbus/TCP aktiviert werden.
Als Port sollte 502 konfiguriert werden. Als SunSpec Model Type kann "float" gewählt werden.
Es steht nur der Messwert für die Erzeugung (PV) bereit.

### GEN24-Serie Hybrid-Wechselrichter
{manufacturer_to_bubbles("Fronius", "GEN24- und GEN24 Plus-Serie Hybrid-Wechselrichter")}

Fronius Wechselrichter der GEN24-Serie (GEN24 und GEN24 Plus) sprechen SunSpec und können direkt ausgelesen werden. Es muss nur die Datenausgabe über Modbus/TCP aktiviert werden.
Als Port sollte 502 konfiguriert werden. Als SunSpec Model Type kann "float" gewählt werden. Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler (Fronius Smart Meter) am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

### GEN24-Serie Hybrid-Wechselrichter (Speicher)
{manufacturer_to_bubbles("Fronius", "GEN24- und GEN24 Plus-Serie Hybrid-Wechselrichter (Speicher)")}
Die SunSpec Implementierung von Fronius ist leider unzureichend um einen angeschlossenen Speicher auslesen zu können. Aus diesem Grund haben wir zum Auslesen des Speichers eine eigenes Template für Modbus/TCP angelegt. Dieses ist für den Stromzähler "Speicher" zu wählen.



### Primo- und Symo-Serie String-Wechselrichter
{manufacturer_to_bubbles("Fronius", "Primo- und Symo-Serie String-Wechselrichter")}

Fronius Wechselrichter der Primo- und Symo-Serie und Symo Advanced-Serie sprechen SunSpec und können direkt ausgelesen werden. Es muss nur die Datenausgabe über Modbus/TCP aktiviert werden.
Als Port sollte 502 konfiguriert werden. Als SunSpec Model Type kann "float" gewählt werden. Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler (Fronius Smart Meter) am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

### Tauro-Serie String-Wechselrichter
{manufacturer_to_bubbles("Fronius", "Tauro-Serie String-Wechselrichter")}

Fronius Wechselrichter der Tauro-Serie sprechen SunSpec und können direkt ausgelesen werden. Es muss nur die Datenausgabe über Modbus/TCP aktiviert werden.
Als Port sollte 502 konfiguriert werden. Als SunSpec Model Type kann "float" gewählt werden. Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler (Fronius Smart Meter) am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

### Verto-Serie String-Wechselrichter
{manufacturer_to_bubbles("Fronius", "Verto-Serie String-Wechselrichter")}

Fronius Wechselrichter der Verto-Serie sprechen SunSpec und können direkt ausgelesen werden. Es muss nur die Datenausgabe über Modbus/TCP aktiviert werden.
Als Port sollte 502 konfiguriert werden. Als SunSpec Model Type kann "float" gewählt werden. Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler (Fronius Smart Meter) am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

### Verto Plus-Serie Hybrid-Wechselrichter
{manufacturer_to_bubbles("Fronius", "Verto Plus-Serie Hybrid-Wechselrichter")}

Fronius Hybrid-Wechselrichter der Verto Plus-Serie sprechen SunSpec und können direkt ausgelesen werden. Es muss nur die Datenausgabe über Modbus/TCP aktiviert werden.
Als Port sollte 502 konfiguriert werden. Als SunSpec Model Type kann "float" gewählt werden. Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler (Fronius Smart Meter) am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

Leider scheint es so, als wenn Fronius aktuell keine SunSpec Modelle zum Auslesen des Speichers unterstützen (Stand Mai 2025).


## Goodwe

### EHB Serie String-Wechselrichter
{manufacturer_to_bubbles("Goodwe", "EHB Serie String-Wechselrichter")}

Die Einbindung eines Wechselrichters der EHB Serie ist mittels Modbus/TCP über das LAN bzw. WiFi+LAN Modul möglich.
Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler (z.B. GM300) am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.
Die Geräteadresse ist typischerweise 247.

### EH, EM, ES, ET Hybrid-Wechselrichter
{manufacturer_to_bubbles("Goodwe", "EH, EM, ES, ET Hybrid-Wechselrichter")}

Die Einbindung eines Wechselrichters der EH, EM, ES, ET Serie ist mittels Modbus/TCP über das LAN bzw. WiFi+LAN Modul möglich.
Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler (z.B. GM300) am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.
Die Geräteadresse ist typischerweise 247.

### BT, SBP, BH-Serie Batterie-Wechselrichter
{manufacturer_to_bubbles("Goodwe", "BT, SBP, BH-Serie Batterie-Wechselrichter")}
Die Einbindung eines Wechselrichters der BT, SBP, BH-Serie ist mittels Modbus/TCP über das LAN bzw. WiFi+LAN Modul möglich.
Es ist uns leider unbekannt ob ein Stromzähler Netz zur Verfügung steht und ob dieser für ein PV-Überschussladen oder dynamisches Lastmanagement genutzt werden kann.
Die Geräteadresse ist typischerweise 247.

## Hailei

### HL-TPH10K
{manufacturer_to_bubbles("Hailei")}

Ein Auslesen ist mittels Modbus/TCP nach Modbus/RTU Wandlern (z.B. EW11) über die Modbus/RTU Schnittstelle des Wechselrichters möglich.
Siehe [Modbus/RTU Wandler](/compatible_devices/interfaces.md#modbusrtu-wandler).
Wechselrichter, die direkt einen LAN Anschluss bieten, können direkt über diesen LAN Anschluss mit Modbus/TCP ausgelesen werden. Die IP Adresse des Wechselrichters
lässt sich über das Display des Wechselrichters unter "Settings", dann "Ethernet" konfigurieren und auch einsehen.

:::note

Bei Anschluss direkt über den LAN Port des Wechselrichters
wird nur eine gleichzeitige Verbindung vom Wechselrichter unterstützt. Ein paralleler Zugriff von WARP Charger oder WARP Energy Manager zusammen mit anderen Geräten ist somit nicht möglich.
:::


Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.
Die Geräteadresse ist typischerweise 85.

## Hoymiles

### HMS- und HMT-Serie
{manufacturer_to_bubbles("Hoymiles")}

Hoymiles Mikrowechselrichter der Generation HMS und HMT können über die Hoymiles DTU-Pro-S Datenübertragungseinheit mittels SunSpec ausgelesen werden. Die Kommunikation erfolgt mittels Sub-1G.
Es steht nur der Messwert für die Erzeugung (PV) bereit.

## Huawei

### SUN2000
{manufacturer_to_bubbles("Huawei", "SUN-Serie Hybrid-Wechselrichter")}

Wechselrichter der SUN2000 Serie (2kW - 215kW) und der SUN8000-500KTL können über das Huawei Smart Dongle (ab Firmware SPC124) mittels Modbus/TCP ausgelesen werden.

Die unterstützen Modelle sind ab Seite 1 in dem Dokument [SUN2000MA Modbus Interface Definitions](https://support.huawei.com/enterprise/de/doc/EDOC1100387581?currentPartNo=k004&togo=content&section=k004) aufgeführt.

Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann (typischerweise "Huawei Smart Power Sensor" vom Typ DTSU666-H oder DDSU666-H).
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt. Speicher ist nur nutzbar im Zusammenspiel mit der LUNA2000 Batterie.

### EMMA
{manufacturer_to_bubbles("Huawei", "EMMA Energiemanager")}

Der Huawei EMMA-A02 Energiemanager kann mittels Modbus/TCP ausgelesen werden.


## Kaco

### Blueplanet Hybrid NH3 Hybrid-Wechselrichter
{manufacturer_to_bubbles("Kaco", "Blueplanet Hybrid NH3 Hybrid-Wechselrichter")}
Kaco Wechselrichter der NH3 Baureihe sprechen SunSpec und können direkt ausgelesen werden. Der Standardport ist 502. SunSpec muss im Protokoll Menü des Wechselrichters aktiviert werden.
Leider bietet Kaco über SunSpec anscheinend nur die Messung der Erzeugung (PV) auch wenn ein externer Stromzähler am Wechselrichter angeschlossen wurde. Auf die Netz-Zählerdaten kann man somit nicht zugreifen.
Es wird nur eine gleichzeitige Verbindung vom Wechselrichter unterstützt. Ein paralleler Zugriff von WARP Charger oder WARP Energy Manager zusammen mit anderen Geräten ist somit nicht möglich.

### Blueplanet NX3 und TL3 String-Wechselrichter
{manufacturer_to_bubbles("Kaco", "Blueplanet NX3 und TL3 String-Wechselrichter")}

Kaco Wechselrichter der NX3 und TL3 Baureihe sprechen SunSpec und können direkt ausgelesen werden. Der Standardport ist 502. SunSpec muss im Protokoll Menü des Wechselrichters aktiviert werden.
Leider bietet Kaco über SunSpec anscheinend nur die Messung der Erzeugung (PV) auch wenn ein externer Stromzähler am Wechselrichter angeschlossen wurde.  Auf die Netz-Zählerdaten kann man somit nicht zugreifen.
Es wird nur eine gleichzeitige Verbindung vom Wechselrichter unterstützt. Ein paralleler Zugriff von WARP Charger oder WARP Energy Manager zusammen mit anderen Geräten ist somit nicht möglich.

## Kostal

### Smart Energy Meter (KSEM G2)
{manufacturer_to_bubbles("Kostal", "Smart Energy Meter (KSEM G2)")}
Das Smart Energy Meter (KSEM G2) kann direkt mittels SunSpec kommunizieren.
Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es können mehrere Stromzähler dieses Typs angelegt werden.
Nur wenn der Stromzähler am Netzanschluss montiert wurde, werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

Seit Kostal Smart Eneergy Meter Firmware Version 2.7.0 muss auf dem Gerät die Modbus/TCP Slave Funktion erst aktiviert werden. Dies ist unter Modbus-Einstellungen -> Modbus TCP -> Slave -> "Aktiviere TCP-Slave" möglich.


### PIKO und PLENTICORE Wechselrichter
{manufacturer_to_bubbles("Kostal", "Piko und Plenticore")}
Kostal PIKO und PLENTICORE (G1, G2, G3, MP) können mittels SunSpec ausgelesen werden. Dazu muss auf dem Webinterface des Wechselrichters unter "Einstellungen" Modbus/TCP aktiviert werden.
Anschließend ist Erzeugung der Piko und Plenticore Wechselrichter (PV) auslesbar. Der Stromzähler "Netz" ist nur nutzbar, wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter direkt ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt. Verfügt der Wechselrichter über eine angeschlossene Batterie, so können
auch die Batteriedaten ausgelesen werden. Batteriewechselrichter, wie der PLENTICORE BI, liefern selbsterklärend keine PV Daten. Ist kein Stromzähler direkt am Wechselrichter angeschlossen,
so wird typischerweise ein KSEM G2 (siehe oben) von den Installateuren verbaut, so dass über diesen dann die Netzdaten abgerufen werden können.


## Q-Cells

### Q.HOME Serie
{manufacturer_to_bubbles("Solax", "Q.HOME Serie")}

Q-Cells nutzt Solax Hybridwechselrichter und bietet diese unter dem Produktnamen Q.Home an.
Siehe [Solax](/docs/compatible_devices/devices#solax).

### Q.VOLT Serie
{manufacturer_to_bubbles("Solax", "Q.VOLT Serie")}

Q-Cells nutzt Solax Stringwechselrichter und bietet diese unter dem Produktnamen Q.Volt an.
Siehe [Solax](/docs/compatible_devices/devices#solax).

## RCT

### RCT Power Speichersysteme
{manufacturer_to_bubbles("RCT")}

Batterie Wechselrichter der Firma RCT können mittels dem RCT Power Serial Communication Protokoll direkt angesprochen werden. Dazu ist ein Stromzähler mit der Klasse "RCT Power" anzulegen und die IP Adresse des Wechselrichters einzugeben.
Nur wenn ein Stromzähler am Netzanschluss montiert wurde, wird die Anwendung PV-Überschussladen (PVÜ) unterstützt. Ein dynamisches Lastmanagement (dLM) ist über diesen Zähler leider nicht möglich, da keine Phasenströme übermittelt werden.

## SAX Power

### SAX Power Home und Power Home Plus
{manufacturer_to_bubbles("SAX Power", "Power Home und Power Home Plus")}

Die SAX Power Batterien Power Home und Power Home Plus können mittels Modbus/TCP ausgelesen werden. Bei den älteren Varianten ist dazu Port 3600 und bei den neueren Varianten der Port 502 einzustellen.
Die Einstellung "Basic Mode" ist getestet. Der "Extended Mode" konnte bisher von uns noch nicht getestet werden.
Nur wenn ein Stromzähler am Netzanschluss montiert wurde, wird die Anwendung PV-Überschussladen (PVÜ) unterstützt. Ein dynamisches Lastmanagement (dLM) ist über diesen Zähler leider nicht möglich, da keine Phasenströme übermittelt werden.


## Shelly

### Shelly Pro EM, Shelly Pro 3EM, Shelly 3EM
{manufacturer_to_bubbles("Shelly")}

Alle Shelly Stromzähler, die über Modbus/TCP verfügen, werden unterstützt.
Shelly Stromzähler sind sehr schmal und benötigen nur wenig Platz und werden für verschiedene Maximalströme angeboten. Folgende Modelle werden direkt als Template unterstützt:

 * Shelly Pro EM-50 (1-phasig)
 * Shelly Pro 3EM 120A
 * Shelly Pro 3EM 400A
 * Shelly Pro 3EM 3CT 63
 * Shelly 3EM-63T Gen3 / 3EM-63W Gen3 

Wichtig ist ein Update auf die neuste Software, so dass Modbus/TCP unterstützt wird. Dieses muss dann unter Settings -> Modbus aktiviert werden.
Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es können mehrere Stromzähler dieses Typs angelegt werden.
Nur wenn der Stromzähler am Netzanschluss montiert wurde, werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

## Siemens

### SENTRON Energiezähler PAC-Serie
{manufacturer_to_bubbles("Siemens")}

Die folgenden Siemens SENTRON Energiezähler können abhängig vom konkreten Modell mittels Modbus/TCP ausgelesen werden:
 * PAC2200
 * PAC3120
 * PAC3200
 * PAC3220
 * PAC4200
 * PAC4220

Die Stromzähler gibt es auch in Varianten, die nicht über einen RJ45 Anschluss verfügen und dementsprechend auch nicht Modbus/TCP unterstützen! Diese werden nicht unterstützt.
Es gibt diverse Modelle entweder als direkter Stromzähler mit verschiedenen Messbereichen oder als Wandlerstromzähler.

Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es können mehrere Stromzähler dieses Typs angelegt werden.
Nur wenn der Stromzähler am Netzanschluss montiert wurde, werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

## SMA

### Sunny Boy 3.0 / 3.6 / 4.0 / 5.0 / 6.0
{manufacturer_to_bubbles("SMA", "Sunny Boy 3.0 / 3.6 / 4.0 / 5.0 / 6.0")}

Sunny Boy Wechselrichter sprechen SunSpec und können direkt ausgelesen werden. Dazu muss als Modbus Profil auf dem Wechselrichter das "SunSpec Modbus-Profil" aktiviert werden.
Laut SMA wird als Unit ID die für das "SMA Modbus-Profil" + 123 verwendet. Diese ist standardmäßig auf 3 gestellt, so dass die Unit ID für SunSpec 126 ist.
Zum Auslesen der Wechselrichterleistung ist mindestens Firmwareversion 4.00.23.R erforderlich.
Es steht nur der Messwert für die Erzeugung (PV) bereit.

### Sunny Boy Storage 3.7 / 4.0 / 5.0 / 6.0
{manufacturer_to_bubbles("SMA", "Sunny Boy Storage 3.7 / 4.0 / 5.0 / 6.0")}

Sunny Boy Storage Wechselrichter sprechen SunSpec und können direkt ausgelesen werden. Dazu muss als Modbus Profil auf dem Wechselrichter das "SunSpec Modbus-Profil" aktiviert werden.
Laut SMA wird als Unit ID die für das "SMA Modbus-Profil" + 123 verwendet. Diese ist standardmäßig auf 3 gestellt, so dass die Unit ID für SunSpec 126 ist.

Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

### Sunny Boy Smart Energy 3.6 / 4.0 / 5.0 / 6.0
{manufacturer_to_bubbles("SMA", "Sunny Boy Smart Energy 3.6 / 4.0 / 5.0 / 6.0")}

Sunny Boy Smart Energy Wechselrichter sprechen SunSpec und können direkt ausgelesen werden. Dazu muss als Modbus Profil auf dem Wechselrichter das "SunSpec Modbus-Profil" aktiviert werden.
Laut SMA wird als Unit ID die für das "SMA Modbus-Profil" + 123 verwendet. Diese ist standardmäßig auf 3 gestellt, so dass die Unit ID für SunSpec 126 ist.

Zum Auslesen der Wechselrichterleistung ist mindestens Firmwareversion  3.01.09.R erforderlich.
Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

### Sunny Tripower 3.0 / 4.0 / 5.0 / 6.0 / 8.0 / 10.0
{manufacturer_to_bubbles("SMA", "Sunny Tripower 3.0 / 4.0 / 5.0 / 6.0 / 8.0 / 10.0")}

Sunny Tripower Wechselrichter sprechen SunSpec und können direkt ausgelesen werden. Dazu muss als Modbus Profil auf dem Wechselrichter das "SunSpec Modbus-Profil" aktiviert werden.
Laut SMA wird als Unit ID die für das "SMA Modbus-Profil" + 123 verwendet. Diese ist standardmäßig auf 3 gestellt, so dass die Unit ID für SunSpec 126 ist.

Zum Auslesen der Wechselrichterleistung ist mindestens Firmwareversion 4.00.02.R erforderlich.
Es steht nur der Messwert für die Erzeugung (PV) bereit.

### Sunny Tripower Smart Energy 5.0 / 6.0 / 8.0 / 10.0
{manufacturer_to_bubbles("SMA", "Sunny Tripower Smart Energy 5.0 / 6.0 / 8.0 / 10.0")}

Sunny Tripower Smart Energy Wechselrichter sprechen SunSpec und können direkt ausgelesen werden. Dazu muss als Modbus Profil auf dem Wechselrichter das "SunSpec Modbus-Profil" aktiviert werden.
Laut SMA wird als Unit ID die für das "SMA Modbus-Profil" + 123 verwendet. Diese ist standardmäßig auf 3 gestellt, so dass die Unit ID für SunSpec 126 ist.

Zum Auslesen der Wechselrichterleistung ist mindestens Firmwareversion 3.01.09.R erforderlich.
Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

### Sunny Tripower CORE1
{manufacturer_to_bubbles("SMA", "Sunny Tripower CORE1")}

Sunny Tripower CORE1 Wechselrichter sprechen SunSpec und können direkt ausgelesen werden. Dazu muss als Modbus Profil auf dem Wechselrichter das "SunSpec Modbus-Profil" aktiviert werden.
Laut SMA wird als Unit ID die für das "SMA Modbus-Profil" + 123 verwendet. Diese ist standardmäßig auf 3 gestellt, so dass die Unit ID für SunSpec 126 ist.

Zum Auslesen der Wechselrichterleistung ist mindestens Firmwareversion 3.14.##.R erforderlich.
Es steht nur der Messwert für die Erzeugung (PV) bereit.

### Sunny Highpower PEAK3
{manufacturer_to_bubbles("SMA", "Sunny Highpower PEAK3")}

Sunny Highpower PEAK3 Wechselrichter sprechen SunSpec und können direkt ausgelesen werden. Dazu muss als Modbus Profil auf dem Wechselrichter das "SunSpec Modbus-Profil" aktiviert werden.
Laut SMA wird als Unit ID die für das "SMA Modbus-Profil" + 123 verwendet. Diese ist standardmäßig auf 3 gestellt, so dass die Unit ID für SunSpec 126 ist.

Zum Auslesen der Wechselrichterleistung ist mindestens Firmwareversion 4.##.##.R erforderlich.
Es steht nur der Messwert für die Erzeugung (PV) bereit.


### Sunny Home Manager 1.0 / 2.0, SMA Energy Meter 2.0
{manufacturer_to_bubbles("SMA", "Sunny Home Manager 1.0 / 2.0, SMA Energy Meter 2.0")}

Auf die Daten vom SMA Energy Meter 2.0 oder dem Sunny Home Manager kann mittels Speedwire Protokoll werden können. Über diese beiden kann die Leistung am Netzanschluss gemessen
werden, so dass ein PV-Überschussladen (PVÜ) und ein dynamisches Lastmanagement (dLM) mit diesen Zählerwerten möglich ist. Ein Zugriff auf andere Werte als Netzdaten ist nicht möglich (PV, Speicher etc.)

:::note
Speedwire nutzt Multicast. Nicht alle Switche und insbesondere viele DLAN/Powerline Komponenten leiten die Pakete nicht weiter. In diesem Fall empfängt die Wallbox oder der Energy Manager
dann keine Daten.

Im Sunnyportal können aber bis zu 3 IP-Adressen angegeben werden, zu denen die Daten per Unicast geschickt werden. Tritt ein Problem mit Multicast auf, kann somit dieses gelöst werden
indem die IP Adresse der Wallbox oder des Energy Managers eingetragen wird.
Diese können unter Konfiguration->Geräteübersicht->HomeManager  dann unter Eigenschaften->Zählerkonfiguration->Direkte Zähler Kommunikation angegeben werden.
Der Kontozugang muss dazu Adminrechte besitzen!

:::

## SolarEdge

### SolarEdge Synergie PV-Wechselrichter
{manufacturer_to_bubbles("SolarEdge", "SolarEdge Synergie PV-Wechselrichter")}

SolarEdge Synergie PV-Wechselrichter verfügen über eine SunSpec Schnittstelle. Der Port scheint für SolarEdge nicht **502** zu sein, sondern **1502**.
Ist die Schnittstelle nicht unter Port 1502 und auch nicht unter Port 502 freigegeben, muss die SunSpec Schnittstelle vermutlich erst aktiviert werden.
Unseres Wissens kann diese Schnittstelle nicht selbst aktiviert werden, sondern muss vom SolarEdge Support oder dem Installateur aktiviert werden.
In der App für Installateure "SetApp" (zur Inbetriebnahme) kann die Schnittstelle unter Anlagekommunikation -> Modbus TCP aktiviert werden.

Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.


### SolarEdge Home Wechselrichter
{manufacturer_to_bubbles("SolarEdge", "SolarEdge Home Wechselrichter")}

SolarEdge Home Wechselrichter verfügen über eine SunSpec Schnittstelle. Der Port scheint für SolarEdge nicht **502** zu sein, sondern **1502**.
Ist die Schnittstelle nicht unter Port 1502 und auch nicht unter Port 502 freigegeben, muss die SunSpec Schnittstelle vermutlich erst aktiviert werden.
Unseres Wissens kann diese Schnittstelle nicht selbst aktiviert werden, sondern muss vom SolarEdge Support oder dem Installateur aktiviert werden.
In der App für Installateure "SetApp" (zur Inbetriebnahme) kann die Schnittstelle unter Anlagekommunikation -> Modbus TCP aktiviert werden.

Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.
Über SunSpec steht zwar auch das Speicherkapazitäts-Modell 713 zur Verfügung, allerdings liefert dieses nicht die Leistung der Batterie. Daher haben wir ein Modbus/TCP
Profil erstellt um auch die Leistung und den SoC erfassen zu können (siehe nächsten Eintrag).

### SolarEdge Home Wechselrichter (Speicher)
{manufacturer_to_bubbles("SolarEdge", "SolarEdge Home Wechselrichter (Speicher)")}
SolarEdge stellt leider keine Leistungsdaten und den SoC per SunSpec zur Verfügung. Daher haben wir ein eigenes Modbus/TCP Profil für SunSpec angelegt.
Dieses dient nur dazu den Messort "Speicher" erfassen zu können. Alle anderen Messorte (PV, Netz etc.) erfolgen mittels SunSpec (siehe oben).

## Solarmax
{manufacturer_to_bubbles("Solarmax")}

Das Speichersystem MAX.STORAGE kann mittels Modbus/TCP ausgelesen werden. Den SoC und die Leistung der Batterie werden mit dem virtuellen Stromzähler "Speicher" bereitgestellt.
Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

## Solax

### X1 & X3 Hybrid
{manufacturer_to_bubbles("Solax", "X1 & X3 Hybrid")}

Solax Hybrid-Wechselrichter können über das WLAN Dongle mittels Modbus/TCP ausgelesen werden. Eine Einrichtungsanleitung inkl. der unterstützen Wechselrichter gibt es in der [Solax Power Academy](https://kb.solaxpower.com/de/solution/detail/ff8080818eae9899018ec71a12ca1ea6).
Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

### X1 & X3 Mini Boost MIC und Pro
{manufacturer_to_bubbles("Solax", "X1 & X3 Mini Boost MIC und Pro")}

Solax String-Wechselrichter können über das WLAN Dongle mittels Modbus/TCP ausgelesen werden. Eine Einrichtungsanleitung inkl. der unterstützen Wechselrichter gibt es in der [Solax Power Academy](https://kb.solaxpower.com/de/solution/detail/ff8080818eae9899018ec71a12ca1ea6).
Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom Wechselrichter ausgelesen werden kann.
Nur in diesem Fall werden die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) unterstützt.

## Sungrow

### SG-Serie
{manufacturer_to_bubbles("Sungrow", "SG-Serie")}

:::note

Wechselrichter und Dongle (WiNet-S/WiNet-S2) besitzen jeweils eine eigene Software. Beide sollten auf den aktuellen Stand gebracht werden!

Beim WiNet-S2 Firmware-Update v16 wird ggf. Port 502 deaktiviert und encrypted Port 516 aktiviert. Bei Nicht-Erreichbarkeit des WR nach einem Update bitte mit dem WiNet-S2 Hotspot ggf. Port 502 wieder aktivieren (i.d.R. ohne „SSL-Verschlüsselung“) falls ein bauseitiges EMS oder ein Proxy zugreift. Dann dem WiNet-S2 15-40 Minuten Zeit geben zum eventuellen Re-Initialisieren am Router.

Bei Auffälligkeiten mit der iSolarCloud WiNet-S(2) für 1 Minute abstecken. Falls weiterhin dann Aufruf mit https:// (mit s) und/oder im WiNet-S WebUI (https://192...) manuell auf iot.isolarcloud.eu umstellen, ggf. auch auf Port 16668.

:::

Sungrow Wechselrichter der Serien SGxxRT/SGxxRS/SGxxCX/SDxxCX-P2 können mittels Modbus/TCP ausgelesen werden. Am Wechselrichter angeschlossene Hardware-Stromzähler vom Typ DTSU666 o.ä., die von diesem ausgelesen werden können, können ebenfalls ausgelesen werden.
Nur wenn ein Stromzähler am Netzanschluss installiert wurde wird die Anwendung PV-Überschussladen (PVÜ) unterstützt. Ein dynamisches Lastmanagement (dLM) ist mit den Zählerdaten nicht möglich, da keine Phasenströme zur Verfügung gestellt werden.

Das Auslesen kann über einen WiNet-S mittels LAN/WLAN erfolgen (versionsabhängig). Hierbei scheint es aber je nach Hardwareversion und Firmware Probleme geben zu können. Sungrow empfiehlt, sofern vorhanden, direkt den LAN Anschluss
des Wechselrichters zu verwenden. Die Modbus/TCP Verbindung unterstützt oftmals anscheinend nur einen Client.

Ein älterer Dongle vom Typ Typ ***WiNet-S*** scheint immer Modbus/TCP über Port 502 bereitzustellen. Ist ein neuerer Dongle vom Typ **WiNet-S2** vorhanden, so muss die Option "Modbus TCP-Port 502 EIN/AUS" aktiviert werden. Vermutlich ist dies nur über den Installateurzugang möglich.
Die Aktivierung von Modbus/TCP für den WiNet-S2 ist in der iSolarCloud in der jeweiligen Anlage unter Geräte->Einstellungen->Allgemeine Parametereinstellungen zu finden.

Generell sollte das Gerät mit Geräteadresse 1 erreichbar sein. Anscheinend ist die Adresse nicht vom Nutzer konfigurierbar. Ein Nutzer hat uns mitgeteilt, dass bei ihm Geräteadresse 1 nicht funktioniert, das System aber unter Geräteadresse 4 erreichbar ist.

### SH-Serie
{manufacturer_to_bubbles("Sungrow", "SH-Serie")}

:::note

Wechselrichter und Dongle (WiNet-S/WiNet-S2) besitzen jeweils eine eigene Software. Beide sollten auf den aktuellen Stand gebracht werden!

Beim WiNet-S2 Firmware-Update v16 wird ggf. Port 502 deaktiviert und encrypted Port 516 aktiviert. Bei Nicht-Erreichbarkeit des WR nach einem Update bitte mit dem WiNet-S2 Hotspot ggf. Port 502 wieder aktivieren (i.d.R. ohne „SSL-Verschlüsselung“) falls ein bauseitiges EMS oder ein Proxy zugreift. Dann dem WiNet-S2 15-40 Minuten Zeit geben zum eventuellen Re-Initialisieren am Router.

Bei Auffälligkeiten mit der iSolarCloud WiNet-S(2) für 1 Minute abstecken. Falls weiterhin dann Aufruf mit https:// (mit s) und/oder im WiNet-S WebUI (https://192...) manuell auf iot.isolarcloud.eu umstellen, ggf. auch auf Port 16668.

:::

Sungrow Wechselrichter der Serien SHxxRT/SHxxRS/SHxxT können mittels Modbus/TCP ausgelesen werden. Am Wechselrichter angeschlossene Hardware-Stromzähler vom Typ DTSU666 o.ä., die von diesem ausgelesen werden können, können ebenfalls ausgelesen werden.
Nur wenn ein Stromzähler am Netzanschluss installiert wurde wird die Anwendung PV-Überschussladen (PVÜ) unterstützt. Ein dynamisches Lastmanagement (dLM) ist mit den Zählerdaten nicht möglich, da keine Phasenströme zur Verfügung gestellt werden.

Das Auslesen kann über einen WiNet-S mittels LAN/WLAN erfolgen (versionsabhängig). Hierbei scheint es aber je nach Hardwareversion und Firmware Probleme geben zu können. Sungrow empfiehlt, sofern vorhanden, direkt den LAN Anschluss
des Wechselrichters zu verwenden. Die Modbus/TCP Verbindung unterstützt oftmals anscheinend nur einen Client.

Ein älterer Dongle vom Typ Typ ***WiNet-S*** scheint immer Modbus/TCP über Port 502 bereitzustellen. Ist ein neuerer Dongle vom Typ **WiNet-S2** vorhanden, so muss die Option "Modbus TCP-Port 502 EIN/AUS" aktiviert werden. Vermutlich ist dies nur über den Installateurzugang möglich.
Die Aktivierung von Modbus/TCP für den WiNet-S2 ist in der iSolarCloud in der jeweiligen Anlage unter Geräte->Einstellungen->Allgemeine Parametereinstellungen zu finden.

Generell sollte das Gerät mit Geräteadresse 1 erreichbar sein. Anscheinend ist die Adresse nicht vom Nutzer konfigurierbar. Ein Nutzer hat uns mitgeteilt, dass bei ihm Geräteadresse 1 nicht funktioniert, das System aber unter Geräteadresse 4 erreichbar ist.
## Tasmota

### Smart Meter Interface
{manufacturer_to_bubbles("Tasmota")}

Tasmota bietet ein [Smart Meter Interface](https://tasmota.github.io/docs/Smart-Meter-Interface/) damit können über ein IR-Lesekopf die Zählerwerte des Smart Meters vom Netzbetreiber ausgelesen werden.
Netzbetreiber nutzen zum Beispiel Smart Meter der Marke DZG (z.B.: DWS, DVS), EMH (z.B.: ED300, eHZ) oder Holley (z.B.: DTZ, EHZ). Eine vollständige Liste lässt sich unter dem oben genannten Link finden.

Das Smart Meter ist typischerweise mit einem Pin gesichert und muss zuvor mittels Pin freigegeben werden. Den Pin erhält man von seinem Netzbetreiber. Es werden selbstgebaute Leseköpfe unterstützt, welche typischerweise aus einem
ESP32 und einer Fotodiode bestehen. Es gibt aber auch Fertiglösungen wie den "Hichi TTL - Lesekopf".

Technisch muss für das jeweilige Zählermodell der entsprechende [Smart Meter Descriptor](https://tasmota.github.io/docs/Smart-Meter-Interface/#smart-meter-descriptors) im ESP32 des Lesekopf hinterlegt werden.
Kann darüber der Zähler ausgelesen werden, so kann der Leistungs-Messwert (ab Tasmota Version 13.x) mittels der [HTTP API](/docs/interfaces/mqtt_http/api_reference/meters.mdx)
 an einen WARP Charger oder einen WARP Energy Meter geschickt werden.

Dazu wird auf dem WARP Energy Manager oder WARP Charger ein API Stromzähler konfiguriert. Dieser erhält nur den einzigen Wert "Wirkleistung (Bezug minus Einspeisung); Σ L1, L2, L3 \[W\]".
Dieser Wert wird dann aus dem Tasmota SML Skript mittels folgender Zeile übertragen:

```
=>WebQuery http://192.168.2.30/meters/0/update POST [Content-Type:application/json] [%sml[3]%]
```

Im unten angegeben Beispielskript für einen DZG DWS7412 werden insgesamt 4 Messwerte extrahiert, die dann entsprechend ihrer Reihenfolge über die Variablen sml\[1\] bis sml\[4\] zugreifbar sind. Im Beispielfall enthält sml\[3\] den gewünschten Wert für Leistung in W. Dieser Wert Wert wird mittels HTTP Post an den API Zähler 0 übertragen. Zusätzlich werden alle sml Messwerte anschließend im Log ausgegeben (print). Das print Statement dient nur der Kontrolle und kann nach erfolgreicher Bestimmung des gewünschten Wertes entfernt werden.

```
>D
>B
=>sensor53 r
>S
=>WebQuery http://192.168.2.30/meters/0/update POST [Content-Type:application/json] [%sml[3]%]
print %sml[1]%,%sml[2]%,%sml[3]%,%sml[4]%
>M 1
+1,3,s,16,9600,DWS7412
1,77070100010800ff@1000,Energie,kWh,energy,4
1,77070100020800ff@1000,Lieferung,kWh,en_out,4
1,77070100100700ff@1,Leistung,W,power,2
1,7707010060320101@#,SID,,meter_id,0
```

Diese Lösung lässt sich nur für ein PV-Überschussladen (PVÜ) verwenden, da nur die Gesamtleistung übertragen wird. Für ein dynamisches Lastmanagement (dLM) ist auch die Übertragung der Phasenströme notwendig. Auch diese werden von einigen Smart Metern mit übertragen. In diesem muss der API Zähler entsprechend um die zusätzlichen 3 Werte erweitert und im Skript der POST Request ergänzt werden.

Hier der [Tinkerunity Blogpost](https://www.tinkerunity.org/topic/12255-z%C3%A4hler-des-netzbetreibers-sml-ir-hichi%E2%80%A6-f%C3%BCr-pv-%C3%BCberschuss/?do=findComment&comment=55374)
auf dem diese Beschreibung ursprünglich basiert.

## Tinkerforge

### HTTP API
{manufacturer_to_bubbles("Tinkerforge", "WARP Meters API HTTP")}

Über die [HTTP API](/docs/interfaces/mqtt_http/introduction#http) kann auf WARP Produkte zugegriffen werden. Das [Meters](/docs/interfaces/mqtt_http/api_reference/meters) Modul ist Teil der API und erlaubt es angelegte API Stromzähler mit Daten zu versorgen.
Weitere Informationen wie dies funktioniert sind auf der Seite [Schnittstellen](/docs/compatible_devices/interfaces#api) verfügbar.

### MQTT API
{manufacturer_to_bubbles("Tinkerforge", "WARP Meters API MQTT")}

Über die [MQTT API](/docs/interfaces/mqtt_http/introduction#mqtt) kann auf WARP Produkte zugegriffen werden. Das [Meters](/docs/interfaces/mqtt_http/api_reference/meters) Modul ist Teil der API und erlaubt es angelegte API Stromzähler mit Daten zu versorgen.
Weitere Informationen wie dies funktioniert sind auf der Seite [Schnittstellen](/docs/compatible_devices/interfaces#api) verfügbar.

## Varta

### VARTA Element, One, Pulse, Pulse neo, Link, Flex Storage
{manufacturer_to_bubbles("Varta", "VARTA Element, One, Pulse, Pulse neo, Link, Flex Storage")}

Auf Varta Speicher kann mittels Modbus/TCP zugegriffen werden. Die Geräte unterstützen den Zugriff auf die Daten vom Netzanschluss (sofern vorhanden) und vom Speicher.

## Victron Energy

### GX und damit verbundene Geräte
{manufacturer_to_bubbles("Victron Energy", "GX und damit verbundene Geräte")}

Victron Geräte, wie zum Beispiel die bekannten Multiplus oder Quattro Wechselrichter, werden üblicherweise von einem [GX Gerät](https://www.victronenergy.com/live/venus-os:start) gesteuert.
Für diese Geräte kann Modbus/TCP aktiviert werden, so dass auf die Daten mittels Modbus/TCP zugegriffen werden kann.

Ein GX Gerät stellt unter "Services" eine Zusammenfassung aller angeschlossen Geräte wie Inverter, Batterien etc. dar. Auch wenn diese Geräte per VE.BUS angeschlossen sind. Die Daten werden unter "System" summiert.
Werden als Beispiel mehrere Wechselrichter parallel betrieben, so stellt "System" die Summe der Erzeugung da. Wir unterstützen den Zugriff auf die "System"-Daten für "Netzanschluss",
"Last", "Wechselrichter" und "Speicher". Üblicherweise ist die **Geräteadresse 100**.

:::note
Die Geräteadressen für Modbus/TCP entsprechen typischerweise Victrons "Unit IDs". Laut Victron können sich diese auch unterscheiden, wenn die "Device Instance IDs" größer als 247 sind.
Unter "Settings → Integrations → Modbus TCP server → Available services" können die Geräteadressen für Modbus/TCP gefunden werden.
:::

Technisch nutzen wir folgende Modbus/TCP Register:
 * Netzanschluss: Grid Power L1-L3 (Register 820-822)
 * PV - Summe aus allen PV-Erzeugern (AC, DC):
   * AC Coupled PV On Output L1-L3 (Register 808-810)
   * PV - AC-coupled on input L1-L3 (Register 811-813)
   * PV - AC-coupled on generator L1-L3 (Register 814-816)
   * PV - DC-coupled power (Register 850)
 * Speicher: Voltage (Register 840), Current (Register 841), Power (Register 842) und SoC (Register 843)
 * Last: AC Consumption L1-L3 (Register 817-819) 

Der Stromzähler "Netz" ist nur nutzbar wenn ein externer Hardware-Stromzähler am Netzanschluss installiert wurde und vom System ausgelesen werden kann. Wird der gesamte Netz-Anschluss über Multiplus-Wechselrichter geleitet, so können diese
als Netzstromzähler fungieren. In diesem Fall ist nicht ein extra Netzstromzähler notwendig.
Die Anwendung PV-Überschussladen (PVÜ) wird nur unterstützt, wenn Messwerte vom Netzanschluss zur Verfügung stehen.
Ein dynamisches Lastmanagement (dLM) ist mit den "System"-Zählerdaten nicht möglich, da keine Phasenströme geliefert werden. Soll dennoch ein dynamisches Lastmanagement realisiert werden, muss ein zusätzlicher Stromzähler, der Phasenströme bereitstellt, installiert werden.

### EM540, ET340, ET112, EM24 Stromzähler
{manufacturer_to_bubbles("Victron Energy", "EM540, ET340, ET112, EM24 Stromzähler")}
Diese Stromzähler sind technisch Carlo Galvazzi Stromzähler und können somit als [Carlo Galvazzi Stromzähler](/docs/compatible_devices/devices#carlo-galvazzi) angelegt werden.

## YTL

### DEM4A Stromzähler
{manufacturer_to_bubbles("YTL")}

Der YTL Stromzähler DEM4A mit RS485 (Modbus/RTU) Schnittstelle kann direkt am WARP Energy Manager angeschlossen und von diesem ausgelesen werden.
Ein Stromzähler kann immer genau nur einem Messort zugeordnet werden. Der Messort ist abhängig vom Montageort des Stromzählers. Es kann nur ein Stromzähler dieses Typs angelegt werden, da die RS485 (Modbus/RTU) Schnittstelle
des WARP Energy Managers nur einen Stromzähler unterstützt. Um die Anwendungen PV-Überschussladen (PVÜ) und dynamisches Lastmanagement (dLM) nutzen zu können, muss der Stromzähler am Netzanschluss verbaut sein.

