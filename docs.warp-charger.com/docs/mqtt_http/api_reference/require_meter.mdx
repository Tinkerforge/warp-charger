---
description: Zählerüberwachung
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/require_meter.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Zählerüberwachung

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `require_meter/config` {#require_meter_config_warp1}


Die Konfiguration der Zählerüberwachung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```

#### Lesen
```bash
curl http://$HOST/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Schreiben
```bash
curl http://$HOST/require_meter/config -d '{ "config": 2 }'
```

oder abgekürzt:

```bash
curl http://$HOST/require_meter/config -d '2'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/require_meter/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '{ "config": 2 }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '2'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>int</th>
    <td>
        Konfiguration der Zählerüberwachung.
        - **0** - Es wurde noch nie ein Stromzähler gesehen. Zählerüberwachung deaktiviert. Sobald das erste Mal ein Stromzähler erkannt wird, wird dieser Wert automatisch auf 2 geändert.
        - **1** - Es wurde bereits einmal ein Stromzähler gesehen. Zählerüberwachung deaktiviert.
        - **2** - Es wurde bereits einmal ein Stromzähler gesehen. Zählerüberwachung aktiviert.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `require_meter/config` {#require_meter_config_warp2}


Die Konfiguration der Zählerüberwachung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp2-AbCd
```

#### Lesen
```bash
curl http://$HOST/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Schreiben
```bash
curl http://$HOST/require_meter/config -d '{ "config": 2 }'
```

oder abgekürzt:

```bash
curl http://$HOST/require_meter/config -d '2'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp2/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/require_meter/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '{ "config": 2 }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '2'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>int</th>
    <td>
        Konfiguration der Zählerüberwachung.
        - **0** - Es wurde noch nie ein Stromzähler gesehen. Zählerüberwachung deaktiviert. Sobald das erste Mal ein Stromzähler erkannt wird, wird dieser Wert automatisch auf 2 geändert.
        - **1** - Es wurde bereits einmal ein Stromzähler gesehen. Zählerüberwachung deaktiviert.
        - **2** - Es wurde bereits einmal ein Stromzähler gesehen. Zählerüberwachung aktiviert.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `require_meter/config` {#require_meter_config_warp3}


Die Konfiguration der Zählerüberwachung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp3-AbCd
```

#### Lesen
```bash
curl http://$HOST/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Schreiben
```bash
curl http://$HOST/require_meter/config -d '{ "config": 2 }'
```

oder abgekürzt:

```bash
curl http://$HOST/require_meter/config -d '2'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp3/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/require_meter/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '{ "config": 2 }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '2'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>int</th>
    <td>
        Konfiguration der Zählerüberwachung.
        - **0** - Es wurde noch nie ein Stromzähler gesehen. Zählerüberwachung deaktiviert. Sobald das erste Mal ein Stromzähler erkannt wird, wird dieser Wert automatisch auf 2 geändert.
        - **1** - Es wurde bereits einmal ein Stromzähler gesehen. Zählerüberwachung deaktiviert.
        - **2** - Es wurde bereits einmal ein Stromzähler gesehen. Zählerüberwachung aktiviert.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="wem2">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="any">
## `require_meter/config` {#require_meter_config_any}


Die Konfiguration der Zählerüberwachung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```

#### Lesen
```bash
curl http://$HOST/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Schreiben
```bash
curl http://$HOST/require_meter/config -d '{ "config": 2 }'
```

oder abgekürzt:

```bash
curl http://$HOST/require_meter/config -d '2'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/require_meter/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '{ "config": 2 }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '2'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>int</th>
    <td>
        Konfiguration der Zählerüberwachung.
        
        
        
        - **0** - Es wurde noch nie ein Stromzähler gesehen. Zählerüberwachung deaktiviert. Sobald das erste Mal ein Stromzähler erkannt wird, wird dieser Wert automatisch auf 2 geändert.
        - **1** - Es wurde bereits einmal ein Stromzähler gesehen. Zählerüberwachung deaktiviert.
        - **2** - Es wurde bereits einmal ein Stromzähler gesehen. Zählerüberwachung aktiviert.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "require_meter/config",
    "id": "require_meter_config",
    "level": 2,
    
},
];
