---
description: Zeiteinstellungen der Real-Time Clock
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/rtc.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Zeiteinstellungen der Real-Time Clock
**Benötigt das Feature <a href="#features_nfc"><code>"rtc"</code></a>.**

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `rtc/time` {#rtc_time_warp1}


Die aktuelle Zeit auf der Real-Time Clock.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
```bash
curl http://$HOST/rtc/time -d '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/time<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/time_update -m '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`year`<br/>int</th>
    <td>
        Das Jahr. 2000 bis 2099
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`month`<br/>int</th>
    <td>
        Der Monat. 1 bis 12
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`day`<br/>int</th>
    <td>
        Der Tag. 1 bis 31
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hour`<br/>int</th>
    <td>
        Die Stunde. 0 bis 23
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minute`<br/>int</th>
    <td>
        Die Minute. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`second`<br/>int</th>
    <td>
        Die Sekunde. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`weekday`<br/>int</th>
    <td>
        Der Wochentag. 0 (Montag) bis 6 (Sonntag).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `rtc/config` {#rtc_config_warp1}


Die Real-Time-Clock-Konfiguration.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
```bash
curl http://$HOST/rtc/config -d '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
curl http://$HOST/rtc/config -d 'false'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m 'false'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`auto_sync`<br/>bool</th>
    <td>
        Gibt an, ob beim Laden des Webinterfaces automatisch die Zeit auf der RTC gestellt werden soll, falls keine Netzwerk-Zeitsynchronisierung vorliegt.
        - **true** - Die RTC-Zeit wird automatisch auf die Systemzeit des Geräts gesetzt, dass das Webinterface aufgerufen hat.
        - **false** - Die RTC-Zeit wird nur manuell gesetzt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `rtc/time` {#rtc_time_warp2}


Die aktuelle Zeit auf der Real-Time Clock.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp2-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
```bash
curl http://$HOST/rtc/time -d '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp2/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/time<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/time_update -m '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`year`<br/>int</th>
    <td>
        Das Jahr. 2000 bis 2099
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`month`<br/>int</th>
    <td>
        Der Monat. 1 bis 12
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`day`<br/>int</th>
    <td>
        Der Tag. 1 bis 31
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hour`<br/>int</th>
    <td>
        Die Stunde. 0 bis 23
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minute`<br/>int</th>
    <td>
        Die Minute. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`second`<br/>int</th>
    <td>
        Die Sekunde. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`weekday`<br/>int</th>
    <td>
        Der Wochentag. 0 (Montag) bis 6 (Sonntag).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `rtc/config` {#rtc_config_warp2}


Die Real-Time-Clock-Konfiguration.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp2-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
```bash
curl http://$HOST/rtc/config -d '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
curl http://$HOST/rtc/config -d 'false'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp2/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m 'false'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`auto_sync`<br/>bool</th>
    <td>
        Gibt an, ob beim Laden des Webinterfaces automatisch die Zeit auf der RTC gestellt werden soll, falls keine Netzwerk-Zeitsynchronisierung vorliegt.
        - **true** - Die RTC-Zeit wird automatisch auf die Systemzeit des Geräts gesetzt, dass das Webinterface aufgerufen hat.
        - **false** - Die RTC-Zeit wird nur manuell gesetzt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `rtc/time` {#rtc_time_warp3}


Die aktuelle Zeit auf der Real-Time Clock.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp3-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
```bash
curl http://$HOST/rtc/time -d '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp3/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/time<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/time_update -m '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`year`<br/>int</th>
    <td>
        Das Jahr. 2000 bis 2099
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`month`<br/>int</th>
    <td>
        Der Monat. 1 bis 12
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`day`<br/>int</th>
    <td>
        Der Tag. 1 bis 31
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hour`<br/>int</th>
    <td>
        Die Stunde. 0 bis 23
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minute`<br/>int</th>
    <td>
        Die Minute. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`second`<br/>int</th>
    <td>
        Die Sekunde. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`weekday`<br/>int</th>
    <td>
        Der Wochentag. 0 (Montag) bis 6 (Sonntag).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `rtc/config` {#rtc_config_warp3}


Die Real-Time-Clock-Konfiguration.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp3-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
```bash
curl http://$HOST/rtc/config -d '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
curl http://$HOST/rtc/config -d 'false'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp3/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m 'false'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`auto_sync`<br/>bool</th>
    <td>
        Gibt an, ob beim Laden des Webinterfaces automatisch die Zeit auf der RTC gestellt werden soll, falls keine Netzwerk-Zeitsynchronisierung vorliegt.
        - **true** - Die RTC-Zeit wird automatisch auf die Systemzeit des Geräts gesetzt, dass das Webinterface aufgerufen hat.
        - **false** - Die RTC-Zeit wird nur manuell gesetzt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `rtc/time` {#rtc_time_wem}


Die aktuelle Zeit auf der Real-Time Clock.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
```bash
curl http://$HOST/rtc/time -d '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/time<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/time_update -m '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`year`<br/>int</th>
    <td>
        Das Jahr. 2000 bis 2099
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`month`<br/>int</th>
    <td>
        Der Monat. 1 bis 12
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`day`<br/>int</th>
    <td>
        Der Tag. 1 bis 31
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hour`<br/>int</th>
    <td>
        Die Stunde. 0 bis 23
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minute`<br/>int</th>
    <td>
        Die Minute. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`second`<br/>int</th>
    <td>
        Die Sekunde. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`weekday`<br/>int</th>
    <td>
        Der Wochentag. 0 (Montag) bis 6 (Sonntag).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `rtc/config` {#rtc_config_wem}


Die Real-Time-Clock-Konfiguration.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
```bash
curl http://$HOST/rtc/config -d '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
curl http://$HOST/rtc/config -d 'false'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m 'false'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`auto_sync`<br/>bool</th>
    <td>
        Gibt an, ob beim Laden des Webinterfaces automatisch die Zeit auf der RTC gestellt werden soll, falls keine Netzwerk-Zeitsynchronisierung vorliegt.
        - **true** - Die RTC-Zeit wird automatisch auf die Systemzeit des Geräts gesetzt, dass das Webinterface aufgerufen hat.
        - **false** - Die RTC-Zeit wird nur manuell gesetzt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `rtc/time` {#rtc_time_wem2}


Die aktuelle Zeit auf der Real-Time Clock.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem2-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
```bash
curl http://$HOST/rtc/time -d '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem2/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/time<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/time_update -m '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`year`<br/>int</th>
    <td>
        Das Jahr. 2000 bis 2099
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`month`<br/>int</th>
    <td>
        Der Monat. 1 bis 12
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`day`<br/>int</th>
    <td>
        Der Tag. 1 bis 31
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hour`<br/>int</th>
    <td>
        Die Stunde. 0 bis 23
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minute`<br/>int</th>
    <td>
        Die Minute. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`second`<br/>int</th>
    <td>
        Die Sekunde. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`weekday`<br/>int</th>
    <td>
        Der Wochentag. 0 (Montag) bis 6 (Sonntag).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `rtc/config` {#rtc_config_wem2}


Die Real-Time-Clock-Konfiguration.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem2-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
```bash
curl http://$HOST/rtc/config -d '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
curl http://$HOST/rtc/config -d 'false'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem2/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m 'false'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`auto_sync`<br/>bool</th>
    <td>
        Gibt an, ob beim Laden des Webinterfaces automatisch die Zeit auf der RTC gestellt werden soll, falls keine Netzwerk-Zeitsynchronisierung vorliegt.
        - **true** - Die RTC-Zeit wird automatisch auf die Systemzeit des Geräts gesetzt, dass das Webinterface aufgerufen hat.
        - **false** - Die RTC-Zeit wird nur manuell gesetzt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `rtc/time` {#rtc_time_any}


Die aktuelle Zeit auf der Real-Time Clock.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
```bash
curl http://$HOST/rtc/time -d '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/time
```
```jsx
{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/time<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/time_update -m '{
  "year": 2024,
  "month": 5,
  "day": 3,
  "hour": 12,
  "minute": 38,
  "second": 43,
  "weekday": 5
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`year`<br/>int</th>
    <td>
        Das Jahr. 2000 bis 2099
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`month`<br/>int</th>
    <td>
        Der Monat. 1 bis 12
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`day`<br/>int</th>
    <td>
        Der Tag. 1 bis 31
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hour`<br/>int</th>
    <td>
        Die Stunde. 0 bis 23
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minute`<br/>int</th>
    <td>
        Die Minute. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`second`<br/>int</th>
    <td>
        Die Sekunde. 0 bis 59
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`weekday`<br/>int</th>
    <td>
        Der Wochentag. 0 (Montag) bis 6 (Sonntag).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `rtc/config` {#rtc_config_any}


Die Real-Time-Clock-Konfiguration.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```

#### Lesen
```bash
curl http://$HOST/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
```bash
curl http://$HOST/rtc/config -d '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
curl http://$HOST/rtc/config -d 'false'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/rtc/config
```
```jsx
{ "auto_sync": false }
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/rtc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m '{ "auto_sync": false }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/rtc/config_update -m 'false'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Bedeutung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`auto_sync`<br/>bool</th>
    <td>
        Gibt an, ob beim Laden des Webinterfaces automatisch die Zeit auf der RTC gestellt werden soll, falls keine Netzwerk-Zeitsynchronisierung vorliegt.
        
        
        
        - **true** - Die RTC-Zeit wird automatisch auf die Systemzeit des Geräts gesetzt, dass das Webinterface aufgerufen hat.
        - **false** - Die RTC-Zeit wird nur manuell gesetzt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "rtc/time",
    "id": "rtc_time",
    "level": 2,
    
},{
    "value": "rtc/config",
    "id": "rtc_config",
    "level": 2,
    
},
];
