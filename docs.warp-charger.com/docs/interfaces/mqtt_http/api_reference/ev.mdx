---
description: Fahrzeuge
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/ev.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Fahrzeuge
Über das `ev`-Modul können Fahrzeug-Profile konfiguriert werden.
    Fahrzeuge werden anhand ihrer MAC-Adresse identifiziert, die sie z.B. beim Verbindungsaufbau
    per ISO 15118 (Autocharge) übermitteln oder die per [`ev/inject_ev`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_inject_ev) über die API vorgetäuscht werden kann.
    Anhand des zugeordneten Profils können Ladevorgänge einem Benutzer zugeordnet (Autocharge)
    und der Ladestand (SoC) des Fahrzeugs über Batteriekapazität und Ladeeffizienz geschätzt werden.
    
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="warp2">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="warp3">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="warp4">
## `ev/state` {#ev_state_warp4}


Der Zustand des aktuell verbundenen Fahrzeugs.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp4-AbCd
```
#### Lesen
```bash
curl http://$HOST/ev/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp4/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active_ev_index": 0,
    "name": "ID.4",
    "mac": "0A:1B:2C:D3:E4:F5",
    "soc": 42.5,
    "capacity": 77.0,
    "charging_efficiency": 0.92
}
```
Ein Fahrzeug ist verbunden und wurde dem ersten Fahrzeug-Profil zugeordnet.
Der geschätzte Ladestand beträgt 42,5 %.

:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active_ev_index`<br/>int</th>
    <td>
        Index des zugeordneten Fahrzeug-Profils in [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config). -1 falls kein Fahrzeug verbunden ist oder das Fahrzeug keinem Profil zugeordnet werden konnte.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Name des zugeordneten Fahrzeug-Profils. Leer, falls kein Fahrzeug verbunden ist oder das Fahrzeug keinem Profil zugeordnet werden konnte.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
    <td>
        MAC-Adresse des verbundenen Fahrzeugs im Format `AA:BB:CC:DD:EE:FF`. Leer, falls kein Fahrzeug verbunden ist.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`soc`<br/>float (<abbr title="Prozent">%</abbr>)</th>
    <td>
        Aktueller Ladestand (SoC) des Fahrzeugs in Prozent. Wird entweder vom Fahrzeug übermittelt (ISO 15118-20), über [`ev/inject_soc`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_inject_soc) gesetzt oder anhand der geladenen Energie, der Batteriekapazität und der Ladeeffizienz geschätzt. null, falls unbekannt.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`capacity`<br/>float (<abbr title="Kilowattstunde">kWh</abbr>)</th>
    <td>
        Batteriekapazität des verbundenen Fahrzeugs in kWh. Wird dem zugeordneten Fahrzeug-Profil entnommen oder vom Fahrzeug übermittelt (ISO 15118-20). null, falls kein Fahrzeug verbunden ist.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`charging_efficiency`<br/>float</th>
    <td>
        Ladeeffizienz des verbundenen Fahrzeugs: Verhältnis von gespeicherter zu verbrauchter Energie. Wird dem zugeordneten Fahrzeug-Profil entnommen. 0,92 falls kein Profil zugeordnet ist.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/seen_macs` {#ev_seen_macs_warp4}


Die MAC-Adressen der zuletzt verbundenen Fahrzeuge (maximal 8). Hilfreich, um die MAC-Adresse eines Fahrzeugs zu ermitteln, das in [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config) konfiguriert werden soll.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp4-AbCd
```
#### Lesen
```bash
curl http://$HOST/ev/seen_macs
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp4/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/seen_macs
```
        </TabItem>
    </Tabs>

```jsx
[
    { "mac": "0A:1B:2C:D3:E4:F5", "seen_at": 1234567 },
    { "mac": "5C:AF:06:12:34:56", "seen_at": 42000 }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[..]`<br/>object</th>
    <td>
        Eine gesehene MAC-Adresse
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Beschreibung</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
            <td>
                MAC-Adresse des Fahrzeugs im Format `AA:BB:CC:DD:EE:FF`.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`seen_at`<br/>int (<abbr title="Millisekunde">ms</abbr>)</th>
            <td>
                Zeitpunkt, zu dem die MAC-Adresse zuletzt gesehen wurde, in Millisekunden seit Systemstart.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/inject_soc` {#ev_inject_soc_warp4}


Setzt den Ladestand (SoC) des aktuell verbundenen Fahrzeugs über die API. Ausgehend von diesem Wert wird der Ladestand anhand der geladenen Energie, der Batteriekapazität und der Ladeeffizienz weiter geschätzt.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp4-AbCd
```
#### Schreiben
```bash
curl http://$HOST/ev/inject_soc -d '{ "soc": 42.5 }'
```

oder abgekürzt:

```bash
curl http://$HOST/ev/inject_soc -d '42.5'
```


Setzt den Ladestand des verbundenen Fahrzeugs auf 42,5 %.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp4/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_soc -m '{ "soc": 42.5 }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_soc -m '42.5'
```


Setzt den Ladestand des verbundenen Fahrzeugs auf 42,5 %.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`soc`<br/>float (<abbr title="Prozent">%</abbr>)</th>
    <td>
        Ladestand in Prozent. Muss zwischen 0 und 100 liegen.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/inject_ev` {#ev_inject_ev_warp4}


Täuscht vor, dass sich ein Fahrzeug mit der übergebenen MAC-Adresse verbunden hat. Hiermit kann über die API ein Fahrzeug identifiziert werden, das seine MAC-Adresse nicht selbst (per ISO 15118) übermittelt. Falls die MAC-Adresse einem Fahrzeug-Profil aus [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config) zugeordnet werden kann, werden dessen Einstellungen übernommen und (falls Autocharge aktiv ist) der Ladevorgang dem konfigurierten Benutzer zugeordnet.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp4-AbCd
```
#### Schreiben
```bash
curl http://$HOST/ev/inject_ev -d '{ "mac": "0A:1B:2C:D3:E4:F5" }'
```

oder abgekürzt:

```bash
curl http://$HOST/ev/inject_ev -d '"0A:1B:2C:D3:E4:F5"'
```


Täuscht vor, dass sich ein Fahrzeug mit der MAC-Adresse 0A:1B:2C:D3:E4:F5 verbunden hat.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp4/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_ev -m '{ "mac": "0A:1B:2C:D3:E4:F5" }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_ev -m '"0A:1B:2C:D3:E4:F5"'
```


Täuscht vor, dass sich ein Fahrzeug mit der MAC-Adresse 0A:1B:2C:D3:E4:F5 verbunden hat.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
    <td>
        MAC-Adresse des Fahrzeugs im Format `AA:BB:CC:DD:EE:FF`.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/config` {#ev_config_warp4}


Die konfigurierten Fahrzeug-Profile.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp4-AbCd
```

#### Lesen
```bash
curl http://$HOST/ev/config
```
```jsx
{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}
```
Konfiguriert zwei Fahrzeug-Profile: Ein Fahrzeug mit wechselnder MAC-Adresse
(nur die ersten drei Bytes werden zur Identifikation verwendet), dessen Ladevorgänge
dem Benutzer mit der ID 2 zugeordnet werden, und ein Fahrzeug mit fester MAC-Adresse
ohne Benutzerzuordnung.


#### Schreiben
```bash
curl http://$HOST/ev/config -d '{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}'
```

oder abgekürzt:

```bash
curl http://$HOST/ev/config -d '[
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]'
```


Konfiguriert zwei Fahrzeug-Profile: Ein Fahrzeug mit wechselnder MAC-Adresse
(nur die ersten drei Bytes werden zur Identifikation verwendet), dessen Ladevorgänge
dem Benutzer mit der ID 2 zugeordnet werden, und ein Fahrzeug mit fester MAC-Adresse
ohne Benutzerzuordnung.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp4/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/config
```
```jsx
{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}
```
Konfiguriert zwei Fahrzeug-Profile: Ein Fahrzeug mit wechselnder MAC-Adresse
(nur die ersten drei Bytes werden zur Identifikation verwendet), dessen Ladevorgänge
dem Benutzer mit der ID 2 zugeordnet werden, und ein Fahrzeug mit fester MAC-Adresse
ohne Benutzerzuordnung.


#### Schreiben
            Mit MQTT auf <code>$PREFIX/ev/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/config_update -m '{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/config_update -m '[
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]'
```


Konfiguriert zwei Fahrzeug-Profile: Ein Fahrzeug mit wechselnder MAC-Adresse
(nur die ersten drei Bytes werden zur Identifikation verwendet), dessen Ladevorgänge
dem Benutzer mit der ID 2 zugeordnet werden, und ein Fahrzeug mit fester MAC-Adresse
ohne Benutzerzuordnung.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`evs`<br/>object[..]</th>
    <td>
        Liste der Fahrzeug-Profile (maximal 16).
        - **[0]** - Ein Fahrzeug-Profil
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Beschreibung</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Name zur Identifikation dieses Fahrzeugs. Maximal 16 Zeichen.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
            <td>
                MAC-Adresse des Fahrzeugs im Format `AA:BB:CC:DD:EE:FF`. Falls das Fahrzeug einen Teil seiner Adresse zwischen Ladevorgängen ändert (z.B. VW-ID-Modelle), können die wechselnden Bytes mit `xx` als Platzhalter markiert werden, z.B. `AA:BB:CC:xx:xx:xx`.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`capacity`<br/>float (<abbr title="Kilowattstunde">kWh</abbr>)</th>
            <td>
                Batteriekapazität des Fahrzeugs in kWh. Wird zur Schätzung des Ladestands verwendet. 0, falls unbekannt (dann werden 60 kWh angenommen).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`charging_efficiency`<br/>float</th>
            <td>
                Ladeeffizienz des Fahrzeugs: Verhältnis von gespeicherter zu verbrauchter Energie. Wird zur Schätzung des Ladestands verwendet. Typischerweise 0,92.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID des Benutzers, dem dieses Fahrzeug zugeordnet ist, oder 0 falls es keinem Benutzer zugeordnet ist. Wird das Fahrzeug erkannt und ist Autocharge aktiv, wird der Ladevorgang diesem Benutzer zugeordnet.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="wem2">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="any">
## `ev/state` {#ev_state_any}


Der Zustand des aktuell verbundenen Fahrzeugs.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Lesen
```bash
curl http://$HOST/ev/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active_ev_index": 0,
    "name": "ID.4",
    "mac": "0A:1B:2C:D3:E4:F5",
    "soc": 42.5,
    "capacity": 77.0,
    "charging_efficiency": 0.92
}
```
Ein Fahrzeug ist verbunden und wurde dem ersten Fahrzeug-Profil zugeordnet.
Der geschätzte Ladestand beträgt 42,5 %.

:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active_ev_index`<br/>int</th>
    <td>
        Index des zugeordneten Fahrzeug-Profils in [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config). -1 falls kein Fahrzeug verbunden ist oder das Fahrzeug keinem Profil zugeordnet werden konnte.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Name des zugeordneten Fahrzeug-Profils. Leer, falls kein Fahrzeug verbunden ist oder das Fahrzeug keinem Profil zugeordnet werden konnte.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
    <td>
        MAC-Adresse des verbundenen Fahrzeugs im Format `AA:BB:CC:DD:EE:FF`. Leer, falls kein Fahrzeug verbunden ist.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`soc`<br/>float (<abbr title="Prozent">%</abbr>)</th>
    <td>
        Aktueller Ladestand (SoC) des Fahrzeugs in Prozent. Wird entweder vom Fahrzeug übermittelt (ISO 15118-20), über [`ev/inject_soc`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_inject_soc) gesetzt oder anhand der geladenen Energie, der Batteriekapazität und der Ladeeffizienz geschätzt. null, falls unbekannt.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`capacity`<br/>float (<abbr title="Kilowattstunde">kWh</abbr>)</th>
    <td>
        Batteriekapazität des verbundenen Fahrzeugs in kWh. Wird dem zugeordneten Fahrzeug-Profil entnommen oder vom Fahrzeug übermittelt (ISO 15118-20). null, falls kein Fahrzeug verbunden ist.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`charging_efficiency`<br/>float</th>
    <td>
        Ladeeffizienz des verbundenen Fahrzeugs: Verhältnis von gespeicherter zu verbrauchter Energie. Wird dem zugeordneten Fahrzeug-Profil entnommen. 0,92 falls kein Profil zugeordnet ist.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/seen_macs` {#ev_seen_macs_any}


Die MAC-Adressen der zuletzt verbundenen Fahrzeuge (maximal 8). Hilfreich, um die MAC-Adresse eines Fahrzeugs zu ermitteln, das in [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config) konfiguriert werden soll.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Lesen
```bash
curl http://$HOST/ev/seen_macs
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/seen_macs
```
        </TabItem>
    </Tabs>

```jsx
[
    { "mac": "0A:1B:2C:D3:E4:F5", "seen_at": 1234567 },
    { "mac": "5C:AF:06:12:34:56", "seen_at": 42000 }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[..]`<br/>object</th>
    <td>
        Eine gesehene MAC-Adresse
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Beschreibung</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
            <td>
                MAC-Adresse des Fahrzeugs im Format `AA:BB:CC:DD:EE:FF`.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`seen_at`<br/>int (<abbr title="Millisekunde">ms</abbr>)</th>
            <td>
                Zeitpunkt, zu dem die MAC-Adresse zuletzt gesehen wurde, in Millisekunden seit Systemstart.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/inject_soc` {#ev_inject_soc_any}


Setzt den Ladestand (SoC) des aktuell verbundenen Fahrzeugs über die API. Ausgehend von diesem Wert wird der Ladestand anhand der geladenen Energie, der Batteriekapazität und der Ladeeffizienz weiter geschätzt.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Schreiben
```bash
curl http://$HOST/ev/inject_soc -d '{ "soc": 42.5 }'
```

oder abgekürzt:

```bash
curl http://$HOST/ev/inject_soc -d '42.5'
```


Setzt den Ladestand des verbundenen Fahrzeugs auf 42,5 %.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_soc -m '{ "soc": 42.5 }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_soc -m '42.5'
```


Setzt den Ladestand des verbundenen Fahrzeugs auf 42,5 %.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`soc`<br/>float (<abbr title="Prozent">%</abbr>)</th>
    <td>
        Ladestand in Prozent. Muss zwischen 0 und 100 liegen.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/inject_ev` {#ev_inject_ev_any}


Täuscht vor, dass sich ein Fahrzeug mit der übergebenen MAC-Adresse verbunden hat. Hiermit kann über die API ein Fahrzeug identifiziert werden, das seine MAC-Adresse nicht selbst (per ISO 15118) übermittelt. Falls die MAC-Adresse einem Fahrzeug-Profil aus [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config) zugeordnet werden kann, werden dessen Einstellungen übernommen und (falls Autocharge aktiv ist) der Ladevorgang dem konfigurierten Benutzer zugeordnet.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Schreiben
```bash
curl http://$HOST/ev/inject_ev -d '{ "mac": "0A:1B:2C:D3:E4:F5" }'
```

oder abgekürzt:

```bash
curl http://$HOST/ev/inject_ev -d '"0A:1B:2C:D3:E4:F5"'
```


Täuscht vor, dass sich ein Fahrzeug mit der MAC-Adresse 0A:1B:2C:D3:E4:F5 verbunden hat.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_ev -m '{ "mac": "0A:1B:2C:D3:E4:F5" }'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_ev -m '"0A:1B:2C:D3:E4:F5"'
```


Täuscht vor, dass sich ein Fahrzeug mit der MAC-Adresse 0A:1B:2C:D3:E4:F5 verbunden hat.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
    <td>
        MAC-Adresse des Fahrzeugs im Format `AA:BB:CC:DD:EE:FF`.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/config` {#ev_config_any}


Die konfigurierten Fahrzeug-Profile.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```

#### Lesen
```bash
curl http://$HOST/ev/config
```
```jsx
{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}
```
Konfiguriert zwei Fahrzeug-Profile: Ein Fahrzeug mit wechselnder MAC-Adresse
(nur die ersten drei Bytes werden zur Identifikation verwendet), dessen Ladevorgänge
dem Benutzer mit der ID 2 zugeordnet werden, und ein Fahrzeug mit fester MAC-Adresse
ohne Benutzerzuordnung.


#### Schreiben
```bash
curl http://$HOST/ev/config -d '{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}'
```

oder abgekürzt:

```bash
curl http://$HOST/ev/config -d '[
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]'
```


Konfiguriert zwei Fahrzeug-Profile: Ein Fahrzeug mit wechselnder MAC-Adresse
(nur die ersten drei Bytes werden zur Identifikation verwendet), dessen Ladevorgänge
dem Benutzer mit der ID 2 zugeordnet werden, und ein Fahrzeug mit fester MAC-Adresse
ohne Benutzerzuordnung.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/config
```
```jsx
{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}
```
Konfiguriert zwei Fahrzeug-Profile: Ein Fahrzeug mit wechselnder MAC-Adresse
(nur die ersten drei Bytes werden zur Identifikation verwendet), dessen Ladevorgänge
dem Benutzer mit der ID 2 zugeordnet werden, und ein Fahrzeug mit fester MAC-Adresse
ohne Benutzerzuordnung.


#### Schreiben
            Mit MQTT auf <code>$PREFIX/ev/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/config_update -m '{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}'
```

oder abgekürzt:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/config_update -m '[
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]'
```


Konfiguriert zwei Fahrzeug-Profile: Ein Fahrzeug mit wechselnder MAC-Adresse
(nur die ersten drei Bytes werden zur Identifikation verwendet), dessen Ladevorgänge
dem Benutzer mit der ID 2 zugeordnet werden, und ein Fahrzeug mit fester MAC-Adresse
ohne Benutzerzuordnung.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`evs`<br/>object[..]</th>
    <td>
        Liste der Fahrzeug-Profile (maximal 16).
        
        
        
        - **[0]** - Ein Fahrzeug-Profil
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Beschreibung</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Name zur Identifikation dieses Fahrzeugs. Maximal 16 Zeichen.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
            <td>
                MAC-Adresse des Fahrzeugs im Format `AA:BB:CC:DD:EE:FF`. Falls das Fahrzeug einen Teil seiner Adresse zwischen Ladevorgängen ändert (z.B. VW-ID-Modelle), können die wechselnden Bytes mit `xx` als Platzhalter markiert werden, z.B. `AA:BB:CC:xx:xx:xx`.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`capacity`<br/>float (<abbr title="Kilowattstunde">kWh</abbr>)</th>
            <td>
                Batteriekapazität des Fahrzeugs in kWh. Wird zur Schätzung des Ladestands verwendet. 0, falls unbekannt (dann werden 60 kWh angenommen).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`charging_efficiency`<br/>float</th>
            <td>
                Ladeeffizienz des Fahrzeugs: Verhältnis von gespeicherter zu verbrauchter Energie. Wird zur Schätzung des Ladestands verwendet. Typischerweise 0,92.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID des Benutzers, dem dieses Fahrzeug zugeordnet ist, oder 0 falls es keinem Benutzer zugeordnet ist. Wird das Fahrzeug erkannt und ist Autocharge aktiv, wird der Ladevorgang diesem Benutzer zugeordnet.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "ev/state",
    "id": "ev_state",
    "level": 2,
    "hardwareType": ["warp4"]
},{
    "value": "ev/seen_macs",
    "id": "ev_seen_macs",
    "level": 2,
    "hardwareType": ["warp4"]
},{
    "value": "ev/inject_soc",
    "id": "ev_inject_soc",
    "level": 2,
    "hardwareType": ["warp4"]
},{
    "value": "ev/inject_ev",
    "id": "ev_inject_ev",
    "level": 2,
    "hardwareType": ["warp4"]
},{
    "value": "ev/config",
    "id": "ev_config",
    "level": 2,
    "hardwareType": ["warp4"]
},
];
