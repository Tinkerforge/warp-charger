---
description: §14a EnWG
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/p14a_enwg.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# §14a EnWG
Über das `p14a_enwg`-Modul können die Einstellungen für die Steuerung gemäß §14a des Energiewirtschaftsgesetzes (EnWG) konfiguriert werden. §14a EnWG ermöglicht es Netzbetreibern, den Strombezug steuerbarer Verbrauchseinrichtungen (Wallboxen, Wärmepumpen, Batteriespeicher, Klimaanlagen) temporär zu reduzieren, um eine lokale Netzüberlastung zu vermeiden. Die Geräte werden dabei nie vollständig abgeschaltet. Eine Mindestleistung von 4200 W bleibt stets verfügbar.
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="warp2">
## `p14a_enwg/config` {#p14a_enwg_config_warp2}


Die Konfiguration der §14a-EnWG-Steuerung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp2-AbCd
```

#### Lesen
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}
```



#### Schreiben
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp2/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-EnWG-Steuerung aktiviert werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signalquelle für die §14a-EnWG-Steuerung.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Eingang">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Beschreibung</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Gibt an, ob die §14a-Steuerung bei geschlossenem oder geöffnetem Eingang aktiv ist.
                - **true** - Aktiv wenn geschlossen
                - **false** - Aktiv wenn geöffnet
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Leistungslimit in Watt, das bei Auslösung über den Eingang angewendet wird.
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charger`<br/>bool</th>
    <td>
        <strong>(Nur WARP 2 und WARP 3)</strong> Gibt an, ob das §14a-Leistungslimit auf diese Wallbox angewendet werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Gibt an, ob das §14a-Leistungslimit auf verwaltete Wallboxen angewendet werden soll.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_warp2}


Der Zustand der §14a-EnWG-Steuerung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp2-AbCd
```
#### Lesen
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp2/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-Steuerung aktuell aktiv ist, d.h. ob eine Leistungsbegrenzung vorliegt.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Das aktuell wirksame Leistungslimit. Ist 0 wenn die §14a-Steuerung nicht aktiv ist.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_warp2}


Der Steuerungszustand für die API-Quelle. Wird beim Neustart auf inaktiv zurückgesetzt. Die Werte werden nur berücksichtigt, wenn die Signalquelle auf API (2) gesetzt ist.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp2-AbCd
```
#### Lesen
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp2/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-Steuerung über die API aktiv ist.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Leistungslimit in Watt, das über die API gesetzt wird.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_warp2}


Setzt den §14a-Steuerungszustand über die API. Funktioniert nur, wenn die Signalquelle auf API (2) gesetzt ist. Der Zustand ist volatil und wird beim Neustart auf inaktiv zurückgesetzt.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp2-AbCd
```
#### Schreiben
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp2/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Aktiviert oder deaktiviert die §14a-Steuerung über die API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Leistungslimit in Watt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `p14a_enwg/config` {#p14a_enwg_config_warp3}


Die Konfiguration der §14a-EnWG-Steuerung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp3-AbCd
```

#### Lesen
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}
```



#### Schreiben
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp3/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-EnWG-Steuerung aktiviert werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signalquelle für die §14a-EnWG-Steuerung.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Eingang">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Beschreibung</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Gibt an, ob die §14a-Steuerung bei geschlossenem oder geöffnetem Eingang aktiv ist.
                - **true** - Aktiv wenn geschlossen
                - **false** - Aktiv wenn geöffnet
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Leistungslimit in Watt, das bei Auslösung über den Eingang angewendet wird.
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charger`<br/>bool</th>
    <td>
        <strong>(Nur WARP 2 und WARP 3)</strong> Gibt an, ob das §14a-Leistungslimit auf diese Wallbox angewendet werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Gibt an, ob das §14a-Leistungslimit auf verwaltete Wallboxen angewendet werden soll.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_warp3}


Der Zustand der §14a-EnWG-Steuerung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp3-AbCd
```
#### Lesen
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp3/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-Steuerung aktuell aktiv ist, d.h. ob eine Leistungsbegrenzung vorliegt.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Das aktuell wirksame Leistungslimit. Ist 0 wenn die §14a-Steuerung nicht aktiv ist.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_warp3}


Der Steuerungszustand für die API-Quelle. Wird beim Neustart auf inaktiv zurückgesetzt. Die Werte werden nur berücksichtigt, wenn die Signalquelle auf API (2) gesetzt ist.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp3-AbCd
```
#### Lesen
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp3/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-Steuerung über die API aktiv ist.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Leistungslimit in Watt, das über die API gesetzt wird.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_warp3}


Setzt den §14a-Steuerungszustand über die API. Funktioniert nur, wenn die Signalquelle auf API (2) gesetzt ist. Der Zustand ist volatil und wird beim Neustart auf inaktiv zurückgesetzt.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp3-AbCd
```
#### Schreiben
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp3/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Aktiviert oder deaktiviert die §14a-Steuerung über die API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Leistungslimit in Watt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
Auf dieser Hardware nicht unterstützt!
</TabItem>
<TabItem value="wem2">
## `p14a_enwg/config` {#p14a_enwg_config_wem2}


Die Konfiguration der §14a-EnWG-Steuerung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem2-AbCd
```

#### Lesen
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}
```



#### Schreiben
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem2/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-EnWG-Steuerung aktiviert werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signalquelle für die §14a-EnWG-Steuerung.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Eingang">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Beschreibung</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Gibt an, ob die §14a-Steuerung bei geschlossenem oder geöffnetem Eingang aktiv ist.
                - **true** - Aktiv wenn geschlossen
                - **false** - Aktiv wenn geöffnet
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Leistungslimit in Watt, das bei Auslösung über den Eingang angewendet wird.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`input_index`<br/>int</th>
            <td>
                <strong>(Nur Energy Manager 2.0)</strong> Gibt an, welcher der vier Eingänge des Energy Managers für die §14a-Steuerung verwendet wird.
                - **0** - Eingang 1
                - **1** - Eingang 2
                - **2** - Eingang 3
                - **3** - Eingang 4
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Gibt an, ob das §14a-Leistungslimit auf verwaltete Wallboxen angewendet werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_heating`<br/>bool</th>
    <td>
        <strong>(Nur Energy Manager 2.0)</strong> Gibt an, ob das §14a-Leistungslimit auf die Heizungssteuerung angewendet werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`heating_max_power`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        <strong>(Nur Energy Manager 2.0)</strong> Maximale Leistungsaufnahme der Heizungsanlage.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_wem2}


Der Zustand der §14a-EnWG-Steuerung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem2-AbCd
```
#### Lesen
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem2/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-Steuerung aktuell aktiv ist, d.h. ob eine Leistungsbegrenzung vorliegt.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Das aktuell wirksame Leistungslimit. Ist 0 wenn die §14a-Steuerung nicht aktiv ist.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_wem2}


Der Steuerungszustand für die API-Quelle. Wird beim Neustart auf inaktiv zurückgesetzt. Die Werte werden nur berücksichtigt, wenn die Signalquelle auf API (2) gesetzt ist.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem2-AbCd
```
#### Lesen
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem2/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-Steuerung über die API aktiv ist.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Leistungslimit in Watt, das über die API gesetzt wird.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_wem2}


Setzt den §14a-Steuerungszustand über die API. Funktioniert nur, wenn die Signalquelle auf API (2) gesetzt ist. Der Zustand ist volatil und wird beim Neustart auf inaktiv zurückgesetzt.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem2-AbCd
```
#### Schreiben
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem2/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Aktiviert oder deaktiviert die §14a-Steuerung über die API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Leistungslimit in Watt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `p14a_enwg/config` {#p14a_enwg_config_any}


Die Konfiguration der §14a-EnWG-Steuerung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```

#### Lesen
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charger": true,
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}
```



#### Schreiben
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charger": true,
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```

#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charger": true,
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}
```



#### Schreiben
            Mit MQTT auf <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charger": true,
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-EnWG-Steuerung aktiviert werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signalquelle für die §14a-EnWG-Steuerung.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Eingang">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Beschreibung</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Gibt an, ob die §14a-Steuerung bei geschlossenem oder geöffnetem Eingang aktiv ist.
                
                
                
                - **true** - Aktiv wenn geschlossen
                - **false** - Aktiv wenn geöffnet
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Leistungslimit in Watt, das bei Auslösung über den Eingang angewendet wird.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`input_index`<br/>int</th>
            <td>
                <strong>(Nur Energy Manager 2.0)</strong> Gibt an, welcher der vier Eingänge des Energy Managers für die §14a-Steuerung verwendet wird.
                
                
                
                - **0** - Eingang 1
                - **1** - Eingang 2
                - **2** - Eingang 3
                - **3** - Eingang 4
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charger`<br/>bool</th>
    <td>
        <strong>(Nur WARP 2 und WARP 3)</strong> Gibt an, ob das §14a-Leistungslimit auf diese Wallbox angewendet werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Gibt an, ob das §14a-Leistungslimit auf verwaltete Wallboxen angewendet werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_heating`<br/>bool</th>
    <td>
        <strong>(Nur Energy Manager 2.0)</strong> Gibt an, ob das §14a-Leistungslimit auf die Heizungssteuerung angewendet werden soll.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`heating_max_power`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        <strong>(Nur Energy Manager 2.0)</strong> Maximale Leistungsaufnahme der Heizungsanlage.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_any}


Der Zustand der §14a-EnWG-Steuerung.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Lesen
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-Steuerung aktuell aktiv ist, d.h. ob eine Leistungsbegrenzung vorliegt.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Das aktuell wirksame Leistungslimit. Ist 0 wenn die §14a-Steuerung nicht aktiv ist.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_any}


Der Steuerungszustand für die API-Quelle. Wird beim Neustart auf inaktiv zurückgesetzt. Die Werte werden nur berücksichtigt, wenn die Signalquelle auf API (2) gesetzt ist.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Lesen
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Lesen
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Gibt an, ob die §14a-Steuerung über die API aktiv ist.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Leistungslimit in Watt, das über die API gesetzt wird.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_any}


Setzt den §14a-Steuerungszustand über die API. Funktioniert nur, wenn die Signalquelle auf API (2) gesetzt ist. Der Zustand ist volatil und wird beim Neustart auf inaktiv zurückgesetzt.
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Schreiben
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Aktiviert oder deaktiviert die §14a-Steuerung über die API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Leistungslimit in Watt.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "p14a_enwg/config",
    "id": "p14a_enwg_config",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem2"]
},{
    "value": "p14a_enwg/state",
    "id": "p14a_enwg_state",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem2"]
},{
    "value": "p14a_enwg/control",
    "id": "p14a_enwg_control",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem2"]
},{
    "value": "p14a_enwg/control_update",
    "id": "p14a_enwg_control_update",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem2"]
},
];
