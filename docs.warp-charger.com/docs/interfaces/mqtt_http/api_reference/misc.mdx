---
description: Sonstiges
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/misc.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Sonstiges

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `reboot` {#reboot_warp1}


Startet den ESP neu, um beispielsweise Konfigurationsänderungen anzuwenden.
**Leerer Payload. Es muss einer der folgenden Werte übergeben werden: `null`, `""`, `false`, `0`, `[]` oder `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Schreiben
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_warp1}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generiert einen Debug-Report. Dieser besteht aus allen Zuständen und Konfigurationen, sowie den letzten empfangenen Kommandos und Konfigurationsupdates. Passwörter werden, genau wie bei Konfigurationsabfragen, zensiert.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_warp1}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Erzwingt einen sofortigen Neustart des ESPs. Nützlich, falls [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) aus irgendwelchen Gründen hängt.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_warp1}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Nimmt ein Firmware-Update als POST entgegen, dass dann geflasht wird.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_warp1}


Setzt alle Einstellungen zurück aber behält aufgezeichnete Ladevorgänge und die Nutzernamenhistorie. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_warp1}


Setzt alle Einstellungen zurück und löscht alle aufgezeichneten Ladevorgänge. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen, aufgezeichneten Ladevorgänge und die Nutzernamenhistorie löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `reboot` {#reboot_warp2}


Startet den ESP neu, um beispielsweise Konfigurationsänderungen anzuwenden.
**Leerer Payload. Es muss einer der folgenden Werte übergeben werden: `null`, `""`, `false`, `0`, `[]` oder `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp2-AbCd
```
#### Schreiben
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp2/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_warp2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generiert einen Debug-Report. Dieser besteht aus allen Zuständen und Konfigurationen, sowie den letzten empfangenen Kommandos und Konfigurationsupdates. Passwörter werden, genau wie bei Konfigurationsabfragen, zensiert.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_warp2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Erzwingt einen sofortigen Neustart des ESPs. Nützlich, falls [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) aus irgendwelchen Gründen hängt.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_warp2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Nimmt ein Firmware-Update als POST entgegen, dass dann geflasht wird.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_warp2}


Setzt alle Einstellungen zurück aber behält aufgezeichnete Ladevorgänge und die Nutzernamenhistorie. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_warp2}


Setzt alle Einstellungen zurück und löscht alle aufgezeichneten Ladevorgänge. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen, aufgezeichneten Ladevorgänge und die Nutzernamenhistorie löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `reboot` {#reboot_warp3}


Startet den ESP neu, um beispielsweise Konfigurationsänderungen anzuwenden.
**Leerer Payload. Es muss einer der folgenden Werte übergeben werden: `null`, `""`, `false`, `0`, `[]` oder `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp3-AbCd
```
#### Schreiben
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp3/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_warp3}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generiert einen Debug-Report. Dieser besteht aus allen Zuständen und Konfigurationen, sowie den letzten empfangenen Kommandos und Konfigurationsupdates. Passwörter werden, genau wie bei Konfigurationsabfragen, zensiert.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_warp3}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Erzwingt einen sofortigen Neustart des ESPs. Nützlich, falls [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) aus irgendwelchen Gründen hängt.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_warp3}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Nimmt ein Firmware-Update als POST entgegen, dass dann geflasht wird.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_warp3}


Setzt alle Einstellungen zurück aber behält aufgezeichnete Ladevorgänge und die Nutzernamenhistorie. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_warp3}


Setzt alle Einstellungen zurück und löscht alle aufgezeichneten Ladevorgänge. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen, aufgezeichneten Ladevorgänge und die Nutzernamenhistorie löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `reboot` {#reboot_wem}


Startet den ESP neu, um beispielsweise Konfigurationsänderungen anzuwenden.
**Leerer Payload. Es muss einer der folgenden Werte übergeben werden: `null`, `""`, `false`, `0`, `[]` oder `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem-AbCd
```
#### Schreiben
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_wem}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generiert einen Debug-Report. Dieser besteht aus allen Zuständen und Konfigurationen, sowie den letzten empfangenen Kommandos und Konfigurationsupdates. Passwörter werden, genau wie bei Konfigurationsabfragen, zensiert.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_wem}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Erzwingt einen sofortigen Neustart des ESPs. Nützlich, falls [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) aus irgendwelchen Gründen hängt.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_wem}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Nimmt ein Firmware-Update als POST entgegen, dass dann geflasht wird.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_wem}


Setzt alle Einstellungen zurück aber behält aufgezeichnete Ladevorgänge und die Nutzernamenhistorie. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_wem}


Setzt alle Einstellungen zurück und löscht alle aufgezeichneten Ladevorgänge. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen, aufgezeichneten Ladevorgänge und die Nutzernamenhistorie löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `reboot` {#reboot_wem2}


Startet den ESP neu, um beispielsweise Konfigurationsänderungen anzuwenden.
**Leerer Payload. Es muss einer der folgenden Werte übergeben werden: `null`, `""`, `false`, `0`, `[]` oder `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. wem2-AbCd
```
#### Schreiben
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. wem2/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_wem2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generiert einen Debug-Report. Dieser besteht aus allen Zuständen und Konfigurationen, sowie den letzten empfangenen Kommandos und Konfigurationsupdates. Passwörter werden, genau wie bei Konfigurationsabfragen, zensiert.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_wem2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Erzwingt einen sofortigen Neustart des ESPs. Nützlich, falls [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) aus irgendwelchen Gründen hängt.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_wem2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Nimmt ein Firmware-Update als POST entgegen, dass dann geflasht wird.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_wem2}


Setzt alle Einstellungen zurück aber behält aufgezeichnete Ladevorgänge und die Nutzernamenhistorie. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_wem2}


Setzt alle Einstellungen zurück und löscht alle aufgezeichneten Ladevorgänge. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        - **true** - Alle Einstellungen, aufgezeichneten Ladevorgänge und die Nutzernamenhistorie löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `reboot` {#reboot_any}


Startet den ESP neu, um beispielsweise Konfigurationsänderungen anzuwenden.
**Leerer Payload. Es muss einer der folgenden Werte übergeben werden: `null`, `""`, `false`, `0`, `[]` oder `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Löst eine einmalige Aktion aus. Nachrichten, die über den Broker retained wurden, werden ignoriert."></Admonition></TabItem></Tabs>
:::info[Beispiel]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST z.B. warp-AbCd
```
#### Schreiben
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER z.B. my_mosquitto.localdomain
# $PREFIX z.B. warp/AbCd
```
#### Schreiben
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_any}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generiert einen Debug-Report. Dieser besteht aus allen Zuständen und Konfigurationen, sowie den letzten empfangenen Kommandos und Konfigurationsupdates. Passwörter werden, genau wie bei Konfigurationsabfragen, zensiert.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_any}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Erzwingt einen sofortigen Neustart des ESPs. Nützlich, falls [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) aus irgendwelchen Gründen hängt.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_any}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Nimmt ein Firmware-Update als POST entgegen, dass dann geflasht wird.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Wird nur von der HTTP-API unterstützt"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_any}


Setzt alle Einstellungen zurück aber behält aufgezeichnete Ladevorgänge und die Nutzernamenhistorie. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        
        
        
        - **true** - Alle Einstellungen löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_any}


Setzt alle Einstellungen zurück und löscht alle aufgezeichneten Ladevorgänge. <strong>Kann nicht rückgängig gemacht werden!</strong> Danach wird automatisch ein Neustart ausgeführt.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Beschreibung</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Gibt an ob der Löschvorgang ausgeführt werden soll
        
        
        
        - **true** - Alle Einstellungen, aufgezeichneten Ladevorgänge und die Nutzernamenhistorie löschen
        - **false** - Keine Aktion durchführen
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "reboot",
    "id": "reboot",
    "level": 2,
    
},{
    "value": "debug_report",
    "id": "debug_report",
    "level": 2,
    
},{
    "value": "force_reboot",
    "id": "force_reboot",
    "level": 2,
    
},{
    "value": "flash_firmware",
    "id": "flash_firmware",
    "level": 2,
    
},{
    "value": "config_reset",
    "id": "config_reset",
    "level": 2,
    
},{
    "value": "factory_reset",
    "id": "factory_reset",
    "level": 2,
    
},
];
