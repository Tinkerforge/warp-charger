---
description: Configuration of the Emulated Legacy Electricity Meter API
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/meters_legacy_api.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Configuration of the Emulated Legacy Electricity Meter API

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `meters_legacy_api/state` {#meters_legacy_api_state_warp1}


The state of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/meters_legacy_api/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/state
```
        </TabItem>
    </Tabs>

```jsx
{"writable":false}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`writable`<br/>bool</th>
    <td>
        Indicates whether the legacy electricity meter API can be written to. This is only the case if the meter entered under [`meters_legacy_api/config`](/interfaces/mqtt_http/api_reference/meters_legacy_api.mdx#meters_legacy_api_config) as `linked_meter_slot` is a meter configured as an API meter in its [`meters/X/config`](/interfaces/mqtt_http/api_reference/meters.mdx#meters_X_config).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `meters_legacy_api/config` {#meters_legacy_api_config_warp1}


The configuration of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
```bash
curl http://$HOST/meters_legacy_api/config -d '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
curl http://$HOST/meters_legacy_api/config -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
            With MQTT on <code>$PREFIX/meters_legacy_api/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`linked_meter_slot`<br/>int</th>
    <td>
        Meter to which the emulated legacy electricity meter API should be mapped
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `meters_legacy_api/state` {#meters_legacy_api_state_warp2}


The state of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/meters_legacy_api/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/state
```
        </TabItem>
    </Tabs>

```jsx
{"writable":false}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`writable`<br/>bool</th>
    <td>
        Indicates whether the legacy electricity meter API can be written to. This is only the case if the meter entered under [`meters_legacy_api/config`](/interfaces/mqtt_http/api_reference/meters_legacy_api.mdx#meters_legacy_api_config) as `linked_meter_slot` is a meter configured as an API meter in its [`meters/X/config`](/interfaces/mqtt_http/api_reference/meters.mdx#meters_X_config).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `meters_legacy_api/config` {#meters_legacy_api_config_warp2}


The configuration of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
```bash
curl http://$HOST/meters_legacy_api/config -d '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
curl http://$HOST/meters_legacy_api/config -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
            With MQTT on <code>$PREFIX/meters_legacy_api/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`linked_meter_slot`<br/>int</th>
    <td>
        Meter to which the emulated legacy electricity meter API should be mapped
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `meters_legacy_api/state` {#meters_legacy_api_state_warp3}


The state of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/meters_legacy_api/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/state
```
        </TabItem>
    </Tabs>

```jsx
{"writable":false}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`writable`<br/>bool</th>
    <td>
        Indicates whether the legacy electricity meter API can be written to. This is only the case if the meter entered under [`meters_legacy_api/config`](/interfaces/mqtt_http/api_reference/meters_legacy_api.mdx#meters_legacy_api_config) as `linked_meter_slot` is a meter configured as an API meter in its [`meters/X/config`](/interfaces/mqtt_http/api_reference/meters.mdx#meters_X_config).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `meters_legacy_api/config` {#meters_legacy_api_config_warp3}


The configuration of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
```bash
curl http://$HOST/meters_legacy_api/config -d '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
curl http://$HOST/meters_legacy_api/config -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
            With MQTT on <code>$PREFIX/meters_legacy_api/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`linked_meter_slot`<br/>int</th>
    <td>
        Meter to which the emulated legacy electricity meter API should be mapped
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `meters_legacy_api/state` {#meters_legacy_api_state_wem}


The state of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/meters_legacy_api/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/state
```
        </TabItem>
    </Tabs>

```jsx
{"writable":false}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`writable`<br/>bool</th>
    <td>
        Indicates whether the legacy electricity meter API can be written to. This is only the case if the meter entered under [`meters_legacy_api/config`](/interfaces/mqtt_http/api_reference/meters_legacy_api.mdx#meters_legacy_api_config) as `linked_meter_slot` is a meter configured as an API meter in its [`meters/X/config`](/interfaces/mqtt_http/api_reference/meters.mdx#meters_X_config).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `meters_legacy_api/config` {#meters_legacy_api_config_wem}


The configuration of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```

#### Read
```bash
curl http://$HOST/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
```bash
curl http://$HOST/meters_legacy_api/config -d '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
curl http://$HOST/meters_legacy_api/config -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
            With MQTT on <code>$PREFIX/meters_legacy_api/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`linked_meter_slot`<br/>int</th>
    <td>
        Meter to which the emulated legacy electricity meter API should be mapped
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `meters_legacy_api/state` {#meters_legacy_api_state_wem2}


The state of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Read
```bash
curl http://$HOST/meters_legacy_api/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/state
```
        </TabItem>
    </Tabs>

```jsx
{"writable":false}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`writable`<br/>bool</th>
    <td>
        Indicates whether the legacy electricity meter API can be written to. This is only the case if the meter entered under [`meters_legacy_api/config`](/interfaces/mqtt_http/api_reference/meters_legacy_api.mdx#meters_legacy_api_config) as `linked_meter_slot` is a meter configured as an API meter in its [`meters/X/config`](/interfaces/mqtt_http/api_reference/meters.mdx#meters_X_config).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `meters_legacy_api/config` {#meters_legacy_api_config_wem2}


The configuration of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```

#### Read
```bash
curl http://$HOST/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
```bash
curl http://$HOST/meters_legacy_api/config -d '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
curl http://$HOST/meters_legacy_api/config -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
            With MQTT on <code>$PREFIX/meters_legacy_api/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`linked_meter_slot`<br/>int</th>
    <td>
        Meter to which the emulated legacy electricity meter API should be mapped
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `meters_legacy_api/state` {#meters_legacy_api_state_any}


The state of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/meters_legacy_api/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/state
```
        </TabItem>
    </Tabs>

```jsx
{"writable":false}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`writable`<br/>bool</th>
    <td>
        Indicates whether the legacy electricity meter API can be written to. This is only the case if the meter entered under [`meters_legacy_api/config`](/interfaces/mqtt_http/api_reference/meters_legacy_api.mdx#meters_legacy_api_config) as `linked_meter_slot` is a meter configured as an API meter in its [`meters/X/config`](/interfaces/mqtt_http/api_reference/meters.mdx#meters_X_config).
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `meters_legacy_api/config` {#meters_legacy_api_config_any}


The configuration of the emulated legacy electricity meter API
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
```bash
curl http://$HOST/meters_legacy_api/config -d '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
curl http://$HOST/meters_legacy_api/config -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/meters_legacy_api/config
```
```jsx
{"linked_meter_slot":0}
```



#### Write
            With MQTT on <code>$PREFIX/meters_legacy_api/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '{"linked_meter_slot":0}'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/meters_legacy_api/config_update -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`linked_meter_slot`<br/>int</th>
    <td>
        Meter to which the emulated legacy electricity meter API should be mapped
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "meters_legacy_api/state",
    "id": "meters_legacy_api_state",
    "level": 2,
    
},{
    "value": "meters_legacy_api/config",
    "id": "meters_legacy_api_config",
    "level": 2,
    
},
];
