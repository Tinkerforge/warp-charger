---
description: LAN Connection
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/ethernet.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# LAN Connection
**Requires the feature <a href="#features_ethernet"><code>"ethernet"</code></a>.**
The `ethernet` module and appropriate hardware allow connection to a LAN.
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
Not supported on this hardware!
</TabItem>
<TabItem value="warp2">
## `ethernet/state` {#ethernet_state_warp2}


The state of the LAN/Ethernet connection.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/ethernet/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "connection_state": 3,
  "connection_start": 4241,
  "connection_end": 0,
  "ip": "192.168.1.129",
  "subnet": "255.255.255.0",
  "full_duplex": true,
  "link_speed": 100
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_state`<br/>int</th>
    <td>
        Connection state.
        - **0** - Not configured
        - **1** - Not connected
        - **2** - Connecting
        - **3** - Connected
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP of the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask used by the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`full_duplex`<br/>bool</th>
    <td>
        true for a full-duplex connection, otherwise false
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`link_speed`<br/>int (<abbr title="Megabit per second">Mbit/s</abbr>)</th>
    <td>
        Negotiated connection speed.
        - **0** - not connected
        - **10** - 
        - **100** - 
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_start`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last established.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_end`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last disconnected.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ethernet/config` {#ethernet_config_warp2}


The LAN/Ethernet connection configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
```bash
curl http://$HOST/ethernet/config -d '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
            With MQTT on <code>$PREFIX/ethernet/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ethernet/config_update -m '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_ethernet`<br/>bool</th>
    <td>
        Indicates whether a LAN connection should be established.
        - **true** - If a LAN connection should be established.
        - **false** - If not.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP address the device should use. This entry and the following are optional and can be set to "0.0.0.0" if the address configuration should be obtained automatically via DHCP.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`gateway`<br/>string</th>
    <td>
        Gateway address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns`<br/>string</th>
    <td>
        DNS server address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns2`<br/>string</th>
    <td>
        Alternative DNS server address the device should use.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `ethernet/state` {#ethernet_state_warp3}


The state of the LAN/Ethernet connection.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/ethernet/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "connection_state": 3,
  "connection_start": 4241,
  "connection_end": 0,
  "ip": "192.168.1.129",
  "subnet": "255.255.255.0",
  "full_duplex": true,
  "link_speed": 100
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_state`<br/>int</th>
    <td>
        Connection state.
        - **0** - Not configured
        - **1** - Not connected
        - **2** - Connecting
        - **3** - Connected
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP of the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask used by the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`full_duplex`<br/>bool</th>
    <td>
        true for a full-duplex connection, otherwise false
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`link_speed`<br/>int (<abbr title="Megabit per second">Mbit/s</abbr>)</th>
    <td>
        Negotiated connection speed.
        - **0** - not connected
        - **10** - 
        - **100** - 
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_start`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last established.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_end`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last disconnected.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ethernet/config` {#ethernet_config_warp3}


The LAN/Ethernet connection configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
```bash
curl http://$HOST/ethernet/config -d '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
            With MQTT on <code>$PREFIX/ethernet/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ethernet/config_update -m '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_ethernet`<br/>bool</th>
    <td>
        Indicates whether a LAN connection should be established.
        - **true** - If a LAN connection should be established.
        - **false** - If not.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP address the device should use. This entry and the following are optional and can be set to "0.0.0.0" if the address configuration should be obtained automatically via DHCP.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`gateway`<br/>string</th>
    <td>
        Gateway address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns`<br/>string</th>
    <td>
        DNS server address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns2`<br/>string</th>
    <td>
        Alternative DNS server address the device should use.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `ethernet/state` {#ethernet_state_wem}


The state of the LAN/Ethernet connection.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/ethernet/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "connection_state": 3,
  "connection_start": 4241,
  "connection_end": 0,
  "ip": "192.168.1.129",
  "subnet": "255.255.255.0",
  "full_duplex": true,
  "link_speed": 100
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_state`<br/>int</th>
    <td>
        Connection state.
        - **0** - Not configured
        - **1** - Not connected
        - **2** - Connecting
        - **3** - Connected
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP of the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask used by the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`full_duplex`<br/>bool</th>
    <td>
        true for a full-duplex connection, otherwise false
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`link_speed`<br/>int (<abbr title="Megabit per second">Mbit/s</abbr>)</th>
    <td>
        Negotiated connection speed.
        - **0** - not connected
        - **10** - 
        - **100** - 
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_start`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last established.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_end`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last disconnected.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ethernet/config` {#ethernet_config_wem}


The LAN/Ethernet connection configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```

#### Read
```bash
curl http://$HOST/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
```bash
curl http://$HOST/ethernet/config -d '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
            With MQTT on <code>$PREFIX/ethernet/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ethernet/config_update -m '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_ethernet`<br/>bool</th>
    <td>
        Indicates whether a LAN connection should be established.
        - **true** - If a LAN connection should be established.
        - **false** - If not.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP address the device should use. This entry and the following are optional and can be set to "0.0.0.0" if the address configuration should be obtained automatically via DHCP.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`gateway`<br/>string</th>
    <td>
        Gateway address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns`<br/>string</th>
    <td>
        DNS server address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns2`<br/>string</th>
    <td>
        Alternative DNS server address the device should use.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `ethernet/state` {#ethernet_state_wem2}


The state of the LAN/Ethernet connection.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Read
```bash
curl http://$HOST/ethernet/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "connection_state": 3,
  "connection_start": 4241,
  "connection_end": 0,
  "ip": "192.168.1.129",
  "subnet": "255.255.255.0",
  "full_duplex": true,
  "link_speed": 100
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_state`<br/>int</th>
    <td>
        Connection state.
        - **0** - Not configured
        - **1** - Not connected
        - **2** - Connecting
        - **3** - Connected
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP of the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask used by the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`full_duplex`<br/>bool</th>
    <td>
        true for a full-duplex connection, otherwise false
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`link_speed`<br/>int (<abbr title="Megabit per second">Mbit/s</abbr>)</th>
    <td>
        Negotiated connection speed.
        - **0** - not connected
        - **10** - 
        - **100** - 
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_start`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last established.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_end`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last disconnected.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ethernet/config` {#ethernet_config_wem2}


The LAN/Ethernet connection configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```

#### Read
```bash
curl http://$HOST/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
```bash
curl http://$HOST/ethernet/config -d '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
            With MQTT on <code>$PREFIX/ethernet/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ethernet/config_update -m '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_ethernet`<br/>bool</th>
    <td>
        Indicates whether a LAN connection should be established.
        - **true** - If a LAN connection should be established.
        - **false** - If not.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP address the device should use. This entry and the following are optional and can be set to "0.0.0.0" if the address configuration should be obtained automatically via DHCP.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`gateway`<br/>string</th>
    <td>
        Gateway address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns`<br/>string</th>
    <td>
        DNS server address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns2`<br/>string</th>
    <td>
        Alternative DNS server address the device should use.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `ethernet/state` {#ethernet_state_any}


The state of the LAN/Ethernet connection.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/ethernet/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "connection_state": 3,
  "connection_start": 4241,
  "connection_end": 0,
  "ip": "192.168.1.129",
  "subnet": "255.255.255.0",
  "full_duplex": true,
  "link_speed": 100
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_state`<br/>int</th>
    <td>
        Connection state.
        
        
        
        - **0** - Not configured
        - **1** - Not connected
        - **2** - Connecting
        - **3** - Connected
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP of the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask used by the device in the connected network. 0.0.0.0 if no connection exists.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`full_duplex`<br/>bool</th>
    <td>
        true for a full-duplex connection, otherwise false
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`link_speed`<br/>int (<abbr title="Megabit per second">Mbit/s</abbr>)</th>
    <td>
        Negotiated connection speed.
        
        
        
        - **0** - not connected
        - **10** - 
        - **100** - 
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_start`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last established.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connection_end`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Time in milliseconds when a connection was last disconnected.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ethernet/config` {#ethernet_config_any}


The LAN/Ethernet connection configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
```bash
curl http://$HOST/ethernet/config -d '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ethernet/config
```
```jsx
{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}
```



#### Write
            With MQTT on <code>$PREFIX/ethernet/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ethernet/config_update -m '{
  "enable_ethernet": true,
  "ip": "0.0.0.0",
  "gateway": "0.0.0.0",
  "subnet": "0.0.0.0",
  "dns": "0.0.0.0",
  "dns2": "0.0.0.0"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_ethernet`<br/>bool</th>
    <td>
        Indicates whether a LAN connection should be established.
        
        
        
        - **true** - If a LAN connection should be established.
        - **false** - If not.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`ip`<br/>string</th>
    <td>
        IP address the device should use. This entry and the following are optional and can be set to "0.0.0.0" if the address configuration should be obtained automatically via DHCP.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`gateway`<br/>string</th>
    <td>
        Gateway address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`subnet`<br/>string</th>
    <td>
        Subnet mask the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns`<br/>string</th>
    <td>
        DNS server address the device should use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`dns2`<br/>string</th>
    <td>
        Alternative DNS server address the device should use.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "ethernet/state",
    "id": "ethernet_state",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},{
    "value": "ethernet/config",
    "id": "ethernet_config",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},
];
