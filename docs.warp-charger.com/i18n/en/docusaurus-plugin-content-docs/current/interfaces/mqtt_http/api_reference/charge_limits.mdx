---
description: Charge Time and Energy Limits
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/charge_limits.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Charge Time and Energy Limits
The `charge_limits` module allows defining energy and time limits after which a charging session is terminated. Both default limits can be set ([`charge_limits/default_limits`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_default_limits)), as well as overriding limits for the current or next charging session ([`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy))
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `charge_limits/state` {#charge_limits_state_warp1}


Current state of time and energy limits
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/charge_limits/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "start_timestamp_ms": 786598059,
  "start_energy_kwh": 886.1900024,
  "target_timestamp_ms": 797398059,
  "target_energy_kwh": 901.1900024
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`start_timestamp_ms`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Start timestamp of the current charging session. 0 if no charging session is running.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`target_timestamp_ms`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Target timestamp of the current charging session. 0 if no charging session is running. Equal to the start timestamp if no time limit is set.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`start_energy_kwh`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Start meter value of the current charging session. null if no charging session is running or no electricity meter is available.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`target_energy_kwh`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Target meter value of the current charging session. null if no charging session is running or no electricity meter is available. Equal to the start meter value if no energy value is set.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/default_limits` {#charge_limits_default_limits_warp1}


Configuration of time and energy limits for charging sessions.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/charge_limits/default_limits
```
```jsx
{ "duration": 8, "energy_wh": 50000 }
```



#### Write
```bash
curl http://$HOST/charge_limits/default_limits -d '{ "duration": 8, "energy_wh": 50000 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/default_limits
```
```jsx
{ "duration": 8, "energy_wh": 50000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_limits/default_limits<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/default_limits_update -m '{ "duration": 8, "energy_wh": 50000 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/active_limits` {#charge_limits_active_limits_warp1}


Active time and energy limits for the current or next charging session. Can be updated via [`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/charge_limits/active_limits
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/active_limits
```
        </TabItem>
    </Tabs>

```jsx
{ "duration": 12, "energy_wh": 50000 }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/override_duration` {#charge_limits_override_duration_warp1}


Overrides the time limit for the current or next charging session.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/override_duration -d '{ "duration": 12 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_limits/override_duration -d '12'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_duration -m '{ "duration": 12 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_duration -m '12'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/override_energy` {#charge_limits_override_energy_warp1}


Overrides the energy limit for the current or next charging session.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/override_energy -d '{ "energy_wh": 25000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_limits/override_energy -d '25000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_energy -m '{ "energy_wh": 25000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_energy -m '25000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/restart` {#charge_limits_restart_warp1}


Resets the charge limits as if a new charging session had started, but keeps the overridden charge limits. With this API and [`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy), a new "absolute" charge limit can be set at any time.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/restart -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/restart -m 'null'
```



        </TabItem>
    </Tabs>
:::


</TabItem>
<TabItem value="warp2">
## `charge_limits/state` {#charge_limits_state_warp2}


Current state of time and energy limits
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/charge_limits/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "start_timestamp_ms": 786598059,
  "start_energy_kwh": 886.1900024,
  "target_timestamp_ms": 797398059,
  "target_energy_kwh": 901.1900024
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`start_timestamp_ms`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Start timestamp of the current charging session. 0 if no charging session is running.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`target_timestamp_ms`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Target timestamp of the current charging session. 0 if no charging session is running. Equal to the start timestamp if no time limit is set.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`start_energy_kwh`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Start meter value of the current charging session. null if no charging session is running or no electricity meter is available.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`target_energy_kwh`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Target meter value of the current charging session. null if no charging session is running or no electricity meter is available. Equal to the start meter value if no energy value is set.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/default_limits` {#charge_limits_default_limits_warp2}


Configuration of time and energy limits for charging sessions.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/charge_limits/default_limits
```
```jsx
{ "duration": 8, "energy_wh": 50000 }
```



#### Write
```bash
curl http://$HOST/charge_limits/default_limits -d '{ "duration": 8, "energy_wh": 50000 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/default_limits
```
```jsx
{ "duration": 8, "energy_wh": 50000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_limits/default_limits<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/default_limits_update -m '{ "duration": 8, "energy_wh": 50000 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/active_limits` {#charge_limits_active_limits_warp2}


Active time and energy limits for the current or next charging session. Can be updated via [`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/charge_limits/active_limits
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/active_limits
```
        </TabItem>
    </Tabs>

```jsx
{ "duration": 12, "energy_wh": 50000 }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/override_duration` {#charge_limits_override_duration_warp2}


Overrides the time limit for the current or next charging session.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/override_duration -d '{ "duration": 12 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_limits/override_duration -d '12'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_duration -m '{ "duration": 12 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_duration -m '12'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/override_energy` {#charge_limits_override_energy_warp2}


Overrides the energy limit for the current or next charging session.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/override_energy -d '{ "energy_wh": 25000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_limits/override_energy -d '25000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_energy -m '{ "energy_wh": 25000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_energy -m '25000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/restart` {#charge_limits_restart_warp2}


Resets the charge limits as if a new charging session had started, but keeps the overridden charge limits. With this API and [`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy), a new "absolute" charge limit can be set at any time.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/restart -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/restart -m 'null'
```



        </TabItem>
    </Tabs>
:::


</TabItem>
<TabItem value="warp3">
## `charge_limits/state` {#charge_limits_state_warp3}


Current state of time and energy limits
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/charge_limits/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "start_timestamp_ms": 786598059,
  "start_energy_kwh": 886.1900024,
  "target_timestamp_ms": 797398059,
  "target_energy_kwh": 901.1900024
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`start_timestamp_ms`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Start timestamp of the current charging session. 0 if no charging session is running.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`target_timestamp_ms`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Target timestamp of the current charging session. 0 if no charging session is running. Equal to the start timestamp if no time limit is set.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`start_energy_kwh`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Start meter value of the current charging session. null if no charging session is running or no electricity meter is available.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`target_energy_kwh`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Target meter value of the current charging session. null if no charging session is running or no electricity meter is available. Equal to the start meter value if no energy value is set.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/default_limits` {#charge_limits_default_limits_warp3}


Configuration of time and energy limits for charging sessions.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/charge_limits/default_limits
```
```jsx
{ "duration": 8, "energy_wh": 50000 }
```



#### Write
```bash
curl http://$HOST/charge_limits/default_limits -d '{ "duration": 8, "energy_wh": 50000 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/default_limits
```
```jsx
{ "duration": 8, "energy_wh": 50000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_limits/default_limits<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/default_limits_update -m '{ "duration": 8, "energy_wh": 50000 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/active_limits` {#charge_limits_active_limits_warp3}


Active time and energy limits for the current or next charging session. Can be updated via [`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/charge_limits/active_limits
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/active_limits
```
        </TabItem>
    </Tabs>

```jsx
{ "duration": 12, "energy_wh": 50000 }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/override_duration` {#charge_limits_override_duration_warp3}


Overrides the time limit for the current or next charging session.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/override_duration -d '{ "duration": 12 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_limits/override_duration -d '12'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_duration -m '{ "duration": 12 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_duration -m '12'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/override_energy` {#charge_limits_override_energy_warp3}


Overrides the energy limit for the current or next charging session.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/override_energy -d '{ "energy_wh": 25000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_limits/override_energy -d '25000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_energy -m '{ "energy_wh": 25000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_energy -m '25000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/restart` {#charge_limits_restart_warp3}


Resets the charge limits as if a new charging session had started, but keeps the overridden charge limits. With this API and [`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy), a new "absolute" charge limit can be set at any time.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/restart -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/restart -m 'null'
```



        </TabItem>
    </Tabs>
:::


</TabItem>
<TabItem value="wem">
Not supported on this hardware!
</TabItem>
<TabItem value="wem2">
Not supported on this hardware!
</TabItem>
<TabItem value="any">
## `charge_limits/state` {#charge_limits_state_any}


Current state of time and energy limits
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/charge_limits/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/state
```
        </TabItem>
    </Tabs>

```jsx
{
  "start_timestamp_ms": 786598059,
  "start_energy_kwh": 886.1900024,
  "target_timestamp_ms": 797398059,
  "target_energy_kwh": 901.1900024
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`start_timestamp_ms`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Start timestamp of the current charging session. 0 if no charging session is running.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`target_timestamp_ms`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
    <td>
        Target timestamp of the current charging session. 0 if no charging session is running. Equal to the start timestamp if no time limit is set.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`start_energy_kwh`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Start meter value of the current charging session. null if no charging session is running or no electricity meter is available.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`target_energy_kwh`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Target meter value of the current charging session. null if no charging session is running or no electricity meter is available. Equal to the start meter value if no energy value is set.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/default_limits` {#charge_limits_default_limits_any}


Configuration of time and energy limits for charging sessions.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/charge_limits/default_limits
```
```jsx
{ "duration": 8, "energy_wh": 50000 }
```



#### Write
```bash
curl http://$HOST/charge_limits/default_limits -d '{ "duration": 8, "energy_wh": 50000 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/default_limits
```
```jsx
{ "duration": 8, "energy_wh": 50000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_limits/default_limits<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/default_limits_update -m '{ "duration": 8, "energy_wh": 50000 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        
        
        
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/active_limits` {#charge_limits_active_limits_any}


Active time and energy limits for the current or next charging session. Can be updated via [`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/charge_limits/active_limits
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_limits/active_limits
```
        </TabItem>
    </Tabs>

```jsx
{ "duration": 12, "energy_wh": 50000 }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        
        
        
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/override_duration` {#charge_limits_override_duration_any}


Overrides the time limit for the current or next charging session.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/override_duration -d '{ "duration": 12 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_limits/override_duration -d '12'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_duration -m '{ "duration": 12 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_duration -m '12'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`duration`<br/>int</th>
    <td>
        Time limit of a charging session. The charging session is stopped after the time limit expires.
        
        
        
        - **0** - Unlimited
        - **1** - 15 minutes
        - **2** - 30 minutes
        - **3** - 45 minutes
        - **4** - 1 hour
        - **5** - 2 hours
        - **6** - 3 hours
        - **7** - 4 hours
        - **8** - 6 hours
        - **9** - 8 hours
        - **10** - 12 hours
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/override_energy` {#charge_limits_override_energy_any}


Overrides the energy limit for the current or next charging session.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/override_energy -d '{ "energy_wh": 25000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_limits/override_energy -d '25000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_energy -m '{ "energy_wh": 25000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/override_energy -m '25000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`energy_wh`<br/>int (<abbr title="Watt-hour">Wh</abbr>)</th>
    <td>
        Energy limit of a charging session. The charging session is stopped after the energy limit expires.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_limits/restart` {#charge_limits_restart_any}


Resets the charge limits as if a new charging session had started, but keeps the overridden charge limits. With this API and [`charge_limits/override_duration`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_duration) and [`charge_limits/override_energy`](/interfaces/mqtt_http/api_reference/charge_limits.mdx#charge_limits_override_energy), a new "absolute" charge limit can be set at any time.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/charge_limits/restart -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_limits/restart -m 'null'
```



        </TabItem>
    </Tabs>
:::


</TabItem>
</Tabs>

export const toc = [
    {
    "value": "charge_limits/state",
    "id": "charge_limits_state",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "charge_limits/default_limits",
    "id": "charge_limits_default_limits",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "charge_limits/active_limits",
    "id": "charge_limits_active_limits",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "charge_limits/override_duration",
    "id": "charge_limits_override_duration",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "charge_limits/override_energy",
    "id": "charge_limits_override_energy",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "charge_limits/restart",
    "id": "charge_limits_restart",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},
];
