---
description: TLS Certificate Management
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/certs.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# TLS Certificate Management
The `certs` module manages TLS certificates used for establishing TLS connections (e.g., for https or mqtts). In addition to the certificates that can be stored here, all firmwares have the [Mozilla NSS project certificate bundle](https://docs.espressif.com/projects/esp-idf/en/latest/esp32/api-reference/protocols/esp_crt_bundle.html) embedded. This bundle includes root certificates that allow connections to most publicly accessible servers.
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `certs/state` {#certs_state_warp1}


The current state of TLS certificate management.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/certs/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/certs/state
```
        </TabItem>
    </Tabs>

```jsx
{ "certs": [{ "id": 1, "name": "ISRG Root X1" }] }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`certs`<br/>object[8]</th>
    <td>
        Known TLS certificates.
        - **[0..7]** - A TLS certificate
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
            <td>
                ID of the certificate
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the certificate
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/add` {#certs_add_warp1}


Adds a new TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/certs/add -d '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/add -m '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be added. Must not already be used by a certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/modify` {#certs_modify_warp1}


Modifies an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/certs/modify -d '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/modify -m '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be modified.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/remove` {#certs_remove_warp1}


Deletes an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/certs/remove -d '{ "id": 0 }'
```

or abbreviated:

```bash
curl http://$HOST/certs/remove -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '{ "id": 0 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be deleted.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `certs/state` {#certs_state_warp2}


The current state of TLS certificate management.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/certs/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/certs/state
```
        </TabItem>
    </Tabs>

```jsx
{ "certs": [{ "id": 1, "name": "ISRG Root X1" }] }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`certs`<br/>object[8]</th>
    <td>
        Known TLS certificates.
        - **[0..7]** - A TLS certificate
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
            <td>
                ID of the certificate
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the certificate
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/add` {#certs_add_warp2}


Adds a new TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/certs/add -d '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/add -m '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be added. Must not already be used by a certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/modify` {#certs_modify_warp2}


Modifies an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/certs/modify -d '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/modify -m '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be modified.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/remove` {#certs_remove_warp2}


Deletes an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/certs/remove -d '{ "id": 0 }'
```

or abbreviated:

```bash
curl http://$HOST/certs/remove -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '{ "id": 0 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be deleted.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `certs/state` {#certs_state_warp3}


The current state of TLS certificate management.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/certs/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/certs/state
```
        </TabItem>
    </Tabs>

```jsx
{ "certs": [{ "id": 1, "name": "ISRG Root X1" }] }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`certs`<br/>object[8]</th>
    <td>
        Known TLS certificates.
        - **[0..7]** - A TLS certificate
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
            <td>
                ID of the certificate
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the certificate
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/add` {#certs_add_warp3}


Adds a new TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/certs/add -d '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/add -m '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be added. Must not already be used by a certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/modify` {#certs_modify_warp3}


Modifies an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/certs/modify -d '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/modify -m '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be modified.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/remove` {#certs_remove_warp3}


Deletes an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/certs/remove -d '{ "id": 0 }'
```

or abbreviated:

```bash
curl http://$HOST/certs/remove -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '{ "id": 0 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be deleted.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `certs/state` {#certs_state_wem}


The current state of TLS certificate management.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/certs/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/certs/state
```
        </TabItem>
    </Tabs>

```jsx
{ "certs": [{ "id": 1, "name": "ISRG Root X1" }] }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`certs`<br/>object[8]</th>
    <td>
        Known TLS certificates.
        - **[0..7]** - A TLS certificate
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
            <td>
                ID of the certificate
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the certificate
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/add` {#certs_add_wem}


Adds a new TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Write
```bash
curl http://$HOST/certs/add -d '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/add -m '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be added. Must not already be used by a certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/modify` {#certs_modify_wem}


Modifies an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Write
```bash
curl http://$HOST/certs/modify -d '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/modify -m '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be modified.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/remove` {#certs_remove_wem}


Deletes an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Write
```bash
curl http://$HOST/certs/remove -d '{ "id": 0 }'
```

or abbreviated:

```bash
curl http://$HOST/certs/remove -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '{ "id": 0 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be deleted.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `certs/state` {#certs_state_wem2}


The current state of TLS certificate management.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Read
```bash
curl http://$HOST/certs/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/certs/state
```
        </TabItem>
    </Tabs>

```jsx
{ "certs": [{ "id": 1, "name": "ISRG Root X1" }] }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`certs`<br/>object[8]</th>
    <td>
        Known TLS certificates.
        - **[0..7]** - A TLS certificate
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
            <td>
                ID of the certificate
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the certificate
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/add` {#certs_add_wem2}


Adds a new TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Write
```bash
curl http://$HOST/certs/add -d '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/add -m '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be added. Must not already be used by a certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/modify` {#certs_modify_wem2}


Modifies an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Write
```bash
curl http://$HOST/certs/modify -d '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/modify -m '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be modified.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/remove` {#certs_remove_wem2}


Deletes an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Write
```bash
curl http://$HOST/certs/remove -d '{ "id": 0 }'
```

or abbreviated:

```bash
curl http://$HOST/certs/remove -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '{ "id": 0 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be deleted.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `certs/state` {#certs_state_any}


The current state of TLS certificate management.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/certs/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/certs/state
```
        </TabItem>
    </Tabs>

```jsx
{ "certs": [{ "id": 1, "name": "ISRG Root X1" }] }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`certs`<br/>object[8]</th>
    <td>
        Known TLS certificates.
        
        
        
        - **[0..7]** - A TLS certificate
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
            <td>
                ID of the certificate
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the certificate
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/add` {#certs_add_any}


Adds a new TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/certs/add -d '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/add -m '{
  "id": 0,
  "name": "ISRG Root X1",
  "cert": "-----BEGIN CERTIFICATE-----\nMIIFYDCC[__Zertifikatspayload gekürzt__]psr6GtPAQw4dy753ec5\n-----END CERTIFICATE-----"
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be added. Must not already be used by a certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/modify` {#certs_modify_any}


Modifies an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/certs/modify -d '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/modify -m '{ "id": 0, "name": "ISRG Root X1 (Lets Encrypt)", "cert": null }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be modified.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Display name of the certificate.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert`<br/>string</th>
    <td>
        Certificate in PEM format
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `certs/remove` {#certs_remove_any}


Deletes an existing TLS certificate.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/certs/remove -d '{ "id": 0 }'
```

or abbreviated:

```bash
curl http://$HOST/certs/remove -d '0'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '{ "id": 0 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/certs/remove -m '0'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`id`<br/>int</th>
    <td>
        ID of the certificate to be deleted.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "certs/state",
    "id": "certs_state",
    "level": 2,
    
},{
    "value": "certs/add",
    "id": "certs_add",
    "level": 2,
    
},{
    "value": "certs/modify",
    "id": "certs_modify",
    "level": 2,
    
},{
    "value": "certs/remove",
    "id": "certs_remove",
    "level": 2,
    
},
];
