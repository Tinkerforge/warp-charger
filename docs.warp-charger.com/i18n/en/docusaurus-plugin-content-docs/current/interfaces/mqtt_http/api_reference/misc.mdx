---
description: Miscellaneous
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/misc.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Miscellaneous

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `reboot` {#reboot_warp1}


Restarts the ESP to apply configuration changes, for example.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_warp1}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generates a debug report. This consists of all states and configurations, as well as the last received commands and configuration updates. Passwords are censored, just like in configuration queries.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_warp1}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Forces an immediate restart of the ESP. Useful if [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) hangs for any reason.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_warp1}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Accepts a firmware update as POST which is then flashed.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_warp1}


Resets all settings but keeps recorded charging sessions and username history. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_warp1}


Resets all settings and deletes all recorded charging sessions. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings, recorded charging sessions, and username history
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `reboot` {#reboot_warp2}


Restarts the ESP to apply configuration changes, for example.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_warp2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generates a debug report. This consists of all states and configurations, as well as the last received commands and configuration updates. Passwords are censored, just like in configuration queries.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_warp2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Forces an immediate restart of the ESP. Useful if [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) hangs for any reason.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_warp2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Accepts a firmware update as POST which is then flashed.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_warp2}


Resets all settings but keeps recorded charging sessions and username history. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_warp2}


Resets all settings and deletes all recorded charging sessions. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings, recorded charging sessions, and username history
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `reboot` {#reboot_warp3}


Restarts the ESP to apply configuration changes, for example.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_warp3}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generates a debug report. This consists of all states and configurations, as well as the last received commands and configuration updates. Passwords are censored, just like in configuration queries.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_warp3}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Forces an immediate restart of the ESP. Useful if [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) hangs for any reason.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_warp3}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Accepts a firmware update as POST which is then flashed.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_warp3}


Resets all settings but keeps recorded charging sessions and username history. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_warp3}


Resets all settings and deletes all recorded charging sessions. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings, recorded charging sessions, and username history
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `reboot` {#reboot_wem}


Restarts the ESP to apply configuration changes, for example.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Write
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_wem}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generates a debug report. This consists of all states and configurations, as well as the last received commands and configuration updates. Passwords are censored, just like in configuration queries.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_wem}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Forces an immediate restart of the ESP. Useful if [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) hangs for any reason.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_wem}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Accepts a firmware update as POST which is then flashed.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_wem}


Resets all settings but keeps recorded charging sessions and username history. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_wem}


Resets all settings and deletes all recorded charging sessions. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings, recorded charging sessions, and username history
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `reboot` {#reboot_wem2}


Restarts the ESP to apply configuration changes, for example.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Write
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_wem2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generates a debug report. This consists of all states and configurations, as well as the last received commands and configuration updates. Passwords are censored, just like in configuration queries.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_wem2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Forces an immediate restart of the ESP. Useful if [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) hangs for any reason.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_wem2}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Accepts a firmware update as POST which is then flashed.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_wem2}


Resets all settings but keeps recorded charging sessions and username history. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_wem2}


Resets all settings and deletes all recorded charging sessions. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        - **true** - Delete all settings, recorded charging sessions, and username history
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `reboot` {#reboot_any}


Restarts the ESP to apply configuration changes, for example.
**Empty payload. One of the following values must be passed: `null`, `""`, `false`, `0`, `[]` or `{}`**
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/reboot -d 'null'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/reboot -m 'null'
```



        </TabItem>
    </Tabs>
:::



<br/><br/><br/>
## `debug_report` {#debug_report_any}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Generates a debug report. This consists of all states and configurations, as well as the last received commands and configuration updates. Passwords are censored, just like in configuration queries.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `force_reboot` {#force_reboot_any}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Forces an immediate restart of the ESP. Useful if [reboot](/interfaces/mqtt_http/api_reference/misc.mdx#reboot) hangs for any reason.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `flash_firmware` {#flash_firmware_any}
<Tabs groupId="apiType" queryString className="hidden-tabs">
    <TabItem value="http" label="HTTP (curl)">


Accepts a firmware update as POST which is then flashed.


    </TabItem>
    <TabItem value="mqtt" label="MQTT (mosquitto)">
        <Admonition type="warning" title="Only supported by the HTTP API"></Admonition>
    </TabItem>
</Tabs>

<br/><br/><br/>
## `config_reset` {#config_reset_any}


Resets all settings but keeps recorded charging sessions and username history. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        
        
        
        - **true** - Delete all settings
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `factory_reset` {#factory_reset_any}


Resets all settings and deletes all recorded charging sessions. <strong>Cannot be undone!</strong> A restart will be performed automatically afterwards.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`do_i_know_what_i_am_doing`<br/>bool</th>
    <td>
        Indicates whether the deletion process should be executed
        
        
        
        - **true** - Delete all settings, recorded charging sessions, and username history
        - **false** - Perform no action
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "reboot",
    "id": "reboot",
    "level": 2,
    
},{
    "value": "debug_report",
    "id": "debug_report",
    "level": 2,
    
},{
    "value": "force_reboot",
    "id": "force_reboot",
    "level": 2,
    
},{
    "value": "flash_firmware",
    "id": "flash_firmware",
    "level": 2,
    
},{
    "value": "config_reset",
    "id": "config_reset",
    "level": 2,
    
},{
    "value": "factory_reset",
    "id": "factory_reset",
    "level": 2,
    
},
];
