---
description: Network Configuration
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/network.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Network Configuration

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `network/config` {#network_config_warp1}


General network settings
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/network/config
```
```jsx
{ "hostname": "warp-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
```bash
curl http://$HOST/network/config -d '{ "hostname": "warp-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/network/config
```
```jsx
{ "hostname": "warp-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
            With MQTT on <code>$PREFIX/network/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/network/config_update -m '{ "hostname": "warp-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hostname`<br/>string</th>
    <td>
        Hostname the device should use in all configured networks.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_mdns`<br/>bool</th>
    <td>
        Indicates whether the device should advertise the web interface via mDNS on the network. Via mDNS, the web interface will be accessible at http://[hostname].local (e.g., http://warp2-AbC.local).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`web_server_port`<br/>int</th>
    <td>
        Listen port of the web interface. Typically 80
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `network/state` {#network_state_warp1}


General network state


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connected`<br/>bool</th>
    <td>
        Indicates whether the device is connected to a network.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `network/config` {#network_config_warp2}


General network settings
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/network/config
```
```jsx
{ "hostname": "warp2-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
```bash
curl http://$HOST/network/config -d '{ "hostname": "warp2-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/network/config
```
```jsx
{ "hostname": "warp2-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
            With MQTT on <code>$PREFIX/network/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/network/config_update -m '{ "hostname": "warp2-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hostname`<br/>string</th>
    <td>
        Hostname the device should use in all configured networks.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_mdns`<br/>bool</th>
    <td>
        Indicates whether the device should advertise the web interface via mDNS on the network. Via mDNS, the web interface will be accessible at http://[hostname].local (e.g., http://warp2-AbC.local).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`web_server_port`<br/>int</th>
    <td>
        Listen port of the web interface. Typically 80
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `network/state` {#network_state_warp2}


General network state


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connected`<br/>bool</th>
    <td>
        Indicates whether the device is connected to a network.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `network/config` {#network_config_warp3}


General network settings
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/network/config
```
```jsx
{ "hostname": "warp3-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
```bash
curl http://$HOST/network/config -d '{ "hostname": "warp3-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/network/config
```
```jsx
{ "hostname": "warp3-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
            With MQTT on <code>$PREFIX/network/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/network/config_update -m '{ "hostname": "warp3-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hostname`<br/>string</th>
    <td>
        Hostname the device should use in all configured networks.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_mdns`<br/>bool</th>
    <td>
        Indicates whether the device should advertise the web interface via mDNS on the network. Via mDNS, the web interface will be accessible at http://[hostname].local (e.g., http://warp2-AbC.local).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`web_server_port`<br/>int</th>
    <td>
        Listen port of the web interface. Typically 80
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `network/state` {#network_state_warp3}


General network state


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connected`<br/>bool</th>
    <td>
        Indicates whether the device is connected to a network.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `network/config` {#network_config_wem}


General network settings
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```

#### Read
```bash
curl http://$HOST/network/config
```
```jsx
{ "hostname": "wem-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
```bash
curl http://$HOST/network/config -d '{ "hostname": "wem-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/network/config
```
```jsx
{ "hostname": "wem-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
            With MQTT on <code>$PREFIX/network/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/network/config_update -m '{ "hostname": "wem-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hostname`<br/>string</th>
    <td>
        Hostname the device should use in all configured networks.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_mdns`<br/>bool</th>
    <td>
        Indicates whether the device should advertise the web interface via mDNS on the network. Via mDNS, the web interface will be accessible at http://[hostname].local (e.g., http://warp2-AbC.local).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`web_server_port`<br/>int</th>
    <td>
        Listen port of the web interface. Typically 80
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `network/state` {#network_state_wem}


General network state


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connected`<br/>bool</th>
    <td>
        Indicates whether the device is connected to a network.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `network/config` {#network_config_wem2}


General network settings


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hostname`<br/>string</th>
    <td>
        Hostname the device should use in all configured networks.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_mdns`<br/>bool</th>
    <td>
        Indicates whether the device should advertise the web interface via mDNS on the network. Via mDNS, the web interface will be accessible at http://[hostname].local (e.g., http://warp2-AbC.local).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`web_server_port`<br/>int</th>
    <td>
        Listen port of the web interface. Typically 80
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `network/state` {#network_state_wem2}


General network state


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connected`<br/>bool</th>
    <td>
        Indicates whether the device is connected to a network.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `network/config` {#network_config_any}


General network settings
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/network/config
```
```jsx
{ "hostname": "warp-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
```bash
curl http://$HOST/network/config -d '{ "hostname": "warp-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/network/config
```
```jsx
{ "hostname": "warp-AbCd", "enable_mdns": true, "web_server_port": 80 }
```



#### Write
            With MQTT on <code>$PREFIX/network/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/network/config_update -m '{ "hostname": "warp-AbCd", "enable_mdns": true, "web_server_port": 80 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`hostname`<br/>string</th>
    <td>
        Hostname the device should use in all configured networks.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_mdns`<br/>bool</th>
    <td>
        Indicates whether the device should advertise the web interface via mDNS on the network. Via mDNS, the web interface will be accessible at http://[hostname].local (e.g., http://warp2-AbC.local).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`web_server_port`<br/>int</th>
    <td>
        Listen port of the web interface. Typically 80
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `network/state` {#network_state_any}


General network state


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`connected`<br/>bool</th>
    <td>
        Indicates whether the device is connected to a network.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "network/config",
    "id": "network_config",
    "level": 2,
    
},{
    "value": "network/state",
    "id": "network_state",
    "level": 2,
    
},
];
