---
description: Vehicles
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/ev.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Vehicles
The `ev` module allows configuring vehicle profiles.
    Vehicles are identified by their MAC address, which they transmit e.g. when connecting
    via ISO 15118 (Autocharge) or which can be spoofed via the API using [`ev/inject_ev`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_inject_ev).
    Based on the matched profile, charging sessions can be assigned to a user (Autocharge)
    and the state of charge (SoC) of the vehicle can be estimated from battery capacity and charging efficiency.
    
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
Not supported on this hardware!
</TabItem>
<TabItem value="warp2">
Not supported on this hardware!
</TabItem>
<TabItem value="warp3">
Not supported on this hardware!
</TabItem>
<TabItem value="warp4">
## `ev/state` {#ev_state_warp4}


The state of the currently connected vehicle.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp4-AbCd
```
#### Read
```bash
curl http://$HOST/ev/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp4/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active_ev_index": 0,
    "name": "ID.4",
    "mac": "0A:1B:2C:D3:E4:F5",
    "soc": 42.5,
    "capacity": 77.0,
    "charging_efficiency": 0.92
}
```
A vehicle is connected and was matched to the first vehicle profile.
The estimated state of charge is 42.5 %.

:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active_ev_index`<br/>int</th>
    <td>
        Index of the matched vehicle profile in [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config). -1 if no vehicle is connected or the vehicle could not be matched to a profile.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Name of the matched vehicle profile. Empty if no vehicle is connected or the vehicle could not be matched to a profile.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
    <td>
        MAC address of the connected vehicle in the format `AA:BB:CC:DD:EE:FF`. Empty if no vehicle is connected.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`soc`<br/>float (<abbr title="Percent">%</abbr>)</th>
    <td>
        Current state of charge (SoC) of the vehicle in percent. Either transmitted by the vehicle (ISO 15118-20), set via [`ev/inject_soc`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_inject_soc) or estimated from the charged energy, the battery capacity and the charging efficiency. null if unknown.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`capacity`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Battery capacity of the connected vehicle in kWh. Taken from the matched vehicle profile or transmitted by the vehicle (ISO 15118-20). null if no vehicle is connected.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`charging_efficiency`<br/>float</th>
    <td>
        Charging efficiency of the connected vehicle: ratio of stored to consumed energy. Taken from the matched vehicle profile. 0.92 if no profile is matched.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/seen_macs` {#ev_seen_macs_warp4}


The MAC addresses of the most recently connected vehicles (up to 8). Helpful for determining the MAC address of a vehicle to be configured in [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp4-AbCd
```
#### Read
```bash
curl http://$HOST/ev/seen_macs
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp4/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/seen_macs
```
        </TabItem>
    </Tabs>

```jsx
[
    { "mac": "0A:1B:2C:D3:E4:F5", "seen_at": 1234567 },
    { "mac": "5C:AF:06:12:34:56", "seen_at": 42000 }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[..]`<br/>object</th>
    <td>
        A seen MAC address
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
            <td>
                MAC address of the vehicle in the format `AA:BB:CC:DD:EE:FF`.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`seen_at`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time at which the MAC address was last seen, in milliseconds since boot.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/inject_soc` {#ev_inject_soc_warp4}


Sets the state of charge (SoC) of the currently connected vehicle via the API. Starting from this value, the SoC is further estimated from the charged energy, the battery capacity and the charging efficiency.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp4-AbCd
```
#### Write
```bash
curl http://$HOST/ev/inject_soc -d '{ "soc": 42.5 }'
```

or abbreviated:

```bash
curl http://$HOST/ev/inject_soc -d '42.5'
```


Sets the state of charge of the connected vehicle to 42.5 %.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp4/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_soc -m '{ "soc": 42.5 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_soc -m '42.5'
```


Sets the state of charge of the connected vehicle to 42.5 %.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`soc`<br/>float (<abbr title="Percent">%</abbr>)</th>
    <td>
        State of charge in percent. Must be between 0 and 100.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/inject_ev` {#ev_inject_ev_warp4}


Pretends that a vehicle with the given MAC address has connected. This allows identifying a vehicle via the API that does not transmit its MAC address itself (via ISO 15118). If the MAC address can be matched to a vehicle profile from [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config), its settings are applied and (if Autocharge is active) the charging session is assigned to the configured user.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp4-AbCd
```
#### Write
```bash
curl http://$HOST/ev/inject_ev -d '{ "mac": "0A:1B:2C:D3:E4:F5" }'
```

or abbreviated:

```bash
curl http://$HOST/ev/inject_ev -d '"0A:1B:2C:D3:E4:F5"'
```


Pretends that a vehicle with the MAC address 0A:1B:2C:D3:E4:F5 has connected.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp4/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_ev -m '{ "mac": "0A:1B:2C:D3:E4:F5" }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_ev -m '"0A:1B:2C:D3:E4:F5"'
```


Pretends that a vehicle with the MAC address 0A:1B:2C:D3:E4:F5 has connected.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
    <td>
        MAC address of the vehicle in the format `AA:BB:CC:DD:EE:FF`.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/config` {#ev_config_warp4}


The configured vehicle profiles.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp4-AbCd
```

#### Read
```bash
curl http://$HOST/ev/config
```
```jsx
{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}
```
Configures two vehicle profiles: One vehicle with a rotating MAC address
(only the first three bytes are used for identification) whose charging sessions
are assigned to the user with ID 2, and one vehicle with a fixed MAC address
without user assignment.


#### Write
```bash
curl http://$HOST/ev/config -d '{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}'
```

or abbreviated:

```bash
curl http://$HOST/ev/config -d '[
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]'
```


Configures two vehicle profiles: One vehicle with a rotating MAC address
(only the first three bytes are used for identification) whose charging sessions
are assigned to the user with ID 2, and one vehicle with a fixed MAC address
without user assignment.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp4/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/config
```
```jsx
{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}
```
Configures two vehicle profiles: One vehicle with a rotating MAC address
(only the first three bytes are used for identification) whose charging sessions
are assigned to the user with ID 2, and one vehicle with a fixed MAC address
without user assignment.


#### Write
            With MQTT on <code>$PREFIX/ev/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/config_update -m '{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/config_update -m '[
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]'
```


Configures two vehicle profiles: One vehicle with a rotating MAC address
(only the first three bytes are used for identification) whose charging sessions
are assigned to the user with ID 2, and one vehicle with a fixed MAC address
without user assignment.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`evs`<br/>object[..]</th>
    <td>
        List of vehicle profiles (up to 16).
        - **[0]** - A vehicle profile
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Name to identify this vehicle. Up to 16 characters.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
            <td>
                MAC address of the vehicle in the format `AA:BB:CC:DD:EE:FF`. If the vehicle changes part of its address between charging sessions (e.g. VW ID models), the changing bytes can be marked with `xx` as wildcard, e.g. `AA:BB:CC:xx:xx:xx`.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`capacity`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
            <td>
                Battery capacity of the vehicle in kWh. Used to estimate the state of charge. 0 if unknown (then 60 kWh are assumed).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`charging_efficiency`<br/>float</th>
            <td>
                Charging efficiency of the vehicle: ratio of stored to consumed energy. Used to estimate the state of charge. Typically 0.92.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID of the user this vehicle is assigned to, or 0 if it is not assigned to any user. If the vehicle is detected and Autocharge is active, the charging session is assigned to this user.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
Not supported on this hardware!
</TabItem>
<TabItem value="wem2">
Not supported on this hardware!
</TabItem>
<TabItem value="any">
## `ev/state` {#ev_state_any}


The state of the currently connected vehicle.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/ev/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active_ev_index": 0,
    "name": "ID.4",
    "mac": "0A:1B:2C:D3:E4:F5",
    "soc": 42.5,
    "capacity": 77.0,
    "charging_efficiency": 0.92
}
```
A vehicle is connected and was matched to the first vehicle profile.
The estimated state of charge is 42.5 %.

:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active_ev_index`<br/>int</th>
    <td>
        Index of the matched vehicle profile in [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config). -1 if no vehicle is connected or the vehicle could not be matched to a profile.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        Name of the matched vehicle profile. Empty if no vehicle is connected or the vehicle could not be matched to a profile.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
    <td>
        MAC address of the connected vehicle in the format `AA:BB:CC:DD:EE:FF`. Empty if no vehicle is connected.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`soc`<br/>float (<abbr title="Percent">%</abbr>)</th>
    <td>
        Current state of charge (SoC) of the vehicle in percent. Either transmitted by the vehicle (ISO 15118-20), set via [`ev/inject_soc`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_inject_soc) or estimated from the charged energy, the battery capacity and the charging efficiency. null if unknown.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`capacity`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
    <td>
        Battery capacity of the connected vehicle in kWh. Taken from the matched vehicle profile or transmitted by the vehicle (ISO 15118-20). null if no vehicle is connected.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`charging_efficiency`<br/>float</th>
    <td>
        Charging efficiency of the connected vehicle: ratio of stored to consumed energy. Taken from the matched vehicle profile. 0.92 if no profile is matched.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/seen_macs` {#ev_seen_macs_any}


The MAC addresses of the most recently connected vehicles (up to 8). Helpful for determining the MAC address of a vehicle to be configured in [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/ev/seen_macs
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/seen_macs
```
        </TabItem>
    </Tabs>

```jsx
[
    { "mac": "0A:1B:2C:D3:E4:F5", "seen_at": 1234567 },
    { "mac": "5C:AF:06:12:34:56", "seen_at": 42000 }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[..]`<br/>object</th>
    <td>
        A seen MAC address
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
            <td>
                MAC address of the vehicle in the format `AA:BB:CC:DD:EE:FF`.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`seen_at`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time at which the MAC address was last seen, in milliseconds since boot.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/inject_soc` {#ev_inject_soc_any}


Sets the state of charge (SoC) of the currently connected vehicle via the API. Starting from this value, the SoC is further estimated from the charged energy, the battery capacity and the charging efficiency.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/ev/inject_soc -d '{ "soc": 42.5 }'
```

or abbreviated:

```bash
curl http://$HOST/ev/inject_soc -d '42.5'
```


Sets the state of charge of the connected vehicle to 42.5 %.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_soc -m '{ "soc": 42.5 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_soc -m '42.5'
```


Sets the state of charge of the connected vehicle to 42.5 %.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`soc`<br/>float (<abbr title="Percent">%</abbr>)</th>
    <td>
        State of charge in percent. Must be between 0 and 100.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/inject_ev` {#ev_inject_ev_any}


Pretends that a vehicle with the given MAC address has connected. This allows identifying a vehicle via the API that does not transmit its MAC address itself (via ISO 15118). If the MAC address can be matched to a vehicle profile from [`ev/config`](/interfaces/mqtt_http/api_reference/ev.mdx#ev_config), its settings are applied and (if Autocharge is active) the charging session is assigned to the configured user.
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/ev/inject_ev -d '{ "mac": "0A:1B:2C:D3:E4:F5" }'
```

or abbreviated:

```bash
curl http://$HOST/ev/inject_ev -d '"0A:1B:2C:D3:E4:F5"'
```


Pretends that a vehicle with the MAC address 0A:1B:2C:D3:E4:F5 has connected.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_ev -m '{ "mac": "0A:1B:2C:D3:E4:F5" }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/inject_ev -m '"0A:1B:2C:D3:E4:F5"'
```


Pretends that a vehicle with the MAC address 0A:1B:2C:D3:E4:F5 has connected.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
    <td>
        MAC address of the vehicle in the format `AA:BB:CC:DD:EE:FF`.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `ev/config` {#ev_config_any}


The configured vehicle profiles.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/ev/config
```
```jsx
{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}
```
Configures two vehicle profiles: One vehicle with a rotating MAC address
(only the first three bytes are used for identification) whose charging sessions
are assigned to the user with ID 2, and one vehicle with a fixed MAC address
without user assignment.


#### Write
```bash
curl http://$HOST/ev/config -d '{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}'
```

or abbreviated:

```bash
curl http://$HOST/ev/config -d '[
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]'
```


Configures two vehicle profiles: One vehicle with a rotating MAC address
(only the first three bytes are used for identification) whose charging sessions
are assigned to the user with ID 2, and one vehicle with a fixed MAC address
without user assignment.

        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/ev/config
```
```jsx
{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}
```
Configures two vehicle profiles: One vehicle with a rotating MAC address
(only the first three bytes are used for identification) whose charging sessions
are assigned to the user with ID 2, and one vehicle with a fixed MAC address
without user assignment.


#### Write
            With MQTT on <code>$PREFIX/ev/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/config_update -m '{
    "evs": [
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]
}'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/ev/config_update -m '[
        {
            "name": "ID.4",
            "mac": "0A:1B:2C:xx:xx:xx",
            "capacity": 77.0,
            "charging_efficiency": 0.92,
            "user_id": 2
        },
        {
            "name": "Zoe",
            "mac": "5C:AF:06:12:34:56",
            "capacity": 52.0,
            "charging_efficiency": 0.9,
            "user_id": 0
        }
    ]'
```


Configures two vehicle profiles: One vehicle with a rotating MAC address
(only the first three bytes are used for identification) whose charging sessions
are assigned to the user with ID 2, and one vehicle with a fixed MAC address
without user assignment.

        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`evs`<br/>object[..]</th>
    <td>
        List of vehicle profiles (up to 16).
        
        
        
        - **[0]** - A vehicle profile
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Name to identify this vehicle. Up to 16 characters.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`mac`<br/>string</th>
            <td>
                MAC address of the vehicle in the format `AA:BB:CC:DD:EE:FF`. If the vehicle changes part of its address between charging sessions (e.g. VW ID models), the changing bytes can be marked with `xx` as wildcard, e.g. `AA:BB:CC:xx:xx:xx`.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`capacity`<br/>float (<abbr title="Kilowatt-hour">kWh</abbr>)</th>
            <td>
                Battery capacity of the vehicle in kWh. Used to estimate the state of charge. 0 if unknown (then 60 kWh are assumed).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`charging_efficiency`<br/>float</th>
            <td>
                Charging efficiency of the vehicle: ratio of stored to consumed energy. Used to estimate the state of charge. Typically 0.92.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID of the user this vehicle is assigned to, or 0 if it is not assigned to any user. If the vehicle is detected and Autocharge is active, the charging session is assigned to this user.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "ev/state",
    "id": "ev_state",
    "level": 2,
    "hardwareType": ["warp4"]
},{
    "value": "ev/seen_macs",
    "id": "ev_seen_macs",
    "level": 2,
    "hardwareType": ["warp4"]
},{
    "value": "ev/inject_soc",
    "id": "ev_inject_soc",
    "level": 2,
    "hardwareType": ["warp4"]
},{
    "value": "ev/inject_ev",
    "id": "ev_inject_ev",
    "level": 2,
    "hardwareType": ["warp4"]
},{
    "value": "ev/config",
    "id": "ev_config",
    "level": 2,
    "hardwareType": ["warp4"]
},
];
