---
description: General Information
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/info.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# General Information

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `info/version` {#info_version_warp1}


Version of the currently running firmware.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`firmware`<br/>string</th>
    <td>
        The firmware version currently running. See [Semantic Versioning](https://semver.org/) format.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>string</th>
    <td>
        The version of the configuration currently in use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config_type`<br/>string</th>
    <td>
        Type of configuration stored on this device.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/modules` {#info_modules_warp1}


Initialization state of firmware modules.



<br/><br/><br/>
## `info/features` {#info_features_warp1}


Supported hardware features. See <a class="scrollto" href="#features_section">Features</a> for details.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/info/features
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/features
```
        </TabItem>
    </Tabs>

```jsx
["evse", "nfc", "meters", "meter", "meter_all_values"]
```


:::



<br/><br/><br/>
## `info/name` {#info_name_warp1}


Name and type of the device
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/info/name
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/name
```
        </TabItem>
    </Tabs>

```jsx
{
  "name": "warp-AbCd",
  "type": "warp",
  "display_type": "WARP Charger Pro 22kW +NFC",
  "uid": "AbCd"
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        The name of the device. Consists of the type and UID of the device.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`type`<br/>string</th>
    <td>
        Type of the device.
        - **warp** - A WARP Charger
        - **warp2** - A WARP2 Charger
        - **warp3** - A WARP3 Charger
        - **wem** - A WARP Energy Manager
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_type`<br/>string</th>
    <td>
        User-readable type of the device. Indicates the hardware version, variant (Smart, Pro), maximum charging power, and any upgrades, e.g., "WARP2 Charger Pro 11kW +NFC"
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`uid`<br/>string</th>
    <td>
        UID of the device or the installed ESP (Ethernet) Brick.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/display_name` {#info_display_name_warp1}


User-readable name of the device.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
```bash
curl http://$HOST/info/display_name -d '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
curl http://$HOST/info/display_name -d '"Wallbox Garage links"'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
            With MQTT on <code>$PREFIX/info/display_name<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '"Wallbox Garage links"'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_name`<br/>string</th>
    <td>
        The display name.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/last_boots` {#info_last_boots_warp1}


Debug information about the last firmware executions
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/info/last_boots
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/last_boots
```
        </TabItem>
    </Tabs>

```jsx
[
  {
    "reset_reason": 3,
    "boot_count": 1,
    "timestamp_min": 28568911,
    "uptime": 1648057,
    "uptime_overflows": 0
  },
  // An entry is only recorded after 5 minutes of uptime.
  // Between boot 1 and 4, the device was restarted twice within 5 minutes (e.g. due to configuration changes)
  {
    "reset_reason": 3,
    "boot_count": 4,
    "timestamp_min": 28568946,
    "uptime": 28301027,
    "uptime_overflows": 0
  }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..9]`<br/>object</th>
    <td>
        Debug information about a firmware execution
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`reset_reason`<br/>int</th>
            <td>
                Reason for the restart. See [Espressif documentation](https://docs.espressif.com/projects/esp-idf/en/v4.4.3/esp32/api-reference/system/system.html?highlight=esp_reset_reason#_CPPv418esp_reset_reason_t).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`boot_count`<br/>int</th>
            <td>
                Counter indicating which restart since the device was disconnected from power is being performed. An entry in last_boots is only written five minutes after device startup to preserve flash memory. If a restart occurs within the first five minutes, an entry may be lost, which can be noticed by this counter.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Runtime of the firmware execution (32-bit).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime_overflows`<br/>int</th>
            <td>
                Number of uptime overflows.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`timestamp_min`<br/>int</th>
            <td>
                Unix timestamp in minutes of the firmware execution.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `info/version` {#info_version_warp2}


Version of the currently running firmware.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`firmware`<br/>string</th>
    <td>
        The firmware version currently running. See [Semantic Versioning](https://semver.org/) format.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>string</th>
    <td>
        The version of the configuration currently in use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config_type`<br/>string</th>
    <td>
        Type of configuration stored on this device.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/modules` {#info_modules_warp2}


Initialization state of firmware modules.



<br/><br/><br/>
## `info/features` {#info_features_warp2}


Supported hardware features. See <a class="scrollto" href="#features_section">Features</a> for details.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/info/features
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/features
```
        </TabItem>
    </Tabs>

```jsx
[
  "evse",
  "cp_disconnect",
  "button_configuration",
  "ethernet",
  "meters",
  "nfc",
  "meter",
  "meter_all_values",
  "meter_phases"
]
```


:::



<br/><br/><br/>
## `info/name` {#info_name_warp2}


Name and type of the device
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/info/name
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/name
```
        </TabItem>
    </Tabs>

```jsx
{
  "name": "warp2-AbCd",
  "type": "warp2",
  "display_type": "WARP2 Charger Pro 22kW",
  "uid": "AbCd"
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        The name of the device. Consists of the type and UID of the device.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`type`<br/>string</th>
    <td>
        Type of the device.
        - **warp** - A WARP Charger
        - **warp2** - A WARP2 Charger
        - **warp3** - A WARP3 Charger
        - **wem** - A WARP Energy Manager
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_type`<br/>string</th>
    <td>
        User-readable type of the device. Indicates the hardware version, variant (Smart, Pro), maximum charging power, and any upgrades, e.g., "WARP2 Charger Pro 11kW +NFC"
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`uid`<br/>string</th>
    <td>
        UID of the device or the installed ESP (Ethernet) Brick.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/display_name` {#info_display_name_warp2}


User-readable name of the device.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
```bash
curl http://$HOST/info/display_name -d '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
curl http://$HOST/info/display_name -d '"Wallbox Garage links"'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
            With MQTT on <code>$PREFIX/info/display_name<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '"Wallbox Garage links"'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_name`<br/>string</th>
    <td>
        The display name.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/last_boots` {#info_last_boots_warp2}


Debug information about the last firmware executions
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/info/last_boots
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/last_boots
```
        </TabItem>
    </Tabs>

```jsx
[
  {
    "reset_reason": 3,
    "boot_count": 1,
    "timestamp_min": 28568911,
    "uptime": 1648057,
    "uptime_overflows": 0
  },
  // An entry is only recorded after 5 minutes of uptime.
  // Between boot 1 and 4, the device was restarted twice within 5 minutes (e.g. due to configuration changes)
  {
    "reset_reason": 3,
    "boot_count": 4,
    "timestamp_min": 28568946,
    "uptime": 28301027,
    "uptime_overflows": 0
  }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..9]`<br/>object</th>
    <td>
        Debug information about a firmware execution
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`reset_reason`<br/>int</th>
            <td>
                Reason for the restart. See [Espressif documentation](https://docs.espressif.com/projects/esp-idf/en/v4.4.3/esp32/api-reference/system/system.html?highlight=esp_reset_reason#_CPPv418esp_reset_reason_t).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`boot_count`<br/>int</th>
            <td>
                Counter indicating which restart since the device was disconnected from power is being performed. An entry in last_boots is only written five minutes after device startup to preserve flash memory. If a restart occurs within the first five minutes, an entry may be lost, which can be noticed by this counter.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Runtime of the firmware execution (32-bit).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime_overflows`<br/>int</th>
            <td>
                Number of uptime overflows.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`timestamp_min`<br/>int</th>
            <td>
                Unix timestamp in minutes of the firmware execution.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `info/version` {#info_version_warp3}


Version of the currently running firmware.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/info/version
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/version
```
        </TabItem>
    </Tabs>

```jsx
{ "firmware": "2.4.0+66558ade", "config": "2.2.0", "config_type": "warp" }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`firmware`<br/>string</th>
    <td>
        The firmware version currently running. See [Semantic Versioning](https://semver.org/) format.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>string</th>
    <td>
        The version of the configuration currently in use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config_type`<br/>string</th>
    <td>
        Type of configuration stored on this device.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/modules` {#info_modules_warp3}


Initialization state of firmware modules.



<br/><br/><br/>
## `info/features` {#info_features_warp3}


Supported hardware features. See <a class="scrollto" href="#features_section">Features</a> for details.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/info/features
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/features
```
        </TabItem>
    </Tabs>

```jsx
[
  "evse",
  "cp_disconnect",
  "button_configuration",
  "ethernet",
  "meters",
  "nfc",
  "meter",
  "meter_all_values",
  "meter_phases",
  "rtc"
]
```


:::



<br/><br/><br/>
## `info/name` {#info_name_warp3}


Name and type of the device
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/info/name
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/name
```
        </TabItem>
    </Tabs>

```jsx
{
  "name": "warp3-AbCd",
  "type": "warp3",
  "display_type": "WARP3 Charger Pro 22kW",
  "uid": "AbCd"
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        The name of the device. Consists of the type and UID of the device.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`type`<br/>string</th>
    <td>
        Type of the device.
        - **warp** - A WARP Charger
        - **warp2** - A WARP2 Charger
        - **warp3** - A WARP3 Charger
        - **wem** - A WARP Energy Manager
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_type`<br/>string</th>
    <td>
        User-readable type of the device. Indicates the hardware version, variant (Smart, Pro), maximum charging power, and any upgrades, e.g., "WARP2 Charger Pro 11kW +NFC"
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`uid`<br/>string</th>
    <td>
        UID of the device or the installed ESP (Ethernet) Brick.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/display_name` {#info_display_name_warp3}


User-readable name of the device.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
```bash
curl http://$HOST/info/display_name -d '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
curl http://$HOST/info/display_name -d '"Wallbox Garage links"'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
            With MQTT on <code>$PREFIX/info/display_name<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '"Wallbox Garage links"'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_name`<br/>string</th>
    <td>
        The display name.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/last_boots` {#info_last_boots_warp3}


Debug information about the last firmware executions
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/info/last_boots
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/last_boots
```
        </TabItem>
    </Tabs>

```jsx
[
  {
    "reset_reason": 3,
    "boot_count": 1,
    "timestamp_min": 28568911,
    "uptime": 1648057,
    "uptime_overflows": 0
  },
  // An entry is only recorded after 5 minutes of uptime.
  // Between boot 1 and 4, the device was restarted twice within 5 minutes (e.g. due to configuration changes)
  {
    "reset_reason": 3,
    "boot_count": 4,
    "timestamp_min": 28568946,
    "uptime": 28301027,
    "uptime_overflows": 0
  }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..9]`<br/>object</th>
    <td>
        Debug information about a firmware execution
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`reset_reason`<br/>int</th>
            <td>
                Reason for the restart. See [Espressif documentation](https://docs.espressif.com/projects/esp-idf/en/v4.4.3/esp32/api-reference/system/system.html?highlight=esp_reset_reason#_CPPv418esp_reset_reason_t).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`boot_count`<br/>int</th>
            <td>
                Counter indicating which restart since the device was disconnected from power is being performed. An entry in last_boots is only written five minutes after device startup to preserve flash memory. If a restart occurs within the first five minutes, an entry may be lost, which can be noticed by this counter.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Runtime of the firmware execution (32-bit).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime_overflows`<br/>int</th>
            <td>
                Number of uptime overflows.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`timestamp_min`<br/>int</th>
            <td>
                Unix timestamp in minutes of the firmware execution.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `info/version` {#info_version_wem}


Version of the currently running firmware.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/info/version
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/version
```
        </TabItem>
    </Tabs>

```jsx
{ "firmware": "2.1.0+6655c859", "config": "2.1.0", "config_type": "wem" }
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`firmware`<br/>string</th>
    <td>
        The firmware version currently running. See [Semantic Versioning](https://semver.org/) format.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>string</th>
    <td>
        The version of the configuration currently in use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config_type`<br/>string</th>
    <td>
        Type of configuration stored on this device.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/modules` {#info_modules_wem}


Initialization state of firmware modules.



<br/><br/><br/>
## `info/features` {#info_features_wem}


Supported hardware features. See <a class="scrollto" href="#features_section">Features</a> for details.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/info/features
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/features
```
        </TabItem>
    </Tabs>

```jsx
["ethernet", "meters", "energy_manager", "rtc"]
```


:::



<br/><br/><br/>
## `info/name` {#info_name_wem}


Name and type of the device
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/info/name
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/name
```
        </TabItem>
    </Tabs>

```jsx
{
  "name": "wem-AbCd",
  "type": "wem",
  "display_type": "WARP Energy Manager",
  "uid": "AbCd"
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        The name of the device. Consists of the type and UID of the device.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`type`<br/>string</th>
    <td>
        Type of the device.
        - **warp** - A WARP Charger
        - **warp2** - A WARP2 Charger
        - **warp3** - A WARP3 Charger
        - **wem** - A WARP Energy Manager
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_type`<br/>string</th>
    <td>
        User-readable type of the device. Indicates the hardware version, variant (Smart, Pro), maximum charging power, and any upgrades, e.g., "WARP2 Charger Pro 11kW +NFC"
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`uid`<br/>string</th>
    <td>
        UID of the device or the installed ESP (Ethernet) Brick.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/display_name` {#info_display_name_wem}


User-readable name of the device.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```

#### Read
```bash
curl http://$HOST/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
```bash
curl http://$HOST/info/display_name -d '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
curl http://$HOST/info/display_name -d '"Wallbox Garage links"'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
            With MQTT on <code>$PREFIX/info/display_name<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '"Wallbox Garage links"'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_name`<br/>string</th>
    <td>
        The display name.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/last_boots` {#info_last_boots_wem}


Debug information about the last firmware executions
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/info/last_boots
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/last_boots
```
        </TabItem>
    </Tabs>

```jsx
[
  {
    "reset_reason": 3,
    "boot_count": 1,
    "timestamp_min": 28568911,
    "uptime": 1648057,
    "uptime_overflows": 0
  },
  // An entry is only recorded after 5 minutes of uptime.
  // Between boot 1 and 4, the device was restarted twice within 5 minutes (e.g. due to configuration changes)
  {
    "reset_reason": 3,
    "boot_count": 4,
    "timestamp_min": 28568946,
    "uptime": 28301027,
    "uptime_overflows": 0
  }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..9]`<br/>object</th>
    <td>
        Debug information about a firmware execution
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`reset_reason`<br/>int</th>
            <td>
                Reason for the restart. See [Espressif documentation](https://docs.espressif.com/projects/esp-idf/en/v4.4.3/esp32/api-reference/system/system.html?highlight=esp_reset_reason#_CPPv418esp_reset_reason_t).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`boot_count`<br/>int</th>
            <td>
                Counter indicating which restart since the device was disconnected from power is being performed. An entry in last_boots is only written five minutes after device startup to preserve flash memory. If a restart occurs within the first five minutes, an entry may be lost, which can be noticed by this counter.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Runtime of the firmware execution (32-bit).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime_overflows`<br/>int</th>
            <td>
                Number of uptime overflows.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`timestamp_min`<br/>int</th>
            <td>
                Unix timestamp in minutes of the firmware execution.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `info/version` {#info_version_wem2}


Version of the currently running firmware.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`firmware`<br/>string</th>
    <td>
        The firmware version currently running. See [Semantic Versioning](https://semver.org/) format.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>string</th>
    <td>
        The version of the configuration currently in use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config_type`<br/>string</th>
    <td>
        Type of configuration stored on this device.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/modules` {#info_modules_wem2}


Initialization state of firmware modules.



<br/><br/><br/>
## `info/features` {#info_features_wem2}


Supported hardware features. See <a class="scrollto" href="#features_section">Features</a> for details.



<br/><br/><br/>
## `info/name` {#info_name_wem2}


Name and type of the device


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        The name of the device. Consists of the type and UID of the device.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`type`<br/>string</th>
    <td>
        Type of the device.
        - **warp** - A WARP Charger
        - **warp2** - A WARP2 Charger
        - **warp3** - A WARP3 Charger
        - **wem** - A WARP Energy Manager
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_type`<br/>string</th>
    <td>
        User-readable type of the device. Indicates the hardware version, variant (Smart, Pro), maximum charging power, and any upgrades, e.g., "WARP2 Charger Pro 11kW +NFC"
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`uid`<br/>string</th>
    <td>
        UID of the device or the installed ESP (Ethernet) Brick.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/display_name` {#info_display_name_wem2}


User-readable name of the device.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```

#### Read
```bash
curl http://$HOST/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
```bash
curl http://$HOST/info/display_name -d '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
curl http://$HOST/info/display_name -d '"Wallbox Garage links"'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
            With MQTT on <code>$PREFIX/info/display_name<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '"Wallbox Garage links"'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_name`<br/>string</th>
    <td>
        The display name.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/last_boots` {#info_last_boots_wem2}


Debug information about the last firmware executions
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Read
```bash
curl http://$HOST/info/last_boots
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/last_boots
```
        </TabItem>
    </Tabs>

```jsx
[
  {
    "reset_reason": 3,
    "boot_count": 1,
    "timestamp_min": 28568911,
    "uptime": 1648057,
    "uptime_overflows": 0
  },
  // An entry is only recorded after 5 minutes of uptime.
  // Between boot 1 and 4, the device was restarted twice within 5 minutes (e.g. due to configuration changes)
  {
    "reset_reason": 3,
    "boot_count": 4,
    "timestamp_min": 28568946,
    "uptime": 28301027,
    "uptime_overflows": 0
  }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..9]`<br/>object</th>
    <td>
        Debug information about a firmware execution
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`reset_reason`<br/>int</th>
            <td>
                Reason for the restart. See [Espressif documentation](https://docs.espressif.com/projects/esp-idf/en/v4.4.3/esp32/api-reference/system/system.html?highlight=esp_reset_reason#_CPPv418esp_reset_reason_t).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`boot_count`<br/>int</th>
            <td>
                Counter indicating which restart since the device was disconnected from power is being performed. An entry in last_boots is only written five minutes after device startup to preserve flash memory. If a restart occurs within the first five minutes, an entry may be lost, which can be noticed by this counter.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Runtime of the firmware execution (32-bit).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime_overflows`<br/>int</th>
            <td>
                Number of uptime overflows.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`timestamp_min`<br/>int</th>
            <td>
                Unix timestamp in minutes of the firmware execution.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `info/version` {#info_version_any}


Version of the currently running firmware.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`firmware`<br/>string</th>
    <td>
        The firmware version currently running. See [Semantic Versioning](https://semver.org/) format.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>string</th>
    <td>
        The version of the configuration currently in use.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config_type`<br/>string</th>
    <td>
        Type of configuration stored on this device.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/modules` {#info_modules_any}


Initialization state of firmware modules.



<br/><br/><br/>
## `info/features` {#info_features_any}


Supported hardware features. See <a class="scrollto" href="#features_section">Features</a> for details.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/info/features
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/features
```
        </TabItem>
    </Tabs>

```jsx
["evse", "nfc", "meters", "meter", "meter_all_values"]
```


:::



<br/><br/><br/>
## `info/name` {#info_name_any}


Name and type of the device
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/info/name
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/name
```
        </TabItem>
    </Tabs>

```jsx
{
  "name": "warp-AbCd",
  "type": "warp",
  "display_type": "WARP Charger Pro 22kW +NFC",
  "uid": "AbCd"
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
    <td>
        The name of the device. Consists of the type and UID of the device.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`type`<br/>string</th>
    <td>
        Type of the device.
        
        
        
        - **warp** - A WARP Charger
        - **warp2** - A WARP2 Charger
        - **warp3** - A WARP3 Charger
        - **wem** - A WARP Energy Manager
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_type`<br/>string</th>
    <td>
        User-readable type of the device. Indicates the hardware version, variant (Smart, Pro), maximum charging power, and any upgrades, e.g., "WARP2 Charger Pro 11kW +NFC"
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`uid`<br/>string</th>
    <td>
        UID of the device or the installed ESP (Ethernet) Brick.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/display_name` {#info_display_name_any}


User-readable name of the device.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
```bash
curl http://$HOST/info/display_name -d '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
curl http://$HOST/info/display_name -d '"Wallbox Garage links"'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/display_name
```
```jsx
{ "display_name": "Wallbox Garage links" }
```



#### Write
            With MQTT on <code>$PREFIX/info/display_name<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '{ "display_name": "Wallbox Garage links" }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/info/display_name_update -m '"Wallbox Garage links"'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`display_name`<br/>string</th>
    <td>
        The display name.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `info/last_boots` {#info_last_boots_any}


Debug information about the last firmware executions
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/info/last_boots
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/info/last_boots
```
        </TabItem>
    </Tabs>

```jsx
[
  {
    "reset_reason": 3,
    "boot_count": 1,
    "timestamp_min": 28568911,
    "uptime": 1648057,
    "uptime_overflows": 0
  },
  // An entry is only recorded after 5 minutes of uptime.
  // Between boot 1 and 4, the device was restarted twice within 5 minutes (e.g. due to configuration changes)
  {
    "reset_reason": 3,
    "boot_count": 4,
    "timestamp_min": 28568946,
    "uptime": 28301027,
    "uptime_overflows": 0
  }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..9]`<br/>object</th>
    <td>
        Debug information about a firmware execution
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`reset_reason`<br/>int</th>
            <td>
                Reason for the restart. See [Espressif documentation](https://docs.espressif.com/projects/esp-idf/en/v4.4.3/esp32/api-reference/system/system.html?highlight=esp_reset_reason#_CPPv418esp_reset_reason_t).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`boot_count`<br/>int</th>
            <td>
                Counter indicating which restart since the device was disconnected from power is being performed. An entry in last_boots is only written five minutes after device startup to preserve flash memory. If a restart occurs within the first five minutes, an entry may be lost, which can be noticed by this counter.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Runtime of the firmware execution (32-bit).
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`uptime_overflows`<br/>int</th>
            <td>
                Number of uptime overflows.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`timestamp_min`<br/>int</th>
            <td>
                Unix timestamp in minutes of the firmware execution.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "info/version",
    "id": "info_version",
    "level": 2,
    
},{
    "value": "info/modules",
    "id": "info_modules",
    "level": 2,
    
},{
    "value": "info/features",
    "id": "info_features",
    "level": 2,
    
},{
    "value": "info/name",
    "id": "info_name",
    "level": 2,
    
},{
    "value": "info/display_name",
    "id": "info_display_name",
    "level": 2,
    
},{
    "value": "info/last_boots",
    "id": "info_last_boots",
    "level": 2,
    
},
];
