---
description: NFC Charge Authorization
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/nfc.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# NFC Charge Authorization
**Requires the <a href="#features_nfc"><code>"nfc"</code></a> feature.**

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `nfc/seen_tags` {#nfc_seen_tags_warp1}


The NFC tags most recently seen by the wallbox.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/nfc/seen_tags
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/nfc/seen_tags
```
        </TabItem>
    </Tabs>

```jsx
[
  { "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE", "last_seen": 22724083 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "01:23:AB:3D", "last_seen": 10753 }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..7]`<br/>object</th>
    <td>
        A seen NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`last_seen`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time in milliseconds since the tag was last seen.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[8]`<br/>object</th>
    <td>
        The tag spoofed by [`nfc/inject_tag`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag)
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`last_seen`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time in milliseconds since the tag was last seen.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag` {#nfc_inject_tag_warp1}


Pretends that a tag was detected by the NFC reader. This can be used to start or stop a charging session for a specific user via the API. Analogous to the physical use of a tag, the charging session is alternately authorized or blocked with each API call. See [`nfc/inject_tag_start`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag_start) and [`nfc/inject_tag_stop`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag_stop) for more precise control. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag_start` {#nfc_inject_tag_start_warp1}


Pretends that a tag was detected by the NFC reader. The tag is only used to <strong>start</strong> a charging session. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag_start -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag_start -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag_stop` {#nfc_inject_tag_stop_warp1}


Pretends that a tag was detected by the NFC reader. The tag is only used to <strong>stop</strong> a charging session. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag_stop -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag_stop -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/config` {#nfc_config_warp1}


The NFC configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/nfc/config
```
```jsx
{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}
```



#### Write
```bash
curl http://$HOST/nfc/config -d '{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/nfc/config
```
```jsx
{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/nfc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/config_update -m '{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`deadtime_post_start`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Allows stopping a charging session with an NFC tag only after this time to avoid accidental operation.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`authorized_tags`<br/>object[16]</th>
    <td>
        A list of authorized tags.
        - **[0..15]** - An authorized NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID of the user this tag is assigned to, or 0 if it is not assigned to any user.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag.
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `nfc/seen_tags` {#nfc_seen_tags_warp2}


The NFC tags most recently seen by the wallbox.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/nfc/seen_tags
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/nfc/seen_tags
```
        </TabItem>
    </Tabs>

```jsx
[
  { "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE", "last_seen": 22724083 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "01:23:AB:3D", "last_seen": 10753 }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..7]`<br/>object</th>
    <td>
        A seen NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`last_seen`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time in milliseconds since the tag was last seen.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[8]`<br/>object</th>
    <td>
        The tag spoofed by [`nfc/inject_tag`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag)
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`last_seen`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time in milliseconds since the tag was last seen.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag` {#nfc_inject_tag_warp2}


Pretends that a tag was detected by the NFC reader. This can be used to start or stop a charging session for a specific user via the API. Analogous to the physical use of a tag, the charging session is alternately authorized or blocked with each API call. See [`nfc/inject_tag_start`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag_start) and [`nfc/inject_tag_stop`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag_stop) for more precise control. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag_start` {#nfc_inject_tag_start_warp2}


Pretends that a tag was detected by the NFC reader. The tag is only used to <strong>start</strong> a charging session. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag_start -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag_start -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag_stop` {#nfc_inject_tag_stop_warp2}


Pretends that a tag was detected by the NFC reader. The tag is only used to <strong>stop</strong> a charging session. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag_stop -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag_stop -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/config` {#nfc_config_warp2}


The NFC configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/nfc/config
```
```jsx
{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}
```



#### Write
```bash
curl http://$HOST/nfc/config -d '{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/nfc/config
```
```jsx
{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/nfc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/config_update -m '{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`deadtime_post_start`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Allows stopping a charging session with an NFC tag only after this time to avoid accidental operation.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`authorized_tags`<br/>object[32]</th>
    <td>
        A list of authorized tags.
        - **[0..31]** - An authorized NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID of the user this tag is assigned to, or 0 if it is not assigned to any user.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag.
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `nfc/seen_tags` {#nfc_seen_tags_warp3}


The NFC tags most recently seen by the wallbox.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/nfc/seen_tags
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/nfc/seen_tags
```
        </TabItem>
    </Tabs>

```jsx
[
  { "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE", "last_seen": 22724083 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "01:23:AB:3D", "last_seen": 10753 }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..7]`<br/>object</th>
    <td>
        A seen NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`last_seen`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time in milliseconds since the tag was last seen.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[8]`<br/>object</th>
    <td>
        The tag spoofed by [`nfc/inject_tag`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag)
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`last_seen`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time in milliseconds since the tag was last seen.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag` {#nfc_inject_tag_warp3}


Pretends that a tag was detected by the NFC reader. This can be used to start or stop a charging session for a specific user via the API. Analogous to the physical use of a tag, the charging session is alternately authorized or blocked with each API call. See [`nfc/inject_tag_start`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag_start) and [`nfc/inject_tag_stop`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag_stop) for more precise control. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag_start` {#nfc_inject_tag_start_warp3}


Pretends that a tag was detected by the NFC reader. The tag is only used to <strong>start</strong> a charging session. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag_start -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag_start -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag_stop` {#nfc_inject_tag_stop_warp3}


Pretends that a tag was detected by the NFC reader. The tag is only used to <strong>stop</strong> a charging session. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag_stop -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag_stop -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/config` {#nfc_config_warp3}


The NFC configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/nfc/config
```
```jsx
{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}
```



#### Write
```bash
curl http://$HOST/nfc/config -d '{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/nfc/config
```
```jsx
{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/nfc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/config_update -m '{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`deadtime_post_start`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Allows stopping a charging session with an NFC tag only after this time to avoid accidental operation.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`authorized_tags`<br/>object[32]</th>
    <td>
        A list of authorized tags.
        - **[0..31]** - An authorized NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID of the user this tag is assigned to, or 0 if it is not assigned to any user.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag.
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
Not supported on this hardware!
</TabItem>
<TabItem value="wem2">
Not supported on this hardware!
</TabItem>
<TabItem value="any">
## `nfc/seen_tags` {#nfc_seen_tags_any}


The NFC tags most recently seen by the wallbox.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/nfc/seen_tags
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/nfc/seen_tags
```
        </TabItem>
    </Tabs>

```jsx
[
  { "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE", "last_seen": 22724083 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "", "last_seen": 0 },
  { "tag_type": 0, "tag_id": "01:23:AB:3D", "last_seen": 10753 }
]
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Index</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[0..7]`<br/>object</th>
    <td>
        A seen NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag
                
                
                
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`last_seen`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time in milliseconds since the tag was last seen.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`[8]`<br/>object</th>
    <td>
        The tag spoofed by [`nfc/inject_tag`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag)
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag
                
                
                
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`last_seen`<br/>int (<abbr title="Millisecond">ms</abbr>)</th>
            <td>
                Time in milliseconds since the tag was last seen.
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag` {#nfc_inject_tag_any}


Pretends that a tag was detected by the NFC reader. This can be used to start or stop a charging session for a specific user via the API. Analogous to the physical use of a tag, the charging session is alternately authorized or blocked with each API call. See [`nfc/inject_tag_start`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag_start) and [`nfc/inject_tag_stop`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_inject_tag_stop) for more precise control. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        
        
        
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag_start` {#nfc_inject_tag_start_any}


Pretends that a tag was detected by the NFC reader. The tag is only used to <strong>start</strong> a charging session. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag_start -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag_start -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        
        
        
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/inject_tag_stop` {#nfc_inject_tag_stop_any}


Pretends that a tag was detected by the NFC reader. The tag is only used to <strong>stop</strong> a charging session. The spoofed tag is always the last entry in [`nfc/seen_tags`](/interfaces/mqtt_http/api_reference/nfc.mdx#nfc_seen_tags)
<Tabs groupId="apiType" queryString className="hidden-tabs"><TabItem value="http"></TabItem><TabItem value="mqtt"><Admonition type="warning" title="Triggers a one-time action. Messages retained via the broker are ignored."></Admonition></TabItem></Tabs>
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/nfc/inject_tag_stop -d '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/inject_tag_stop -m '{ "tag_type": 0, "tag_id": "01:23:AB:3D" }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
    <td>
        Type of the tag.
        
        
        
        - **0** - Mifare Classic
        - **1** - NFC Forum Type 1
        - **2** - NFC Forum Type 2
        - **3** - NFC Forum Type 3
        - **4** - NFC Forum Type 4
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
    <td>
        ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `nfc/config` {#nfc_config_any}


The NFC configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/nfc/config
```
```jsx
{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}
```



#### Write
```bash
curl http://$HOST/nfc/config -d '{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/nfc/config
```
```jsx
{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/nfc/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/nfc/config_update -m '{
  "authorized_tags": [
    { "user_id": 1, "tag_type": 2, "tag_id": "04:E3:B2:27:39:AD:FE" },
    { "user_id": 2, "tag_type": 0, "tag_id": "01:23:AB:3D" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`deadtime_post_start`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Allows stopping a charging session with an NFC tag only after this time to avoid accidental operation.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`authorized_tags`<br/>object[..]</th>
    <td>
        A list of authorized tags.
        
        <strong>(Only WARP 1)</strong> 
        
        - **[0..15]** - An authorized NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID of the user this tag is assigned to, or 0 if it is not assigned to any user.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag.
                
                
                
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
            </tbody>
        </table>
        
        <strong>(Only WARP 2 and WARP 3)</strong> 
        
        - **[0..31]** - An authorized NFC tag
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`user_id`<br/>int</th>
            <td>
                ID of the user this tag is assigned to, or 0 if it is not assigned to any user.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_type`<br/>int</th>
            <td>
                Type of the tag.
                
                
                
                - **0** - Mifare Classic
                - **1** - NFC Forum Type 1
                - **2** - NFC Forum Type 2
                - **3** - NFC Forum Type 3
                - **4** - NFC Forum Type 4
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`tag_id`<br/>string</th>
            <td>
                ID of the tag. Depending on tag type, up to 10 hex bytes separated by ':'. e.g. `01:23:AB:3D`
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "nfc/seen_tags",
    "id": "nfc_seen_tags",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "nfc/inject_tag",
    "id": "nfc_inject_tag",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "nfc/inject_tag_start",
    "id": "nfc_inject_tag_start",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "nfc/inject_tag_stop",
    "id": "nfc_inject_tag_stop",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},{
    "value": "nfc/config",
    "id": "nfc_config",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},
];
