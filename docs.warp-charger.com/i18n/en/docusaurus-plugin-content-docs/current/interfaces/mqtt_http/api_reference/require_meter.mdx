---
description: Meter Monitoring
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/require_meter.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Meter Monitoring

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `require_meter/config` {#require_meter_config_warp1}


The meter monitoring configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Write
```bash
curl http://$HOST/require_meter/config -d '{ "config": 2 }'
```

or abbreviated:

```bash
curl http://$HOST/require_meter/config -d '2'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Write
            With MQTT on <code>$PREFIX/require_meter/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '{ "config": 2 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '2'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>int</th>
    <td>
        Meter monitoring configuration.
        - **0** - No electricity meter has been detected yet. Meter monitoring disabled. As soon as a meter is detected for the first time, this value will automatically be changed to 2.
        - **1** - An electricity meter has been detected before. Meter monitoring disabled.
        - **2** - An electricity meter has been detected before. Meter monitoring enabled.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `require_meter/config` {#require_meter_config_warp2}


The meter monitoring configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Write
```bash
curl http://$HOST/require_meter/config -d '{ "config": 2 }'
```

or abbreviated:

```bash
curl http://$HOST/require_meter/config -d '2'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Write
            With MQTT on <code>$PREFIX/require_meter/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '{ "config": 2 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '2'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>int</th>
    <td>
        Meter monitoring configuration.
        - **0** - No electricity meter has been detected yet. Meter monitoring disabled. As soon as a meter is detected for the first time, this value will automatically be changed to 2.
        - **1** - An electricity meter has been detected before. Meter monitoring disabled.
        - **2** - An electricity meter has been detected before. Meter monitoring enabled.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `require_meter/config` {#require_meter_config_warp3}


The meter monitoring configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Write
```bash
curl http://$HOST/require_meter/config -d '{ "config": 2 }'
```

or abbreviated:

```bash
curl http://$HOST/require_meter/config -d '2'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Write
            With MQTT on <code>$PREFIX/require_meter/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '{ "config": 2 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '2'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>int</th>
    <td>
        Meter monitoring configuration.
        - **0** - No electricity meter has been detected yet. Meter monitoring disabled. As soon as a meter is detected for the first time, this value will automatically be changed to 2.
        - **1** - An electricity meter has been detected before. Meter monitoring disabled.
        - **2** - An electricity meter has been detected before. Meter monitoring enabled.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
Not supported on this hardware!
</TabItem>
<TabItem value="wem2">
Not supported on this hardware!
</TabItem>
<TabItem value="any">
## `require_meter/config` {#require_meter_config_any}


The meter monitoring configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Write
```bash
curl http://$HOST/require_meter/config -d '{ "config": 2 }'
```

or abbreviated:

```bash
curl http://$HOST/require_meter/config -d '2'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/require_meter/config
```
```jsx
{ "config": 2 }
```



#### Write
            With MQTT on <code>$PREFIX/require_meter/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '{ "config": 2 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/require_meter/config_update -m '2'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`config`<br/>int</th>
    <td>
        Meter monitoring configuration.
        
        
        
        - **0** - No electricity meter has been detected yet. Meter monitoring disabled. As soon as a meter is detected for the first time, this value will automatically be changed to 2.
        - **1** - An electricity meter has been detected before. Meter monitoring disabled.
        - **2** - An electricity meter has been detected before. Meter monitoring enabled.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "require_meter/config",
    "id": "require_meter_config",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},
];
