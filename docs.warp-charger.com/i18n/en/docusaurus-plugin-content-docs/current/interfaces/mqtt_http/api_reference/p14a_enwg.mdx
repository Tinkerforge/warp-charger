---
description: §14a EnWG
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/p14a_enwg.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# §14a EnWG
The `p14a_enwg` module allows configuration of settings for control according to §14a of the German Energy Industry Act (EnWG). §14a EnWG allows grid operators to temporarily reduce the power consumption of controllable consumer devices (wallboxes, heat pumps, battery storage, air conditioning) to prevent local grid overload. The devices are never fully shut off. A minimum of 4200 W always remains available.
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
Not supported on this hardware!
</TabItem>
<TabItem value="warp2">
## `p14a_enwg/config` {#p14a_enwg_config_warp2}


The §14a EnWG control configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}
```



#### Write
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}
```



#### Write
            With MQTT on <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Specifies whether §14a EnWG control should be enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signal source for §14a EnWG control.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Input">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Specifies whether §14a control is active when the input is closed or open.
                - **true** - Active when closed
                - **false** - Active when open
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Power limit in watts applied when triggered via the input.
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charger`<br/>bool</th>
    <td>
        <strong>(Only WARP 2 and WARP 3)</strong> Specifies whether the §14a power limit should be applied to this charger.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the §14a power limit should be applied to managed chargers.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_warp2}


The state of the §14a EnWG control.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is currently active, i.e. whether a power limitation is in effect.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        The current effective power limit. Is 0 when §14a control is not active.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_warp2}


The control state for the API source. Resets to inactive on reboot. Values are only considered when the signal source is set to API (2).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is active via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts set via the API.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_warp2}


Sets the §14a control state via the API. Only works when the signal source is set to API (2). The state is volatile and resets to inactive on reboot.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Write
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Activates or deactivates §14a control via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `p14a_enwg/config` {#p14a_enwg_config_warp3}


The §14a EnWG control configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}
```



#### Write
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}
```



#### Write
            With MQTT on <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200}],
    "limit_charger": true,
    "limit_charge_manager": false
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Specifies whether §14a EnWG control should be enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signal source for §14a EnWG control.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Input">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Specifies whether §14a control is active when the input is closed or open.
                - **true** - Active when closed
                - **false** - Active when open
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Power limit in watts applied when triggered via the input.
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charger`<br/>bool</th>
    <td>
        <strong>(Only WARP 2 and WARP 3)</strong> Specifies whether the §14a power limit should be applied to this charger.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the §14a power limit should be applied to managed chargers.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_warp3}


The state of the §14a EnWG control.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is currently active, i.e. whether a power limitation is in effect.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        The current effective power limit. Is 0 when §14a control is not active.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_warp3}


The control state for the API source. Resets to inactive on reboot. Values are only considered when the signal source is set to API (2).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is active via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts set via the API.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_warp3}


Sets the §14a control state via the API. Only works when the signal source is set to API (2). The state is volatile and resets to inactive on reboot.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Write
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Activates or deactivates §14a control via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `p14a_enwg/config` {#p14a_enwg_config_wem}


The §14a EnWG control configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```

#### Read
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false
}
```



#### Write
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false
}
```



#### Write
            With MQTT on <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Specifies whether §14a EnWG control should be enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signal source for §14a EnWG control.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Input">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Specifies whether §14a control is active when the input is closed or open.
                - **true** - Active when closed
                - **false** - Active when open
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Power limit in watts applied when triggered via the input.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`input_index`<br/>int</th>
            <td>
                <strong>(Only Energy Manager and Energy Manager 2.0)</strong> Specifies which Energy Manager input is used for §14a control.
                - **0** - Input 1
                - **1** - Input 2
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the §14a power limit should be applied to managed chargers.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_wem}


The state of the §14a EnWG control.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is currently active, i.e. whether a power limitation is in effect.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        The current effective power limit. Is 0 when §14a control is not active.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_wem}


The control state for the API source. Resets to inactive on reboot. Values are only considered when the signal source is set to API (2).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is active via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts set via the API.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_wem}


Sets the §14a control state via the API. Only works when the signal source is set to API (2). The state is volatile and resets to inactive on reboot.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Write
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Activates or deactivates §14a control via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `p14a_enwg/config` {#p14a_enwg_config_wem2}


The §14a EnWG control configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```

#### Read
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}
```



#### Write
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}
```



#### Write
            With MQTT on <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Specifies whether §14a EnWG control should be enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signal source for §14a EnWG control.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Input">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Specifies whether §14a control is active when the input is closed or open.
                - **true** - Active when closed
                - **false** - Active when open
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Power limit in watts applied when triggered via the input.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`input_index`<br/>int</th>
            <td>
                <strong>(Only Energy Manager and Energy Manager 2.0)</strong> Specifies which Energy Manager input is used for §14a control.
                - **2** - Input 3
                - **3** - Input 4
                - **0** - Input 1
                - **1** - Input 2
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the §14a power limit should be applied to managed chargers.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_heating`<br/>bool</th>
    <td>
        <strong>(Only Energy Manager 2.0)</strong> Specifies whether the §14a power limit should be applied to heating control.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`heating_max_power`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        <strong>(Only Energy Manager 2.0)</strong> Maximum power draw of the heating system.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_wem2}


The state of the §14a EnWG control.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is currently active, i.e. whether a power limitation is in effect.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        The current effective power limit. Is 0 when §14a control is not active.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_wem2}


The control state for the API source. Resets to inactive on reboot. Values are only considered when the signal source is set to API (2).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is active via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts set via the API.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_wem2}


Sets the §14a control state via the API. Only works when the signal source is set to API (2). The state is volatile and resets to inactive on reboot.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Write
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Activates or deactivates §14a control via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `p14a_enwg/config` {#p14a_enwg_config_any}


The §14a EnWG control configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charger": true,
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}
```



#### Write
```bash
curl http://$HOST/p14a_enwg/config -d '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charger": true,
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/config
```
```jsx
{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charger": true,
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}
```



#### Write
            With MQTT on <code>$PREFIX/p14a_enwg/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/config_update -m '{
    "enable": true,
    "source": [0, {"limit_on_close": true, "limit_w": 4200, "input_index": 0}],
    "limit_charger": true,
    "limit_charge_manager": false,
    "limit_heating": false,
    "heating_max_power": 0
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Specifies whether §14a EnWG control should be enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`source`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)</th>
    <td>
        Signal source for §14a EnWG control.
        <MultilineTabs className="unique-tabs">
        <TabItem value="0" label="0 - Input">
        
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_on_close`<br/>bool</th>
            <td>
                Specifies whether §14a control is active when the input is closed or open.
                
                
                
                - **true** - Active when closed
                - **false** - Active when open
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
            <td>
                Power limit in watts applied when triggered via the input.
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`input_index`<br/>int</th>
            <td>
                <strong>(Only Energy Manager and Energy Manager 2.0)</strong> Specifies which Energy Manager input is used for §14a control.
                
                <strong>(Only Energy Manager 2.0)</strong> 
                
                - **2** - Input 3
                - **3** - Input 4
                
                <strong>(Only Energy Manager and Energy Manager 2.0)</strong> 
                
                - **0** - Input 1
                - **1** - Input 2
            </td>
        </tr>
            </tbody>
        </table>
        </TabItem>
        
        <TabItem value="1" label="1 - EEBUS">
        null: EEBUS
        </TabItem>
        
        <TabItem value="2" label="2 - API">
        null: API
        </TabItem>
        </MultilineTabs>
        
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charger`<br/>bool</th>
    <td>
        <strong>(Only WARP 2 and WARP 3)</strong> Specifies whether the §14a power limit should be applied to this charger.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the §14a power limit should be applied to managed chargers.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_heating`<br/>bool</th>
    <td>
        <strong>(Only Energy Manager 2.0)</strong> Specifies whether the §14a power limit should be applied to heating control.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`heating_max_power`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        <strong>(Only Energy Manager 2.0)</strong> Maximum power draw of the heating system.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/state` {#p14a_enwg_state_any}


The state of the §14a EnWG control.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": true,
    "limit_w": 4200
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is currently active, i.e. whether a power limitation is in effect.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        The current effective power limit. Is 0 when §14a control is not active.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control` {#p14a_enwg_control_any}


The control state for the API source. Resets to inactive on reboot. Values are only considered when the signal source is set to API (2).
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/p14a_enwg/control
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/p14a_enwg/control
```
        </TabItem>
    </Tabs>

```jsx
{
    "active": false,
    "limit_w": 0
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Indicates whether §14a control is active via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts set via the API.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `p14a_enwg/control_update` {#p14a_enwg_control_update_any}


Sets the §14a control state via the API. Only works when the signal source is set to API (2). The state is volatile and resets to inactive on reboot.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Write
```bash
curl http://$HOST/p14a_enwg/control_update -d '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Write
```bash
mosquitto_pub -h $BROKER -t $PREFIX/p14a_enwg/control_update -m '{
    "active": true,
    "limit_w": 4200
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`active`<br/>bool</th>
    <td>
        Activates or deactivates §14a control via the API.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`limit_w`<br/>int (<abbr title="Watt">W</abbr>)</th>
    <td>
        Power limit in watts.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "p14a_enwg/config",
    "id": "p14a_enwg_config",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},{
    "value": "p14a_enwg/state",
    "id": "p14a_enwg_state",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},{
    "value": "p14a_enwg/control",
    "id": "p14a_enwg_control",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},{
    "value": "p14a_enwg/control_update",
    "id": "p14a_enwg_control_update",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},
];
