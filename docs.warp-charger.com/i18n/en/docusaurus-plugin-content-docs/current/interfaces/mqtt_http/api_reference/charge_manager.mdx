---
description: Charge Manager
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/charge_manager.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Charge Manager
The `charge_manager` module implements a charge manager that can distribute an available amount of current across one or more WARP Chargers (of any generation). The charge manager ensures that the configured group of WARP Chargers never draws more than the available current and attempts to distribute it as fairly as possible. A WARP Energy Manager also uses the charge manager to safely implement phase switching of a group of WARP Chargers via an external contactor.
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `charge_manager/available_current` {#charge_manager_available_current_warp1}


The currently available current. This current is distributed among the configured wallboxes.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
```bash
curl http://$HOST/charge_manager/available_current -d '{ "current": 16000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_manager/available_current -d '16000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/available_current<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '{ "current": 16000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '16000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        The available current. Only currents that are less than the configured maximum current maximum_available_current from [`charge_manager/config`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_config) are accepted.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_manager/state` {#charge_manager_state_warp1}


The state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_state` {#charge_manager_low_level_state_warp1}


The low-level state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_config` {#charge_manager_low_level_config_warp1}


The low-level configuration of the charge manager. Used for fine-tuning the distribution algorithm. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/config` {#charge_manager_config_warp1}


The charge manager configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
```bash
curl http://$HOST/charge_manager/config -d '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/config_update -m '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the charge manager should be enabled.
        - **true** - If the charge manager is enabled.
        - **false** - If the charge manager is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_watchdog`<br/>bool</th>
    <td>
        Specifies whether the watchdog should be enabled. The watchdog sets the available current to the default setting (default_available_current) if no message is received on [`charge_manager/available_current`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_available_current) for 30 seconds. This increases robustness against failure of external control, e.g. with PV excess charging.
        - **true** - If the watchdog is enabled.
        - **false** - If the watchdog is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`verbose`<br/>bool</th>
    <td>
        Specifies whether each current distribution should be recorded in the event log.
        - **true** - If current distributions should be logged.
        - **false** - If current distributions should not be logged.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`default_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Current that should be available after restart of the charge manager. When triggered, the watchdog resets the available current to this current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`maximum_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Maximum that may be set as available current via the API and web interface. Should be configured to the maximum permitted current of the wallbox group connection, which is limited e.g. by the grid connection, fusing or supply line.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_auto`<br/>bool</th>
    <td>
        Specifies whether the minimum charge current is set based on the selected vehicle type (minimum_current_vehicle_type).
        - **true** - Minimum charge current is set based on the selected vehicle type. minimum_current and minimum_current_1p are ignored or overwritten.
        - **false** - minimum_current and minimum_current_1p determine the minimum charge current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_vehicle_type`<br/>int</th>
    <td>
        Vehicle type for which the minimum charge current is selected.
        - **0** - No specific vehicle type selected.
        - **1** - Renault Twingo Z.E., Renault ZOE R110 or R135
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in three-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_1p`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in single-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_threshold`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Wallboxes with an electricity meter that measures phase currents are limited to the largest phase current plus the configured margin requested_current_threshold seconds after charge start. This allows the available current to be distributed more efficiently across multiple wallboxes.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_margin`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Margin added to the largest measured phase current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`chargers`<br/>object[10]</th>
    <td>
        Wallboxes to be controlled by the charge manager.
        - **[0..9]** - A wallbox to be controlled
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`host`<br/>string</th>
            <td>
                IP address of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`rot`<br/>int</th>
            <td>
                Phase rotation of the wallbox to be controlled
                - **0** - Unknown
                - **1** - L123
                - **2** - L132
                - **3** - L231
                - **4** - L213
                - **5** - L321
                - **6** - L312
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `charge_manager/available_current` {#charge_manager_available_current_warp2}


The currently available current. This current is distributed among the configured wallboxes.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
```bash
curl http://$HOST/charge_manager/available_current -d '{ "current": 16000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_manager/available_current -d '16000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/available_current<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '{ "current": 16000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '16000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        The available current. Only currents that are less than the configured maximum current maximum_available_current from [`charge_manager/config`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_config) are accepted.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_manager/state` {#charge_manager_state_warp2}


The state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_state` {#charge_manager_low_level_state_warp2}


The low-level state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_config` {#charge_manager_low_level_config_warp2}


The low-level configuration of the charge manager. Used for fine-tuning the distribution algorithm. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/config` {#charge_manager_config_warp2}


The charge manager configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp2-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
```bash
curl http://$HOST/charge_manager/config -d '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp2-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp2-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/config_update -m '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp2-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the charge manager should be enabled.
        - **true** - If the charge manager is enabled.
        - **false** - If the charge manager is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_watchdog`<br/>bool</th>
    <td>
        Specifies whether the watchdog should be enabled. The watchdog sets the available current to the default setting (default_available_current) if no message is received on [`charge_manager/available_current`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_available_current) for 30 seconds. This increases robustness against failure of external control, e.g. with PV excess charging.
        - **true** - If the watchdog is enabled.
        - **false** - If the watchdog is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`verbose`<br/>bool</th>
    <td>
        Specifies whether each current distribution should be recorded in the event log.
        - **true** - If current distributions should be logged.
        - **false** - If current distributions should not be logged.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`default_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Current that should be available after restart of the charge manager. When triggered, the watchdog resets the available current to this current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`maximum_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Maximum that may be set as available current via the API and web interface. Should be configured to the maximum permitted current of the wallbox group connection, which is limited e.g. by the grid connection, fusing or supply line.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_auto`<br/>bool</th>
    <td>
        Specifies whether the minimum charge current is set based on the selected vehicle type (minimum_current_vehicle_type).
        - **true** - Minimum charge current is set based on the selected vehicle type. minimum_current and minimum_current_1p are ignored or overwritten.
        - **false** - minimum_current and minimum_current_1p determine the minimum charge current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_vehicle_type`<br/>int</th>
    <td>
        Vehicle type for which the minimum charge current is selected.
        - **0** - No specific vehicle type selected.
        - **1** - Renault Twingo Z.E., Renault ZOE R110 or R135
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in three-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_1p`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in single-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_threshold`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Wallboxes with an electricity meter that measures phase currents are limited to the largest phase current plus the configured margin requested_current_threshold seconds after charge start. This allows the available current to be distributed more efficiently across multiple wallboxes.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_margin`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Margin added to the largest measured phase current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`chargers`<br/>object[64]</th>
    <td>
        Wallboxes to be controlled by the charge manager.
        - **[0..63]** - A wallbox to be controlled
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`host`<br/>string</th>
            <td>
                IP address of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`rot`<br/>int</th>
            <td>
                Phase rotation of the wallbox to be controlled
                - **0** - Unknown
                - **1** - L123
                - **2** - L132
                - **3** - L231
                - **4** - L213
                - **5** - L321
                - **6** - L312
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `charge_manager/available_current` {#charge_manager_available_current_warp3}


The currently available current. This current is distributed among the configured wallboxes.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
```bash
curl http://$HOST/charge_manager/available_current -d '{ "current": 16000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_manager/available_current -d '16000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/available_current<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '{ "current": 16000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '16000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        The available current. Only currents that are less than the configured maximum current maximum_available_current from [`charge_manager/config`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_config) are accepted.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_manager/state` {#charge_manager_state_warp3}


The state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_state` {#charge_manager_low_level_state_warp3}


The low-level state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_config` {#charge_manager_low_level_config_warp3}


The low-level configuration of the charge manager. Used for fine-tuning the distribution algorithm. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/config` {#charge_manager_config_warp3}


The charge manager configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp3-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
```bash
curl http://$HOST/charge_manager/config -d '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp3-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp3-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/config_update -m '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp3-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the charge manager should be enabled.
        - **true** - If the charge manager is enabled.
        - **false** - If the charge manager is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_watchdog`<br/>bool</th>
    <td>
        Specifies whether the watchdog should be enabled. The watchdog sets the available current to the default setting (default_available_current) if no message is received on [`charge_manager/available_current`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_available_current) for 30 seconds. This increases robustness against failure of external control, e.g. with PV excess charging.
        - **true** - If the watchdog is enabled.
        - **false** - If the watchdog is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`verbose`<br/>bool</th>
    <td>
        Specifies whether each current distribution should be recorded in the event log.
        - **true** - If current distributions should be logged.
        - **false** - If current distributions should not be logged.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`default_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Current that should be available after restart of the charge manager. When triggered, the watchdog resets the available current to this current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`maximum_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Maximum that may be set as available current via the API and web interface. Should be configured to the maximum permitted current of the wallbox group connection, which is limited e.g. by the grid connection, fusing or supply line.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_auto`<br/>bool</th>
    <td>
        Specifies whether the minimum charge current is set based on the selected vehicle type (minimum_current_vehicle_type).
        - **true** - Minimum charge current is set based on the selected vehicle type. minimum_current and minimum_current_1p are ignored or overwritten.
        - **false** - minimum_current and minimum_current_1p determine the minimum charge current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_vehicle_type`<br/>int</th>
    <td>
        Vehicle type for which the minimum charge current is selected.
        - **0** - No specific vehicle type selected.
        - **1** - Renault Twingo Z.E., Renault ZOE R110 or R135
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in three-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_1p`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in single-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_threshold`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Wallboxes with an electricity meter that measures phase currents are limited to the largest phase current plus the configured margin requested_current_threshold seconds after charge start. This allows the available current to be distributed more efficiently across multiple wallboxes.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_margin`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Margin added to the largest measured phase current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`chargers`<br/>object[64]</th>
    <td>
        Wallboxes to be controlled by the charge manager.
        - **[0..63]** - A wallbox to be controlled
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`host`<br/>string</th>
            <td>
                IP address of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`rot`<br/>int</th>
            <td>
                Phase rotation of the wallbox to be controlled
                - **0** - Unknown
                - **1** - L123
                - **2** - L132
                - **3** - L231
                - **4** - L213
                - **5** - L321
                - **6** - L312
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `charge_manager/available_current` {#charge_manager_available_current_wem}


The currently available current. This current is distributed among the configured wallboxes.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
```bash
curl http://$HOST/charge_manager/available_current -d '{ "current": 16000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_manager/available_current -d '16000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/available_current<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '{ "current": 16000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '16000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        The available current. Only currents that are less than the configured maximum current maximum_available_current from [`charge_manager/config`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_config) are accepted.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_manager/state` {#charge_manager_state_wem}


The state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_state` {#charge_manager_low_level_state_wem}


The low-level state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_config` {#charge_manager_low_level_config_wem}


The low-level configuration of the charge manager. Used for fine-tuning the distribution algorithm. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/config` {#charge_manager_config_wem}


The charge manager configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "192.168.178.12", "name": "warp-222i" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
```bash
curl http://$HOST/charge_manager/config -d '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "192.168.178.12", "name": "warp-222i" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "192.168.178.12", "name": "warp-222i" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/config_update -m '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "192.168.178.12", "name": "warp-222i" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the charge manager should be enabled.
        - **true** - If the charge manager is enabled.
        - **false** - If the charge manager is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_watchdog`<br/>bool</th>
    <td>
        Specifies whether the watchdog should be enabled. The watchdog sets the available current to the default setting (default_available_current) if no message is received on [`charge_manager/available_current`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_available_current) for 30 seconds. This increases robustness against failure of external control, e.g. with PV excess charging.
        - **true** - If the watchdog is enabled.
        - **false** - If the watchdog is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`verbose`<br/>bool</th>
    <td>
        Specifies whether each current distribution should be recorded in the event log.
        - **true** - If current distributions should be logged.
        - **false** - If current distributions should not be logged.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`default_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Current that should be available after restart of the charge manager. When triggered, the watchdog resets the available current to this current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`maximum_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Maximum that may be set as available current via the API and web interface. Should be configured to the maximum permitted current of the wallbox group connection, which is limited e.g. by the grid connection, fusing or supply line.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_auto`<br/>bool</th>
    <td>
        Specifies whether the minimum charge current is set based on the selected vehicle type (minimum_current_vehicle_type).
        - **true** - Minimum charge current is set based on the selected vehicle type. minimum_current and minimum_current_1p are ignored or overwritten.
        - **false** - minimum_current and minimum_current_1p determine the minimum charge current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_vehicle_type`<br/>int</th>
    <td>
        Vehicle type for which the minimum charge current is selected.
        - **0** - No specific vehicle type selected.
        - **1** - Renault Twingo Z.E., Renault ZOE R110 or R135
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in three-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_1p`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in single-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_threshold`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Wallboxes with an electricity meter that measures phase currents are limited to the largest phase current plus the configured margin requested_current_threshold seconds after charge start. This allows the available current to be distributed more efficiently across multiple wallboxes.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_margin`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Margin added to the largest measured phase current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`chargers`<br/>object[64]</th>
    <td>
        Wallboxes to be controlled by the charge manager.
        - **[0..63]** - A wallbox to be controlled
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`host`<br/>string</th>
            <td>
                IP address of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`rot`<br/>int</th>
            <td>
                Phase rotation of the wallbox to be controlled
                - **0** - Unknown
                - **1** - L123
                - **2** - L132
                - **3** - L231
                - **4** - L213
                - **5** - L321
                - **6** - L312
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `charge_manager/available_current` {#charge_manager_available_current_wem2}


The currently available current. This current is distributed among the configured wallboxes.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
```bash
curl http://$HOST/charge_manager/available_current -d '{ "current": 16000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_manager/available_current -d '16000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/available_current<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '{ "current": 16000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '16000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        The available current. Only currents that are less than the configured maximum current maximum_available_current from [`charge_manager/config`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_config) are accepted.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_manager/state` {#charge_manager_state_wem2}


The state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_state` {#charge_manager_low_level_state_wem2}


The low-level state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_config` {#charge_manager_low_level_config_wem2}


The low-level configuration of the charge manager. Used for fine-tuning the distribution algorithm. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/config` {#charge_manager_config_wem2}


The charge manager configuration.


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the charge manager should be enabled.
        - **true** - If the charge manager is enabled.
        - **false** - If the charge manager is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_watchdog`<br/>bool</th>
    <td>
        Specifies whether the watchdog should be enabled. The watchdog sets the available current to the default setting (default_available_current) if no message is received on [`charge_manager/available_current`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_available_current) for 30 seconds. This increases robustness against failure of external control, e.g. with PV excess charging.
        - **true** - If the watchdog is enabled.
        - **false** - If the watchdog is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`verbose`<br/>bool</th>
    <td>
        Specifies whether each current distribution should be recorded in the event log.
        - **true** - If current distributions should be logged.
        - **false** - If current distributions should not be logged.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`default_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Current that should be available after restart of the charge manager. When triggered, the watchdog resets the available current to this current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`maximum_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Maximum that may be set as available current via the API and web interface. Should be configured to the maximum permitted current of the wallbox group connection, which is limited e.g. by the grid connection, fusing or supply line.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_auto`<br/>bool</th>
    <td>
        Specifies whether the minimum charge current is set based on the selected vehicle type (minimum_current_vehicle_type).
        - **true** - Minimum charge current is set based on the selected vehicle type. minimum_current and minimum_current_1p are ignored or overwritten.
        - **false** - minimum_current and minimum_current_1p determine the minimum charge current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_vehicle_type`<br/>int</th>
    <td>
        Vehicle type for which the minimum charge current is selected.
        - **0** - No specific vehicle type selected.
        - **1** - Renault Twingo Z.E., Renault ZOE R110 or R135
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in three-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_1p`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in single-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_threshold`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Wallboxes with an electricity meter that measures phase currents are limited to the largest phase current plus the configured margin requested_current_threshold seconds after charge start. This allows the available current to be distributed more efficiently across multiple wallboxes.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_margin`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Margin added to the largest measured phase current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`chargers`<br/>object[64]</th>
    <td>
        Wallboxes to be controlled by the charge manager.
        - **[0..63]** - A wallbox to be controlled
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`host`<br/>string</th>
            <td>
                IP address of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`rot`<br/>int</th>
            <td>
                Phase rotation of the wallbox to be controlled
                - **0** - Unknown
                - **1** - L123
                - **2** - L132
                - **3** - L231
                - **4** - L213
                - **5** - L321
                - **6** - L312
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `charge_manager/available_current` {#charge_manager_available_current_any}


The currently available current. This current is distributed among the configured wallboxes.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
```bash
curl http://$HOST/charge_manager/available_current -d '{ "current": 16000 }'
```

or abbreviated:

```bash
curl http://$HOST/charge_manager/available_current -d '16000'
```




        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/available_current
```
```jsx
{ "current": 16000 }
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/available_current<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '{ "current": 16000 }'
```

or abbreviated:

```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/available_current_update -m '16000'
```




        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        The available current. Only currents that are less than the configured maximum current maximum_available_current from [`charge_manager/config`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_config) are accepted.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `charge_manager/state` {#charge_manager_state_any}


The state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_state` {#charge_manager_low_level_state_any}


The low-level state of the charge manager and all configured wallboxes. Used by the web interface for display. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/low_level_config` {#charge_manager_low_level_config_any}


The low-level configuration of the charge manager. Used for fine-tuning the distribution algorithm. <strong>Changes to this object are not considered an API breaking change!</strong>



<br/><br/><br/>
## `charge_manager/config` {#charge_manager_config_any}


The charge manager configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
```bash
curl http://$HOST/charge_manager/config -d '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/charge_manager/config
```
```jsx
{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}
```



#### Write
            With MQTT on <code>$PREFIX/charge_manager/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/charge_manager/config_update -m '{
  "enable_charge_manager": true,
  "enable_watchdog": false,
  "default_available_current": 16000,
  "maximum_available_current": 32000,
  "minimum_current_auto": true,
  "minimum_current": 9200,
  "minimum_current_1p": 6000,
  "minimum_current_vehicle_type": 1,
  "verbose": true,
  "requested_current_threshold": 60,
  "requested_current_margin": 3000,
  "chargers": [
    { "host": "127.0.0.1", "name": "warp-AbCd" },
    { "host": "warp2-22oH.local", "name": "warp2-22oH" }
  ]
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_charge_manager`<br/>bool</th>
    <td>
        Specifies whether the charge manager should be enabled.
        
        
        
        - **true** - If the charge manager is enabled.
        - **false** - If the charge manager is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable_watchdog`<br/>bool</th>
    <td>
        Specifies whether the watchdog should be enabled. The watchdog sets the available current to the default setting (default_available_current) if no message is received on [`charge_manager/available_current`](/interfaces/mqtt_http/api_reference/charge_manager.mdx#charge_manager_available_current) for 30 seconds. This increases robustness against failure of external control, e.g. with PV excess charging.
        
        
        
        - **true** - If the watchdog is enabled.
        - **false** - If the watchdog is not enabled.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`verbose`<br/>bool</th>
    <td>
        Specifies whether each current distribution should be recorded in the event log.
        
        
        
        - **true** - If current distributions should be logged.
        - **false** - If current distributions should not be logged.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`default_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Current that should be available after restart of the charge manager. When triggered, the watchdog resets the available current to this current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`maximum_available_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Maximum that may be set as available current via the API and web interface. Should be configured to the maximum permitted current of the wallbox group connection, which is limited e.g. by the grid connection, fusing or supply line.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_auto`<br/>bool</th>
    <td>
        Specifies whether the minimum charge current is set based on the selected vehicle type (minimum_current_vehicle_type).
        
        
        
        - **true** - Minimum charge current is set based on the selected vehicle type. minimum_current and minimum_current_1p are ignored or overwritten.
        - **false** - minimum_current and minimum_current_1p determine the minimum charge current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_vehicle_type`<br/>int</th>
    <td>
        Vehicle type for which the minimum charge current is selected.
        
        
        
        - **0** - No specific vehicle type selected.
        - **1** - Renault Twingo Z.E., Renault ZOE R110 or R135
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in three-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`minimum_current_1p`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Smallest amount of current to be allocated to a wallbox in single-phase operation for it to start a charging session. This can influence how many wallboxes charge simultaneously.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_threshold`<br/>int (<abbr title="Second">s</abbr>)</th>
    <td>
        Wallboxes with an electricity meter that measures phase currents are limited to the largest phase current plus the configured margin requested_current_threshold seconds after charge start. This allows the available current to be distributed more efficiently across multiple wallboxes.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`requested_current_margin`<br/>int (<abbr title="Milliampere">mA</abbr>)</th>
    <td>
        Margin added to the largest measured phase current.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`chargers`<br/>object[..]</th>
    <td>
        Wallboxes to be controlled by the charge manager.
        
        <strong>(Only WARP 1)</strong> 
        
        - **[0..9]** - A wallbox to be controlled
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`host`<br/>string</th>
            <td>
                IP address of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`rot`<br/>int</th>
            <td>
                Phase rotation of the wallbox to be controlled
                
                
                
                - **0** - Unknown
                - **1** - L123
                - **2** - L132
                - **3** - L231
                - **4** - L213
                - **5** - L321
                - **6** - L312
            </td>
        </tr>
            </tbody>
        </table>
        
        <strong>(Only WARP 2 and WARP 3 and Energy Manager and Energy Manager 2.0)</strong> 
        
        - **[0..63]** - A wallbox to be controlled
        <table>
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Meaning</th>
                </tr>
            </thead>
            <tbody>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`host`<br/>string</th>
            <td>
                IP address of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`name`<br/>string</th>
            <td>
                Display name of the wallbox to be controlled
            </td>
        </tr>
        
        <tr>
            <th scope="row" style={{whiteSpace: "nowrap"}}>`rot`<br/>int</th>
            <td>
                Phase rotation of the wallbox to be controlled
                
                
                
                - **0** - Unknown
                - **1** - L123
                - **2** - L132
                - **3** - L231
                - **4** - L213
                - **5** - L321
                - **6** - L312
            </td>
        </tr>
            </tbody>
        </table>
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "charge_manager/available_current",
    "id": "charge_manager_available_current",
    "level": 2,
    
},{
    "value": "charge_manager/state",
    "id": "charge_manager_state",
    "level": 2,
    
},{
    "value": "charge_manager/low_level_state",
    "id": "charge_manager_low_level_state",
    "level": 2,
    
},{
    "value": "charge_manager/low_level_config",
    "id": "charge_manager_low_level_config",
    "level": 2,
    
},{
    "value": "charge_manager/config",
    "id": "charge_manager_config",
    "level": 2,
    
},
];
