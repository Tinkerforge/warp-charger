---
description: Front Panel
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/front_panel.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Front Panel
The `front_panel` module allows configuration of the display in the front panel.
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
Not supported on this hardware!
</TabItem>
<TabItem value="warp2">
Not supported on this hardware!
</TabItem>
<TabItem value="warp3">
Not supported on this hardware!
</TabItem>
<TabItem value="wem">
Not supported on this hardware!
</TabItem>
<TabItem value="wem2">
## `front_panel/config` {#front_panel_config_wem2}


The front panel configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```

#### Read
```bash
curl http://$HOST/front_panel/config
```
```jsx
{"enable":true,"tiles":[[1,0],[4,0],[7,null],[3,1],[3,0],[6,null]]}
```



#### Write
```bash
curl http://$HOST/front_panel/config -d '{"enable":true,"tiles":[[1,0],[4,0],[7,null],[3,1],[3,0],[6,null]]}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/front_panel/config
```
```jsx
{"enable":true,"tiles":[[1,0],[4,0],[7,null],[3,1],[3,0],[6,null]]}
```



#### Write
            With MQTT on <code>$PREFIX/front_panel/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/front_panel/config_update -m '{"enable":true,"tiles":[[1,0],[4,0],[7,null],[3,1],[3,0],[6,null]]}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether the display should be enabled. If the display is disabled, it cannot be reactivated via the button on the front panel. If the display is enabled, it will turn off after 60 minutes to save power, but can be reactivated by pressing the button.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tiles`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)[6]</th>
    <td>
        Specifies what each tile should display in the order: top left, middle left, bottom left, top right, middle right, bottom right.
        - **[0..5]** - Tile configuration
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `front_panel/config` {#front_panel_config_any}


The front panel configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/front_panel/config
```
```jsx
{"enable":true,"tiles":[[1,0],[4,0],[7,null],[3,1],[3,0],[6,null]]}
```



#### Write
```bash
curl http://$HOST/front_panel/config -d '{"enable":true,"tiles":[[1,0],[4,0],[7,null],[3,1],[3,0],[6,null]]}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/front_panel/config
```
```jsx
{"enable":true,"tiles":[[1,0],[4,0],[7,null],[3,1],[3,0],[6,null]]}
```



#### Write
            With MQTT on <code>$PREFIX/front_panel/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/front_panel/config_update -m '{"enable":true,"tiles":[[1,0],[4,0],[7,null],[3,1],[3,0],[6,null]]}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether the display should be enabled. If the display is disabled, it cannot be reactivated via the button on the front panel. If the display is enabled, it will turn off after 60 minutes to save power, but can be reactivated by pressing the button.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`tiles`<br/>[Union](/interfaces/mqtt_http/introduction.md#unions)[6]</th>
    <td>
        Specifies what each tile should display in the order: top left, middle left, bottom left, top right, middle right, bottom right.
        
        
        
        - **[0..5]** - Tile configuration
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "front_panel/config",
    "id": "front_panel_config",
    "level": 2,
    "hardwareType": ["wem2"]
},
];
