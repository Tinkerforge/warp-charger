---
description: Modbus/TCP
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/modbus_tcp.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Modbus/TCP

<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
## `modbus_tcp/config` {#modbus_tcp_config_warp1}


Modbus/TCP server configuration
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/modbus_tcp/config
```
```jsx
{ "enable": true, "port": 502, "table": 0 }
```



#### Write
```bash
curl http://$HOST/modbus_tcp/config -d '{ "enable": true, "port": 502, "table": 0 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/modbus_tcp/config
```
```jsx
{ "enable": true, "port": 502, "table": 0 }
```



#### Write
            With MQTT on <code>$PREFIX/modbus_tcp/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/modbus_tcp/config_update -m '{ "enable": true, "port": 502, "table": 0 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether the Modbus/TCP server is active. For control functionality in addition to state readout, [`evse/modbus_tcp_enabled`](/interfaces/mqtt_http/api_reference/evse.mdx#evse_modbus_tcp_enabled) must also be true.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`port`<br/>int</th>
    <td>
        Port on which the Modbus/TCP server listens for incoming connections. Typically 502.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`table`<br/>int</th>
    <td>
        Register table used by the Modbus/TCP server.
        - **0** - WARP Charger register table
        - **1** - Register table compatible with Bender CC612/613
        - **2** - Register table compatible with Keba C-Series
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`send_illegal_data_address`<br/>bool</th>
    <td>
        Indicates whether the Modbus/TCP server sends IllegalDataAddress errors when unknown registers are read
        - **true** - The server responds with an IllegalDataAddress error when an unknown register is read.
        - **false** - The server responds with value 0 when an unknown register is read.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp2">
## `modbus_tcp/config` {#modbus_tcp_config_warp2}


Modbus/TCP server configuration
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/modbus_tcp/config
```
```jsx
{ "enable": true, "port": 502, "table": 0 }
```



#### Write
```bash
curl http://$HOST/modbus_tcp/config -d '{ "enable": true, "port": 502, "table": 0 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/modbus_tcp/config
```
```jsx
{ "enable": true, "port": 502, "table": 0 }
```



#### Write
            With MQTT on <code>$PREFIX/modbus_tcp/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/modbus_tcp/config_update -m '{ "enable": true, "port": 502, "table": 0 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether the Modbus/TCP server is active. For control functionality in addition to state readout, [`evse/modbus_tcp_enabled`](/interfaces/mqtt_http/api_reference/evse.mdx#evse_modbus_tcp_enabled) must also be true.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`port`<br/>int</th>
    <td>
        Port on which the Modbus/TCP server listens for incoming connections. Typically 502.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`table`<br/>int</th>
    <td>
        Register table used by the Modbus/TCP server.
        - **0** - WARP Charger register table
        - **1** - Register table compatible with Bender CC612/613
        - **2** - Register table compatible with Keba C-Series
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`send_illegal_data_address`<br/>bool</th>
    <td>
        Indicates whether the Modbus/TCP server sends IllegalDataAddress errors when unknown registers are read
        - **true** - The server responds with an IllegalDataAddress error when an unknown register is read.
        - **false** - The server responds with value 0 when an unknown register is read.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `modbus_tcp/config` {#modbus_tcp_config_warp3}


Modbus/TCP server configuration
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/modbus_tcp/config
```
```jsx
{ "enable": true, "port": 502, "table": 0 }
```



#### Write
```bash
curl http://$HOST/modbus_tcp/config -d '{ "enable": true, "port": 502, "table": 0 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/modbus_tcp/config
```
```jsx
{ "enable": true, "port": 502, "table": 0 }
```



#### Write
            With MQTT on <code>$PREFIX/modbus_tcp/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/modbus_tcp/config_update -m '{ "enable": true, "port": 502, "table": 0 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether the Modbus/TCP server is active. For control functionality in addition to state readout, [`evse/modbus_tcp_enabled`](/interfaces/mqtt_http/api_reference/evse.mdx#evse_modbus_tcp_enabled) must also be true.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`port`<br/>int</th>
    <td>
        Port on which the Modbus/TCP server listens for incoming connections. Typically 502.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`table`<br/>int</th>
    <td>
        Register table used by the Modbus/TCP server.
        - **0** - WARP Charger register table
        - **1** - Register table compatible with Bender CC612/613
        - **2** - Register table compatible with Keba C-Series
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`send_illegal_data_address`<br/>bool</th>
    <td>
        Indicates whether the Modbus/TCP server sends IllegalDataAddress errors when unknown registers are read
        - **true** - The server responds with an IllegalDataAddress error when an unknown register is read.
        - **false** - The server responds with value 0 when an unknown register is read.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
Not supported on this hardware!
</TabItem>
<TabItem value="wem2">
Not supported on this hardware!
</TabItem>
<TabItem value="any">
## `modbus_tcp/config` {#modbus_tcp_config_any}


Modbus/TCP server configuration
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/modbus_tcp/config
```
```jsx
{ "enable": true, "port": 502, "table": 0 }
```



#### Write
```bash
curl http://$HOST/modbus_tcp/config -d '{ "enable": true, "port": 502, "table": 0 }'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/modbus_tcp/config
```
```jsx
{ "enable": true, "port": 502, "table": 0 }
```



#### Write
            With MQTT on <code>$PREFIX/modbus_tcp/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/modbus_tcp/config_update -m '{ "enable": true, "port": 502, "table": 0 }'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether the Modbus/TCP server is active. For control functionality in addition to state readout, [`evse/modbus_tcp_enabled`](/interfaces/mqtt_http/api_reference/evse.mdx#evse_modbus_tcp_enabled) must also be true.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`port`<br/>int</th>
    <td>
        Port on which the Modbus/TCP server listens for incoming connections. Typically 502.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`table`<br/>int</th>
    <td>
        Register table used by the Modbus/TCP server.
        
        
        
        - **0** - WARP Charger register table
        - **1** - Register table compatible with Bender CC612/613
        - **2** - Register table compatible with Keba C-Series
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`send_illegal_data_address`<br/>bool</th>
    <td>
        Indicates whether the Modbus/TCP server sends IllegalDataAddress errors when unknown registers are read
        
        
        
        - **true** - The server responds with an IllegalDataAddress error when an unknown register is read.
        - **false** - The server responds with value 0 when an unknown register is read.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "modbus_tcp/config",
    "id": "modbus_tcp_config",
    "level": 2,
    "hardwareType": ["warp1","warp2","warp3"]
},
];
