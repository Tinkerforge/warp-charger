---
description: Dynamic Electricity Prices
---
{/* This file is autogenerated. Edit warp-charger/api_doc_generator/day_ahead_prices.py! */}
import MultilineTabs from '@site/src/components/MultilineTabs';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Dynamic Electricity Prices
The `day_ahead_prices` module allows configuration and reading of dynamic electricity prices.
<Tabs groupId="hardwareType" queryString className="hidden-tabs">
<TabItem value="warp1">
Not supported on this hardware!
</TabItem>
<TabItem value="warp2">
## `day_ahead_prices/config` {#day_ahead_prices_config_warp2}


The dynamic electricity prices configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```

#### Read
```bash
curl http://$HOST/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
```bash
curl http://$HOST/day_ahead_prices/config -d '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
            With MQTT on <code>$PREFIX/day_ahead_prices/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/day_ahead_prices/config_update -m '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether dynamic electricity prices should be used. When enabled, prices are retrieved once per day for the next 24 hours from an external server. Internet access is required to use dynamic electricity prices.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`api_url`<br/>string</th>
    <td>
        Endpoint URL of the dynamic electricity prices server.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`region`<br/>int</th>
    <td>
        Region of the dynamic electricity prices
        - **0** - DE
        - **1** - AT
        - **2** - LU
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices.
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert_id`<br/>int</th>
    <td>
        ID of the CA certificate used to verify the certificate of the electricity price server. See [`certs/state`](/interfaces/mqtt_http/api_reference/certs.mdx#certs_state).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`vat`<br/>int (<abbr title="Hundredth percent">%/100</abbr>)</th>
    <td>
        VAT rate applied to the electricity exchange price.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`grid_costs_and_taxes`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Grid fees and taxes (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_markup`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Electricity supplier price markup (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_base_fee`<br/>int (<abbr title="Cent per month">ct/M</abbr>)</th>
    <td>
        Electricity supplier base fee
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/state` {#day_ahead_prices_state_warp2}


The state of the dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "last_sync": 28898670,
    "last_check": 28898670,
    "next_check": 28900110,
    "current_price": 59999
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_sync`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last updated.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last queried.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`next_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices will be queried next.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current_price`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        The current electricity exchange price.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/prices` {#day_ahead_prices_prices_warp2}


The dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp2-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/prices
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/prices
```
        </TabItem>
    </Tabs>

```jsx
{
    "first_date": 28897860,
    "resolution": 1,
    "prices": [
        11594, 10822, 10731, 10617, 10869, 12531, 16341, 29550,
        36897, 37712, 34864, 37999, 38587, 40491, 37196, 38094,
        43787, 44508, 35992, 34125, 20682, 17120, 15000, 13596,
        12224, 11530, 10996, 10735, 11251, 12367, 17618, 59999,
        65560, 64676, 54383, 46536, 40499, 41530, 49034, 66849,
        81898, 93628, 67418, 55101, 29566, 16940, 15000, 13475
    ]
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`first_date`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating the time for which the first price in the `prices` array is valid.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices in the `prices` array.
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`prices`<br/>int[200] (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Array of dynamic electricity prices for up to two days. The first element of the array is for the time `first_date` and the resolution determines how far apart the elements are in time.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="warp3">
## `day_ahead_prices/config` {#day_ahead_prices_config_warp3}


The dynamic electricity prices configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```

#### Read
```bash
curl http://$HOST/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
```bash
curl http://$HOST/day_ahead_prices/config -d '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
            With MQTT on <code>$PREFIX/day_ahead_prices/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/day_ahead_prices/config_update -m '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether dynamic electricity prices should be used. When enabled, prices are retrieved once per day for the next 24 hours from an external server. Internet access is required to use dynamic electricity prices.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`api_url`<br/>string</th>
    <td>
        Endpoint URL of the dynamic electricity prices server.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`region`<br/>int</th>
    <td>
        Region of the dynamic electricity prices
        - **0** - DE
        - **1** - AT
        - **2** - LU
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices.
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert_id`<br/>int</th>
    <td>
        ID of the CA certificate used to verify the certificate of the electricity price server. See [`certs/state`](/interfaces/mqtt_http/api_reference/certs.mdx#certs_state).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`vat`<br/>int (<abbr title="Hundredth percent">%/100</abbr>)</th>
    <td>
        VAT rate applied to the electricity exchange price.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`grid_costs_and_taxes`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Grid fees and taxes (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_markup`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Electricity supplier price markup (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_base_fee`<br/>int (<abbr title="Cent per month">ct/M</abbr>)</th>
    <td>
        Electricity supplier base fee
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/state` {#day_ahead_prices_state_warp3}


The state of the dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "last_sync": 28898670,
    "last_check": 28898670,
    "next_check": 28900110,
    "current_price": 59999
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_sync`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last updated.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last queried.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`next_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices will be queried next.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current_price`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        The current electricity exchange price.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/prices` {#day_ahead_prices_prices_warp3}


The dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp3-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/prices
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp3/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/prices
```
        </TabItem>
    </Tabs>

```jsx
{
    "first_date": 28897860,
    "resolution": 1,
    "prices": [
        11594, 10822, 10731, 10617, 10869, 12531, 16341, 29550,
        36897, 37712, 34864, 37999, 38587, 40491, 37196, 38094,
        43787, 44508, 35992, 34125, 20682, 17120, 15000, 13596,
        12224, 11530, 10996, 10735, 11251, 12367, 17618, 59999,
        65560, 64676, 54383, 46536, 40499, 41530, 49034, 66849,
        81898, 93628, 67418, 55101, 29566, 16940, 15000, 13475
    ]
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`first_date`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating the time for which the first price in the `prices` array is valid.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices in the `prices` array.
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`prices`<br/>int[200] (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Array of dynamic electricity prices for up to two days. The first element of the array is for the time `first_date` and the resolution determines how far apart the elements are in time.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem">
## `day_ahead_prices/config` {#day_ahead_prices_config_wem}


The dynamic electricity prices configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```

#### Read
```bash
curl http://$HOST/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
```bash
curl http://$HOST/day_ahead_prices/config -d '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
            With MQTT on <code>$PREFIX/day_ahead_prices/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/day_ahead_prices/config_update -m '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether dynamic electricity prices should be used. When enabled, prices are retrieved once per day for the next 24 hours from an external server. Internet access is required to use dynamic electricity prices.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`api_url`<br/>string</th>
    <td>
        Endpoint URL of the dynamic electricity prices server.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`region`<br/>int</th>
    <td>
        Region of the dynamic electricity prices
        - **0** - DE
        - **1** - AT
        - **2** - LU
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices.
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert_id`<br/>int</th>
    <td>
        ID of the CA certificate used to verify the certificate of the electricity price server. See [`certs/state`](/interfaces/mqtt_http/api_reference/certs.mdx#certs_state).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`vat`<br/>int (<abbr title="Hundredth percent">%/100</abbr>)</th>
    <td>
        VAT rate applied to the electricity exchange price.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`grid_costs_and_taxes`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Grid fees and taxes (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_markup`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Electricity supplier price markup (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_base_fee`<br/>int (<abbr title="Cent per month">ct/M</abbr>)</th>
    <td>
        Electricity supplier base fee
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/state` {#day_ahead_prices_state_wem}


The state of the dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "last_sync": 28898670,
    "last_check": 28898670,
    "next_check": 28900110,
    "current_price": 59999
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_sync`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last updated.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last queried.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`next_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices will be queried next.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current_price`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        The current electricity exchange price.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/prices` {#day_ahead_prices_prices_wem}


The dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/prices
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/prices
```
        </TabItem>
    </Tabs>

```jsx
{
    "first_date": 28897860,
    "resolution": 1,
    "prices": [
        11594, 10822, 10731, 10617, 10869, 12531, 16341, 29550,
        36897, 37712, 34864, 37999, 38587, 40491, 37196, 38094,
        43787, 44508, 35992, 34125, 20682, 17120, 15000, 13596,
        12224, 11530, 10996, 10735, 11251, 12367, 17618, 59999,
        65560, 64676, 54383, 46536, 40499, 41530, 49034, 66849,
        81898, 93628, 67418, 55101, 29566, 16940, 15000, 13475
    ]
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`first_date`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating the time for which the first price in the `prices` array is valid.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices in the `prices` array.
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`prices`<br/>int[200] (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Array of dynamic electricity prices for up to two days. The first element of the array is for the time `first_date` and the resolution determines how far apart the elements are in time.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="wem2">
## `day_ahead_prices/config` {#day_ahead_prices_config_wem2}


The dynamic electricity prices configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```

#### Read
```bash
curl http://$HOST/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
```bash
curl http://$HOST/day_ahead_prices/config -d '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
            With MQTT on <code>$PREFIX/day_ahead_prices/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/day_ahead_prices/config_update -m '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether dynamic electricity prices should be used. When enabled, prices are retrieved once per day for the next 24 hours from an external server. Internet access is required to use dynamic electricity prices.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`api_url`<br/>string</th>
    <td>
        Endpoint URL of the dynamic electricity prices server.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`region`<br/>int</th>
    <td>
        Region of the dynamic electricity prices
        - **0** - DE
        - **1** - AT
        - **2** - LU
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices.
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert_id`<br/>int</th>
    <td>
        ID of the CA certificate used to verify the certificate of the electricity price server. See [`certs/state`](/interfaces/mqtt_http/api_reference/certs.mdx#certs_state).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`vat`<br/>int (<abbr title="Hundredth percent">%/100</abbr>)</th>
    <td>
        VAT rate applied to the electricity exchange price.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`grid_costs_and_taxes`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Grid fees and taxes (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_markup`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Electricity supplier price markup (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_base_fee`<br/>int (<abbr title="Cent per month">ct/M</abbr>)</th>
    <td>
        Electricity supplier base fee
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/state` {#day_ahead_prices_state_wem2}


The state of the dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "last_sync": 28898670,
    "last_check": 28898670,
    "next_check": 28900110,
    "current_price": 59999
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_sync`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last updated.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last queried.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`next_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices will be queried next.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current_price`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        The current electricity exchange price.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/prices` {#day_ahead_prices_prices_wem2}


The dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. wem2-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/prices
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. wem2/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/prices
```
        </TabItem>
    </Tabs>

```jsx
{
    "first_date": 28897860,
    "resolution": 1,
    "prices": [
        11594, 10822, 10731, 10617, 10869, 12531, 16341, 29550,
        36897, 37712, 34864, 37999, 38587, 40491, 37196, 38094,
        43787, 44508, 35992, 34125, 20682, 17120, 15000, 13596,
        12224, 11530, 10996, 10735, 11251, 12367, 17618, 59999,
        65560, 64676, 54383, 46536, 40499, 41530, 49034, 66849,
        81898, 93628, 67418, 55101, 29566, 16940, 15000, 13475
    ]
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`first_date`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating the time for which the first price in the `prices` array is valid.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices in the `prices` array.
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`prices`<br/>int[200] (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Array of dynamic electricity prices for up to two days. The first element of the array is for the time `first_date` and the resolution determines how far apart the elements are in time.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
<TabItem value="any">
## `day_ahead_prices/config` {#day_ahead_prices_config_any}


The dynamic electricity prices configuration.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```

#### Read
```bash
curl http://$HOST/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
```bash
curl http://$HOST/day_ahead_prices/config -d '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```

#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/config
```
```jsx
{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}
```



#### Write
            With MQTT on <code>$PREFIX/day_ahead_prices/config<strong>_update</strong></code>


```bash
mosquitto_pub -h $BROKER -t $PREFIX/day_ahead_prices/config_update -m '{
    "enable": true,
    "api_url": "https://api.warp-charger.com/",
    "region": 0,
    "resolution": 1,
    "cert_id": -1,
    "vat": 1900,
    "grid_costs_and_taxes": 18783,
    "supplier_markup": 0,
    "supplier_base_fee": 1564
}'
```



        </TabItem>
    </Tabs>
:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`enable`<br/>bool</th>
    <td>
        Indicates whether dynamic electricity prices should be used. When enabled, prices are retrieved once per day for the next 24 hours from an external server. Internet access is required to use dynamic electricity prices.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`api_url`<br/>string</th>
    <td>
        Endpoint URL of the dynamic electricity prices server.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`region`<br/>int</th>
    <td>
        Region of the dynamic electricity prices
        
        
        
        - **0** - DE
        - **1** - AT
        - **2** - LU
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices.
        
        
        
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`cert_id`<br/>int</th>
    <td>
        ID of the CA certificate used to verify the certificate of the electricity price server. See [`certs/state`](/interfaces/mqtt_http/api_reference/certs.mdx#certs_state).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`vat`<br/>int (<abbr title="Hundredth percent">%/100</abbr>)</th>
    <td>
        VAT rate applied to the electricity exchange price.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`grid_costs_and_taxes`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Grid fees and taxes (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_markup`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Electricity supplier price markup (gross if VAT rate is specified).
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`supplier_base_fee`<br/>int (<abbr title="Cent per month">ct/M</abbr>)</th>
    <td>
        Electricity supplier base fee
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/state` {#day_ahead_prices_state_any}


The state of the dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/state
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/state
```
        </TabItem>
    </Tabs>

```jsx
{
    "last_sync": 28898670,
    "last_check": 28898670,
    "next_check": 28900110,
    "current_price": 59999
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_sync`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last updated.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`last_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices were last queried.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`next_check`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating when the electricity exchange prices will be queried next.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`current_price`<br/>int (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        The current electricity exchange price.
    </td>
</tr>
    </tbody>
</table>

<br/><br/><br/>
## `day_ahead_prices/prices` {#day_ahead_prices_prices_any}


The dynamic electricity prices.
:::info[Example]
    <Tabs groupId="apiType" queryString className="hidden-tabs">
        <TabItem value="http" label="HTTP (curl)">
```bash
# $HOST e.g. warp-AbCd
```
#### Read
```bash
curl http://$HOST/day_ahead_prices/prices
```
        </TabItem>
        <TabItem value="mqtt" label="MQTT (mosquitto)">
```bash
# $BROKER e.g. my_mosquitto.localdomain
# $PREFIX e.g. warp/AbCd
```
#### Read
```bash
mosquitto_sub -v -C 1 -h $BROKER -t $PREFIX/day_ahead_prices/prices
```
        </TabItem>
    </Tabs>

```jsx
{
    "first_date": 28897860,
    "resolution": 1,
    "prices": [
        11594, 10822, 10731, 10617, 10869, 12531, 16341, 29550,
        36897, 37712, 34864, 37999, 38587, 40491, 37196, 38094,
        43787, 44508, 35992, 34125, 20682, 17120, 15000, 13596,
        12224, 11530, 10996, 10735, 11251, 12367, 17618, 59999,
        65560, 64676, 54383, 46536, 40499, 41530, 49034, 66849,
        81898, 93628, 67418, 55101, 29566, 16940, 15000, 13475
    ]
}
```


:::


<table>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Meaning</th>
        </tr>
    </thead>
    <tbody>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`first_date`<br/>int</th>
    <td>
        A Unix timestamp in <strong>minutes</strong> indicating the time for which the first price in the `prices` array is valid.
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`resolution`<br/>int</th>
    <td>
        Time resolution of the dynamic electricity prices in the `prices` array.
        
        
        
        - **0** - 15 minutes
        - **1** - 60 minutes
    </td>
</tr>

<tr>
    <th scope="row" style={{whiteSpace: "nowrap"}}>`prices`<br/>int[200] (<abbr title="Thousandth cent per kilowatt-hour">ct/kWh/1000</abbr>)</th>
    <td>
        Array of dynamic electricity prices for up to two days. The first element of the array is for the time `first_date` and the resolution determines how far apart the elements are in time.
    </td>
</tr>
    </tbody>
</table>
</TabItem>
</Tabs>

export const toc = [
    {
    "value": "day_ahead_prices/config",
    "id": "day_ahead_prices_config",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},{
    "value": "day_ahead_prices/state",
    "id": "day_ahead_prices_state",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},{
    "value": "day_ahead_prices/prices",
    "id": "day_ahead_prices_prices",
    "level": 2,
    "hardwareType": ["warp2","warp3","wem","wem2"]
},
];
